---
phase: 03-dataclasses-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/biblio_validador/core/__init__.py
  - src/biblio_validador/core/enums.py
  - src/biblio_validador/core/dataclasses.py
  - tests/core/__init__.py
  - tests/core/test_dataclasses.py
autonomous: true
requirements: [CORE-02, CORE-03]
nyquist_compliant: true
tags: [dataclasses, enums, modeling, core, python313]
must_haves:
  truths:
    - "Violacao(arquivo=Path, linha_inicio=1, ...) instancia com frozen=True e slots=True"
    - "Patch instancia com slots=True (sem frozen) e aceita EstadoPatch.{PROPOSTO|ACEITO|REJEITADO|SUPRIMIDO}"
    - "dataclasses.asdict(violacao) produz dict estruturalmente correto sem TypeError"
    - "json.dumps(violacao.to_dict()) produz string serializável e round-trip preserva campos"
    - "Mutar violacao.regra_id levanta dataclasses.FrozenInstanceError"
    - "Severidade.peso() retorna 0..3 monotônica (INFO<ALERTA<ERRO<CRITICO)"
    - "from biblio_validador.core import Violacao, Patch, Severidade, EstadoPatch funciona"
  artifacts:
    - path: "src/biblio_validador/core/__init__.py"
      provides: "Re-export público de Violacao, Patch, Severidade, EstadoPatch"
      contains: "__all__"
    - path: "src/biblio_validador/core/enums.py"
      provides: "Severidade + EstadoPatch (enum.Enum string snake_case) + _PESOS_SEVERIDADE"
      contains: "class Severidade(Enum):"
    - path: "src/biblio_validador/core/dataclasses.py"
      provides: "Violacao (frozen+slots) + Patch (slots) + _normalize_for_json + to_dict()"
      contains: "@dataclass(frozen=True, slots=True)"
    - path: "tests/core/__init__.py"
      provides: "Marcador de pacote pytest (vazio)"
      min_lines: 0
    - path: "tests/core/test_dataclasses.py"
      provides: "12+ testes cobrindo CORE-02/CORE-03 success criteria + invariantes + contract tests"
      contains: "def test_violacao_instancia_com_frozen_e_slots"
  key_links:
    - from: "src/biblio_validador/core/dataclasses.py"
      to: "src/biblio_validador/core/enums.py"
      via: "from biblio_validador.core.enums import EstadoPatch, Severidade"
      pattern: "from biblio_validador\\.core\\.enums import"
    - from: "src/biblio_validador/core/__init__.py"
      to: "src/biblio_validador/core/dataclasses.py"
      via: "from biblio_validador.core.dataclasses import Patch, Violacao"
      pattern: "from biblio_validador\\.core\\.dataclasses import"
    - from: "tests/core/test_dataclasses.py"
      to: "src/biblio_validador/core/__init__.py"
      via: "from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao"
      pattern: "from biblio_validador\\.core import"
---

<objective>
Phase 3 entrega o módulo `src/biblio_validador/core/` com as dataclasses e
enums nucleares que circulam pelo pipeline de validação:

- `Violacao` (frozen+slots) — saída dos validadores M2-M4
- `Patch` (slots, mutável) — saída dos fixers M5-M7
- `Severidade` (enum.Enum) — classificação de Violacao com `peso()` para ordenação
- `EstadoPatch` (enum.Enum) — ciclo de vida do Patch (PROPOSTO/ACEITO/REJEITADO/SUPRIMIDO)
- Helper `_normalize_for_json` + métodos `to_dict()` para serialização JSON
- Invariantes runtime via `__post_init__` com diagnóstico contextual

Purpose: bloquear contrato de tipos antes de Phase 4 (ABCs ValidadorBase/FixerBase)
e Phase 6+ (validador piloto) consumirem `Violacao`/`Patch` em assinaturas.
Modelagem pura, zero I/O, zero deps novas (stdlib + Phase 1 deps existentes).

Output: 5 arquivos novos (3 source + 2 test) cobrindo 100% dos 33 D-XX
locked em CONTEXT.md, com coverage >= 95% e mypy strict + ruff line-length=80.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/03-dataclasses-core/03-CONTEXT.md
@.planning/phases/03-dataclasses-core/03-RESEARCH.md
@.planning/phases/03-dataclasses-core/03-PATTERNS.md
@.planning/phases/03-dataclasses-core/03-VALIDATION.md

@.planning/phases/01-bootstrap/01-CONTEXT.md
@.planning/phases/02-parser-markdown/02-CONTEXT.md

@src/biblio_validador/parser/types.py
@src/biblio_validador/parser/__init__.py
@pyproject.toml

<interfaces>
<!-- Contracts produced by this plan, consumed by Phase 4+ -->

Phase 3 produces (consumed by Phases 4, 5, 6, 8, 19, 48, 51, 58):

```python
# src/biblio_validador/core/enums.py
from enum import Enum

class Severidade(Enum):
    INFO = "info"
    ALERTA = "alerta"
    ERRO = "erro"
    CRITICO = "critico"
    def peso(self) -> int: ...  # 0..3 monotônico

class EstadoPatch(Enum):
    PROPOSTO = "proposto"
    ACEITO = "aceito"
    REJEITADO = "rejeitado"
    SUPRIMIDO = "suprimido"
```

```python
# src/biblio_validador/core/dataclasses.py
from dataclasses import dataclass
from pathlib import Path
from typing import Any
from biblio_validador.core.enums import EstadoPatch, Severidade

@dataclass(frozen=True, slots=True)
class Violacao:
    arquivo: Path
    linha_inicio: int       # 1-based
    linha_fim: int          # 1-based, inclusivo
    col_inicio: int         # 1-based, half-open
    col_fim: int            # 1-based, half-open exclusivo
    trecho_violador: str
    regra_id: str           # ex.: "cst_012", "lex_001", "coc_007"
    regra_nome: str
    severidade: Severidade
    sugestoes: tuple[str, ...] = ()
    principio_canonico_violado: str | None = None
    def __post_init__(self) -> None: ...     # raise ValueError com regra_id
    def to_dict(self) -> dict[str, Any]: ... # JSON-ready

@dataclass(slots=True)
class Patch:
    arquivo: Path
    linha: int              # 1-based, single-line
    col_inicio: int         # 1-based, half-open
    col_fim: int            # 1-based, half-open
    texto_original: str
    texto_substituto: str
    motivo: str
    confianca: float        # 0.0..1.0, SEM default (D-17 anti-hidden-default)
    requer_revisao_humana: bool = False
    estado: EstadoPatch = EstadoPatch.PROPOSTO
    def __post_init__(self) -> None: ...
    def to_dict(self) -> dict[str, Any]: ...
```

```python
# src/biblio_validador/core/__init__.py
from biblio_validador.core.dataclasses import Patch, Violacao
from biblio_validador.core.enums import EstadoPatch, Severidade
__all__ = ["EstadoPatch", "Patch", "Severidade", "Violacao"]
```

Inherited from Phase 2 (used as template — DO NOT MODIFY):

```python
# src/biblio_validador/parser/types.py — TEMPLATE for Violacao decorator
@dataclass(frozen=True, slots=True)
class Paragrafo:
    arquivo: Path
    indice: int
    tipo: TipoSecao
    nivel_heading: int | None
    texto: str
    linha_inicio: int       # 1-based — informa convenção D-09
    linha_fim: int          # 1-based, inclusivo
    offset_bytes: int
    len_bytes: int
    ref_nota: str | None
    paragrafo_pai_idx: int | None
```

```python
# src/biblio_validador/parser/__init__.py — TEMPLATE for re-export D-03
"""Sub-pacote parser — segmentação de .md/.tex em list[Paragrafo]."""
from biblio_validador.parser.markdown import ParserMd
from biblio_validador.parser.types import Paragrafo, TipoSecao
__all__ = ["ParserMd", "Paragrafo", "TipoSecao"]
```
</interfaces>

<critical_constraints>
**Naming pitfall (RESEARCH Anti-Patterns line 583-588):**
O módulo se chama `core/dataclasses.py` mas precisa importar do stdlib
`dataclasses`. ALWAYS use `from dataclasses import dataclass, field, asdict`
(NUNCA `import dataclasses` bare — causaria self-import recursivo).

**Forbidden in this phase:**
- Adicionar novas dependências em pyproject.toml (locked: stdlib only)
- Modificar pyproject.toml (já tem tudo: pytest, pytest-cov, mypy, ruff)
- Criar `core/types.py` ou outras subdivisões (D-04: 4 símbolos cabem em 2 arquivos)
- `loguru` em `core/` (D-33: zero side-effect, sem I/O, sem log)
- `from_dict()` reverso (D-27: deferred até Phase 58)
- Enforcement de transições de EstadoPatch (D-21: deferred até Phase 51)
- `__hash__` customizado em Patch (Pitfall 4: Python desabilita corretamente)
- `IntEnum` para Severidade (D-16 + Anti-Patterns rejeitam)
- `StrEnum` para Severidade/EstadoPatch (D-14/D-20 rejeitam — quebra convenção Phase 2)
- `list[str]` em Violacao (Pitfall 1: defeats frozen — usar `tuple[str, ...]`)
- `field(default_factory=tuple)` para coleção vazia (Anti-Patterns: usar `default=()`)
- Default `confianca: float = 1.0` em Patch (D-17: anti-hidden-default)
- Calcular campo derivado em `__post_init__` (Pitfall 3: usaria `object.__setattr__` hack)
- Subclasses de Violacao/Patch (deferred até Phase 18)
- Campo `created_at: datetime` (deferred — telemetria pertence ao orchestrator)
</critical_constraints>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum | Phase 3 é pura modelagem in-process; sem I/O, sem rede, sem deserialização de input não-confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Denial of Service | Construção de grafo massivo de Violacao | accept | Out-of-scope desta phase: orchestrator (Phase 50) controla cardinalidade via batch limits; sem cap necessário em modelo puro |
| T-3-02 | Information Disclosure | `to_dict()` produzindo JSON | mitigate | `json.dumps()` (chamado pelo consumidor, não aqui) escapa caracteres controlados corretamente; `_normalize_for_json` apenas converte tipos (Path->str, Enum->value), sem injeção de marcador |

Nenhum threat de severidade alta/média. Phase 3 passa ASVS L1 trivialmente
(zero superfície de ataque: in-process, sem I/O, sem deserialização não-confiável).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/biblio_validador/core/enums.py com Severidade + EstadoPatch + _PESOS_SEVERIDADE</name>

  <wave>1</wave>

  <files>src/biblio_validador/core/enums.py</files>

  <read_first>
    - .planning/phases/03-dataclasses-core/03-CONTEXT.md (D-14, D-15, D-16, D-20, D-21, D-31, D-32, D-33)
    - .planning/phases/03-dataclasses-core/03-RESEARCH.md (Pattern 3 lines 396-457; Anti-Patterns lines 564-569 sobre IntEnum/StrEnum; Pitfall 6 lines 712-731)
    - .planning/phases/03-dataclasses-core/03-PATTERNS.md ("src/biblio_validador/core/enums.py" section lines 53-114)
    - src/biblio_validador/parser/types.py (template para `class Name(Enum):` lines 8-24)
    - .planning/phases/02-parser-markdown/02-CONTEXT.md (D-03 — convenção `enum.Enum` com value snake_case herdada)
  </read_first>

  <behavior>
    - Test 1: `Severidade.INFO.value == "info"`, `Severidade.ALERTA.value == "alerta"`, `Severidade.ERRO.value == "erro"`, `Severidade.CRITICO.value == "critico"`
    - Test 2: `Severidade.INFO.peso() == 0`, `Severidade.ALERTA.peso() == 1`, `Severidade.ERRO.peso() == 2`, `Severidade.CRITICO.peso() == 3` (monotônico crescente — covered in test_severidade_peso_monotonica)
    - Test 3: `EstadoPatch.PROPOSTO.value == "proposto"`, `EstadoPatch.ACEITO.value == "aceito"`, `EstadoPatch.REJEITADO.value == "rejeitado"`, `EstadoPatch.SUPRIMIDO.value == "suprimido"`
    - Test 4: `isinstance(Severidade.INFO, Enum)` é True; `not isinstance(Severidade.INFO, int)` (NÃO é IntEnum); `not isinstance(Severidade.INFO, str)` (NÃO é StrEnum)
    - Test 5: `repr(Severidade.ERRO)` contém `'erro'` literal (preserva valor string em logs)
  </behavior>

  <action>
Criar arquivo `src/biblio_validador/core/enums.py` com conteúdo exato (replicando
o template de `parser/types.py` linhas 1-24, swapping symbols per D-15/D-20).

Estrutura completa do arquivo:

```python
"""Enums core: Severidade + EstadoPatch (D-14, D-20)."""

from enum import Enum


class Severidade(Enum):
    """Classificação de severidade de uma Violacao (D-15).

    Ordem de prioridade (peso() crescente):
        INFO < ALERTA < ERRO < CRITICO

    Não usa IntEnum (D-16): IntEnum mistura semântica de ranking com
    representação textual em logs. peso() separa as preocupações.
    Não usa StrEnum (D-14): perde marcador de tipo no repr; quebra a
    convenção herdada de TipoSecao (Phase 2 D-03).
    """

    INFO = "info"
    ALERTA = "alerta"
    ERRO = "erro"
    CRITICO = "critico"

    def peso(self) -> int:
        """Peso para ordenação determinística (0..3, monotônico crescente)."""
        return _PESOS_SEVERIDADE[self]


# Tabela de pesos como módulo-level dict — evita lookup em loop e
# mantém a ordenação canônica em local único (RESEARCH Pattern 3).
_PESOS_SEVERIDADE: dict[Severidade, int] = {
    Severidade.INFO: 0,
    Severidade.ALERTA: 1,
    Severidade.ERRO: 2,
    Severidade.CRITICO: 3,
}


class EstadoPatch(Enum):
    """Ciclo de vida de um Patch (D-20).

    Transições válidas (documentadas, não enforçadas em runtime nesta
    phase — enforcement adiado para Phase 51 ORC-02 se necessário):

        PROPOSTO -> ACEITO | REJEITADO | SUPRIMIDO
        ACEITO   -> SUPRIMIDO  (raro: aplicação falhou byte-exact)
        terminais: REJEITADO, SUPRIMIDO
    """

    PROPOSTO = "proposto"
    ACEITO = "aceito"
    REJEITADO = "rejeitado"
    SUPRIMIDO = "suprimido"
```

**Decisões aplicadas:**
- D-14, D-20: `enum.Enum` (NÃO `StrEnum`/`IntEnum`); valores string snake_case
- D-15: 4 valores `INFO/ALERTA/ERRO/CRITICO`
- D-16: método `peso() -> int` retornando 0..3 monotônico via dict module-level
- D-21: transições documentadas em docstring, NÃO enforçadas em runtime
- D-31: Type hints 100% (`peso(self) -> int`, `_PESOS_SEVERIDADE: dict[Severidade, int]`)
- D-32: line-length <= 80 chars (quebrar docstring se necessário)
- D-33: sem `loguru`, sem `print`, sem I/O

**Conventions herdadas (PATTERNS.md):**
- One-line PT-BR module docstring
- Class docstring multi-line PT-BR explicando invariantes e referência (D-15)/(D-20)
- ALL_CAPS_SNAKE para member names; `lower_snake_case` strings para values
- Absolute imports apenas (`from enum import Enum` — stdlib OK)
- Sem `__version__`, sem side-effect
  </action>

  <verify>
    <automated>uv run python -c "from biblio_validador.core.enums import Severidade, EstadoPatch; assert Severidade.INFO.value == 'info'; assert Severidade.CRITICO.peso() == 3; assert EstadoPatch.PROPOSTO.value == 'proposto'; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/biblio_validador/core/enums.py` retorna 0 (arquivo existe)
    - `grep -c 'class Severidade(Enum):' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'class EstadoPatch(Enum):' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'def peso(self) -> int:' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c '_PESOS_SEVERIDADE: dict\[Severidade, int\]' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'INFO = "info"' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'CRITICO = "critico"' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'PROPOSTO = "proposto"' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -c 'SUPRIMIDO = "suprimido"' src/biblio_validador/core/enums.py` retorna `1`
    - `grep -E 'IntEnum|StrEnum' src/biblio_validador/core/enums.py` retorna 1 (não-zero exit code: NÃO encontra IntEnum/StrEnum)
    - `grep -E 'loguru|logger|print\(' src/biblio_validador/core/enums.py` retorna 1 (NÃO encontra side-effects)
    - `uv run ruff check src/biblio_validador/core/enums.py` exit 0
    - `uv run ruff format --check src/biblio_validador/core/enums.py` exit 0
    - `uv run mypy src/biblio_validador/core/enums.py` exit 0
    - `uv run python -c "from biblio_validador.core.enums import Severidade; pesos = [Severidade.INFO.peso(), Severidade.ALERTA.peso(), Severidade.ERRO.peso(), Severidade.CRITICO.peso()]; assert pesos == [0,1,2,3], pesos"` exit 0
  </acceptance_criteria>

  <done>
    Arquivo `src/biblio_validador/core/enums.py` criado contendo `Severidade` (4 membros + método `peso()`) e `EstadoPatch` (4 membros), ambos `enum.Enum` com values string snake_case. Tabela `_PESOS_SEVERIDADE` ao nível do módulo. Type hints 100%. ruff + mypy strict passam.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar src/biblio_validador/core/dataclasses.py com Violacao + Patch + _normalize_for_json</name>

  <wave>1</wave>

  <files>src/biblio_validador/core/dataclasses.py</files>

  <read_first>
    - .planning/phases/03-dataclasses-core/03-CONTEXT.md (D-05..D-13, D-17..D-19, D-22..D-27, D-31..D-33)
    - .planning/phases/03-dataclasses-core/03-RESEARCH.md (Pattern 1 lines 233-318 Violacao; Pattern 2 lines 322-388 Patch; Pattern 4 lines 459-479 __post_init__; Pattern 5 lines 481-529 _normalize_for_json; Pitfalls 1-3 lines 607-672; Anti-Patterns lines 548-588)
    - .planning/phases/03-dataclasses-core/03-PATTERNS.md ("src/biblio_validador/core/dataclasses.py" section lines 117-181)
    - src/biblio_validador/parser/types.py (template para `@dataclass(frozen=True, slots=True)` lines 27-45)
    - src/biblio_validador/core/enums.py (criado na Task 1 — necessário para import)
  </read_first>

  <behavior>
    - Test 1 (CORE-02): instanciar `Violacao(arquivo=Path("a.md"), linha_inicio=1, linha_fim=1, col_inicio=1, col_fim=5, trecho_violador="abcd", regra_id="lex_001", regra_nome="X", severidade=Severidade.ERRO)` — funciona; `v.sugestoes == ()`; `v.principio_canonico_violado is None`; `hasattr(v, "__slots__")`; `not hasattr(v, "__dict__")`
    - Test 2 (CORE-02): mutar `v.regra_id = "outro"` levanta `dataclasses.FrozenInstanceError`
    - Test 3 (CORE-02): `dataclasses.asdict(v)` retorna `dict` com todas as 11 chaves de Violacao (estruturalmente correto)
    - Test 4 (CORE-02): `json.dumps(v.to_dict())` retorna string; `json.loads(...)["regra_id"] == "lex_001"` round-trip
    - Test 5 (CORE-02): `Violacao(linha_inicio=0, ...)` raises `ValueError` com mensagem contendo `"linha_inicio"` E o `regra_id` para diagnóstico
    - Test 6 (CORE-02): `Violacao(col_fim=2, col_inicio=5, ...)` raises `ValueError` com `"col_fim"` na mensagem
    - Test 7 (CORE-03): instanciar `Patch(arquivo=Path("a.md"), linha=1, col_inicio=1, col_fim=5, texto_original="abcd", texto_substituto="efgh", motivo="cst_012", confianca=1.0)` — `p.estado == EstadoPatch.PROPOSTO`; `p.requer_revisao_humana is False`
    - Test 8 (CORE-03): `p.estado = EstadoPatch.ACEITO` muta sem erro (Patch não é frozen); `p.atributo_novo = 42` raises `AttributeError` (slots proíbe novo atributo)
    - Test 9 (CORE-03): `Patch(confianca=1.5, ...)` raises `ValueError` com `"confianca"` na mensagem
    - Test 10 (CORE-03): `Patch(texto_original="", ...)` raises `ValueError` com `"texto_original"` na mensagem
    - Test 11 (CORE-03): `Patch(motivo="", ...)` raises `ValueError` com `"motivo"` na mensagem
    - Test 12 (CORE-03): `json.dumps(p.to_dict())` retorna string; `json.loads(...)["estado"] == "proposto"` (Enum normalizado para value); `json.loads(...)["arquivo"] == "a.md"` (Path normalizado para str)
    - Contract test 1: `Violacao.__dataclass_params__.frozen is True`
    - Contract test 2: `isinstance(v.sugestoes, tuple)` — não regrediu para list
    - Contract test 3: `Violacao` é hashable (`hash(v)` funciona; dois Violacoes idênticos em set deduplicam)
    - Contract test 4: `Patch` é unhashable (`hash(p)` raises `TypeError`)
  </behavior>

  <action>
Criar arquivo `src/biblio_validador/core/dataclasses.py` com `Violacao` (frozen
+slots), `Patch` (slots-only mutable), helper privado `_normalize_for_json`,
e métodos `to_dict()` em ambas dataclasses.

**Estrutura completa do arquivo:**

```python
"""Dataclasses core: Violacao + Patch + helper _normalize_for_json (D-05..D-27).

CRITICAL: módulo se chama `dataclasses.py` mas importa do stdlib
`dataclasses`. SEMPRE usar `from dataclasses import dataclass, asdict`
(NUNCA `import dataclasses` bare — causaria self-import recursivo).
"""

from dataclasses import asdict, dataclass
from enum import Enum
from pathlib import Path
from typing import Any

from biblio_validador.core.enums import EstadoPatch, Severidade


def _normalize_for_json(obj: Any) -> Any:
    """Converte recursivamente Path/Enum/tuple em str/value/list (D-25).

    Pós-processa a saída de dataclasses.asdict() para que json.dumps()
    funcione sem encoder customizado. Recursivo: desce em dicts, listas
    e tuples para normalizar estruturas aninhadas (RESEARCH Pattern 5).
    """
    if isinstance(obj, Path):
        return str(obj)
    if isinstance(obj, Enum):
        return obj.value
    if isinstance(obj, tuple):
        return [_normalize_for_json(x) for x in obj]
    if isinstance(obj, list):
        return [_normalize_for_json(x) for x in obj]
    if isinstance(obj, dict):
        return {k: _normalize_for_json(v) for k, v in obj.items()}
    return obj


@dataclass(frozen=True, slots=True)
class Violacao:
    """Violação de regra canônica detectada por um validador (CORE-02 / D-09).

    Imutável após construção (frozen=True, D-05). Coleções são tuple
    (D-07: tuple[str, ...] em vez de list[str] — frozen só impede
    reatribuir o atributo, tuple fecha o buraco da imutabilidade).

    Convenção col half-open (D-10):
        col_inicio = primeiro caractere violador (1-based)
        col_fim    = primeiro caractere NAO violador (1-based, exclusivo)
        Slicing Python: linha[col_inicio - 1 : col_fim - 1]

    Invariante intra-linha (D-22):
        len(trecho_violador) == col_fim - col_inicio
        quando linha_inicio == linha_fim.

    Nota sobre serialização (D-26):
        - dataclasses.asdict(viol) retorna dict ESTRUTURALMENTE CORRETO
          mas com Path/Enum/tuple preservados (json.dumps falha).
        - viol.to_dict() retorna dict JSON-READY (Path->str, Enum->value,
          tuple->list) via _normalize_for_json.
    """

    arquivo: Path
    linha_inicio: int  # 1-based
    linha_fim: int  # 1-based, inclusivo
    col_inicio: int  # 1-based, half-open
    col_fim: int  # 1-based, half-open exclusivo
    trecho_violador: str  # texto exato detectado
    regra_id: str  # ex.: "cst_012", "lex_001", "coc_007"
    regra_nome: str  # nome humanizado lido do JSON-fonte
    severidade: Severidade
    sugestoes: tuple[str, ...] = ()  # D-07: tuple, não list
    principio_canonico_violado: str | None = None  # AGR-02 preenche

    def __post_init__(self) -> None:
        """Valida invariantes (D-22) com mensagem incluindo regra_id."""
        if self.linha_inicio < 1:
            raise ValueError(
                f"linha_inicio < 1 em {self.regra_id}: "
                f"{self.linha_inicio}"
            )
        if self.linha_fim < self.linha_inicio:
            raise ValueError(
                f"linha_fim < linha_inicio em {self.regra_id}: "
                f"{self.linha_fim} < {self.linha_inicio}"
            )
        if self.col_inicio < 1:
            raise ValueError(
                f"col_inicio < 1 em {self.regra_id}: {self.col_inicio}"
            )
        if self.col_fim < self.col_inicio:
            raise ValueError(
                f"col_fim < col_inicio em {self.regra_id}: "
                f"{self.col_fim} < {self.col_inicio}"
            )
        if not self.regra_id.strip():
            raise ValueError("regra_id vazio")
        if (
            self.linha_inicio == self.linha_fim
            and len(self.trecho_violador)
            != self.col_fim - self.col_inicio
        ):
            raise ValueError(
                f"trecho_violador len {len(self.trecho_violador)} != "
                f"col_fim - col_inicio "
                f"{self.col_fim - self.col_inicio} em {self.regra_id}"
            )

    def to_dict(self) -> dict[str, Any]:
        """Serialização JSON-ready (D-25)."""
        return _normalize_for_json(asdict(self))


@dataclass(slots=True)
class Patch:
    """Patch proposto por um fixer (CORE-03 / D-17, D-18).

    NAO frozen (D-06): estado e requer_revisao_humana podem ser mutados
    pelo orchestrator (Phase 51) e pelo pos-validador LLM (Phase 48).
    Demais campos sao read-only por convencao (nao enforcado em runtime).

    Single-line (D-18): patch que abrange varias linhas e decomposto em
    multiplos Patch pelo fixer; PatchAplicador (Phase 5) ordena reverso
    por (linha, col_inicio).

    Transicoes validas de `estado` (documentadas, nao enforcadas - D-21):
        PROPOSTO -> ACEITO | REJEITADO | SUPRIMIDO
        ACEITO   -> SUPRIMIDO  (raro: aplicacao falhou byte-exact)
        terminais: REJEITADO, SUPRIMIDO

    Patch e UNHASHABLE (Pitfall 4): Python desabilita __hash__ porque
    dataclass mutavel com eq=True nao pode garantir contrato de hash
    estavel. Para deduplicar use tupla (arquivo, linha, col_inicio).
    """

    arquivo: Path
    linha: int  # 1-based, single-line
    col_inicio: int  # 1-based, half-open
    col_fim: int  # 1-based, half-open exclusivo
    texto_original: str  # bytes que serao substituidos
    texto_substituto: str  # bytes que entram no lugar
    motivo: str  # narrativa da regra-origem
    confianca: float  # 0.0..1.0, SEM default (D-17 anti-hidden-default)
    requer_revisao_humana: bool = False
    estado: EstadoPatch = EstadoPatch.PROPOSTO

    def __post_init__(self) -> None:
        """Valida invariantes (D-23)."""
        if self.linha < 1:
            raise ValueError(f"linha < 1: {self.linha}")
        if self.col_inicio < 1:
            raise ValueError(f"col_inicio < 1: {self.col_inicio}")
        if self.col_fim < self.col_inicio:
            raise ValueError(
                f"col_fim < col_inicio: "
                f"{self.col_fim} < {self.col_inicio}"
            )
        if not 0.0 <= self.confianca <= 1.0:
            raise ValueError(
                f"confianca fora de [0,1]: {self.confianca}"
            )
        if not self.texto_original:
            raise ValueError(
                "texto_original vazio (use [] em vez de Patch nulo)"
            )
        if not self.motivo.strip():
            raise ValueError("motivo vazio")

    def to_dict(self) -> dict[str, Any]:
        """Serialização JSON-ready (D-25)."""
        return _normalize_for_json(asdict(self))
```

**Decisões aplicadas:**
- D-05: Violacao = `@dataclass(frozen=True, slots=True)`
- D-06: Patch = `@dataclass(slots=True)` (sem frozen, mutável)
- D-07: `tuple[str, ...] = ()` em sugestoes (NUNCA list[str])
- D-09: 11 campos de Violacao em ordem fixa
- D-10: convenção col half-open documentada na docstring
- D-11: `arquivo: Path` (não str)
- D-13: sem campo `mensagem`
- D-17: 10 campos de Patch; `confianca` SEM default
- D-18: single-line documentado em docstring
- D-19: sem campo `regra_id_origem`
- D-22, D-23: __post_init__ valida invariantes via raise ValueError com regra_id (Violacao) ou contexto (Patch)
- D-24: __post_init__ apenas valida, NÃO muta (sem `object.__setattr__`)
- D-25: método `to_dict()` em ambas + helper `_normalize_for_json` recursivo
- D-26: docstring de Violacao explicita ambiguidade asdict vs to_dict
- D-27: NÃO implementar `from_dict()` (deferred)
- D-31: type hints 100% (`-> None`, `-> dict[str, Any]`, `-> Any`)
- D-32: line-length <= 80 chars
- D-33: sem `loguru`, sem `print`

**Critical:**
- Importar `from dataclasses import asdict, dataclass` (NUNCA `import dataclasses` bare)
- Lazy `from biblio_validador.core.enums import EstadoPatch, Severidade` (top-level OK aqui — não há dep circular)
  </action>

  <verify>
    <automated>uv run python -c "from pathlib import Path; from biblio_validador.core.dataclasses import Violacao, Patch; from biblio_validador.core.enums import Severidade, EstadoPatch; v = Violacao(arquivo=Path('a.md'), linha_inicio=1, linha_fim=1, col_inicio=1, col_fim=5, trecho_violador='abcd', regra_id='lex_001', regra_nome='X', severidade=Severidade.ERRO); assert v.sugestoes == (); p = Patch(arquivo=Path('a.md'), linha=1, col_inicio=1, col_fim=5, texto_original='abcd', texto_substituto='efgh', motivo='m', confianca=1.0); assert p.estado == EstadoPatch.PROPOSTO; import json; assert json.loads(json.dumps(v.to_dict()))['regra_id'] == 'lex_001'; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/biblio_validador/core/dataclasses.py` retorna 0
    - `grep -c '@dataclass(frozen=True, slots=True)' src/biblio_validador/core/dataclasses.py` retorna `1` (Violacao frozen)
    - `grep -c '@dataclass(slots=True)' src/biblio_validador/core/dataclasses.py` retorna `2` (1 frozen+slots, 1 só slots — grep sem `-w` casa ambos)
    - `grep -E '^@dataclass\(slots=True\)$' src/biblio_validador/core/dataclasses.py` retorna `1` (Patch sem frozen)
    - `grep -c 'class Violacao:' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -c 'class Patch:' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -c 'def _normalize_for_json' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -c 'def to_dict(self) -> dict\[str, Any\]:' src/biblio_validador/core/dataclasses.py` retorna `2`
    - `grep -c 'def __post_init__(self) -> None:' src/biblio_validador/core/dataclasses.py` retorna `2`
    - `grep -c 'sugestoes: tuple\[str, \.\.\.\] = ()' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -c 'confianca: float' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -E 'confianca: float = ' src/biblio_validador/core/dataclasses.py` retorna 1 (NÃO encontra default em confianca — anti-hidden-default D-17)
    - `grep -E 'list\[str\]' src/biblio_validador/core/dataclasses.py` retorna 1 (NÃO encontra `list[str]` — Pitfall 1)
    - `grep -E '^import dataclasses$' src/biblio_validador/core/dataclasses.py` retorna 1 (NÃO encontra bare import — recursão)
    - `grep -c 'from dataclasses import' src/biblio_validador/core/dataclasses.py` retorna `1`
    - `grep -E 'IntEnum|StrEnum|loguru|logger|print\(' src/biblio_validador/core/dataclasses.py` retorna 1 (NÃO encontra)
    - `grep -E 'from_dict|object\.__setattr__|created_at' src/biblio_validador/core/dataclasses.py` retorna 1 (NÃO encontra deferred items)
    - `uv run ruff check src/biblio_validador/core/dataclasses.py` exit 0
    - `uv run ruff format --check src/biblio_validador/core/dataclasses.py` exit 0
    - `uv run mypy src/biblio_validador/core/dataclasses.py` exit 0
    - `awk '{ if (length > 80) print NR": "length" chars" }' src/biblio_validador/core/dataclasses.py` retorna vazio (todas as linhas <= 80 chars)
  </acceptance_criteria>

  <done>
    Arquivo `src/biblio_validador/core/dataclasses.py` criado contendo `Violacao` (frozen+slots, 11 campos, __post_init__ com 6 invariantes, to_dict()), `Patch` (slots-only, 10 campos, __post_init__ com 6 invariantes, to_dict()) e `_normalize_for_json` (recursivo). Imports usam `from dataclasses import asdict, dataclass` (sem self-import). ruff + mypy strict + line-length 80 passam.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar src/biblio_validador/core/__init__.py com re-export público</name>

  <wave>2</wave>

  <files>src/biblio_validador/core/__init__.py</files>

  <read_first>
    - .planning/phases/03-dataclasses-core/03-CONTEXT.md (D-03 — re-export pattern)
    - .planning/phases/03-dataclasses-core/03-PATTERNS.md ("src/biblio_validador/core/__init__.py" section lines 21-50; "Re-Export Pattern" section lines 328-340)
    - .planning/phases/03-dataclasses-core/03-RESEARCH.md (Pattern 6 lines 532-547)
    - src/biblio_validador/parser/__init__.py (template literal — copiar shape, swap symbols)
    - src/biblio_validador/core/enums.py (Task 1 — provê Severidade, EstadoPatch)
    - src/biblio_validador/core/dataclasses.py (Task 2 — provê Violacao, Patch)
  </read_first>

  <action>
Criar arquivo `src/biblio_validador/core/__init__.py` replicando exatamente o
pattern de `parser/__init__.py` (Phase 2 D-22), swapping símbolos per CONTEXT
D-03 (alfabético).

**Conteúdo completo do arquivo (7 linhas):**

```python
"""Core domain types — Violacao, Patch, Severidade, EstadoPatch."""

from biblio_validador.core.dataclasses import Patch, Violacao
from biblio_validador.core.enums import EstadoPatch, Severidade

__all__ = ["EstadoPatch", "Patch", "Severidade", "Violacao"]
```

**Decisões aplicadas:**
- D-03: re-export literal dos 4 símbolos públicos via `__all__`
- D-04: NÃO criar `core/types.py` ou outras subdivisões
- PATTERNS.md "Re-Export Pattern": one-line PT-BR docstring; absolute imports; `__all__` flat list de strings; sem `__version__`, sem side-effect, sem logging configuration

**Conventions herdadas (PATTERNS.md "Re-Export Pattern" lines 328-340 + Phase 2 `parser/__init__.py`):**
- Linha 1: docstring PT-BR uma linha
- Linha 2: vazia
- Linhas 3-4: absolute imports `from biblio_validador.core.<mod> import <Symbol>`
- Linha 5: vazia
- Linha 6: `__all__ = [...]` ordem alfabética per D-03 (NÃO topical como Phase 2 — CONTEXT explicitamente fixa alfabético)
- Sem newline final extra além da convenção POSIX
  </action>

  <verify>
    <automated>uv run python -c "from biblio_validador.core import Violacao, Patch, EstadoPatch, Severidade; from biblio_validador.core import __all__; assert set(__all__) == {'Violacao', 'Patch', 'EstadoPatch', 'Severidade'}; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/biblio_validador/core/__init__.py` retorna 0
    - `grep -c 'from biblio_validador.core.dataclasses import Patch, Violacao' src/biblio_validador/core/__init__.py` retorna `1`
    - `grep -c 'from biblio_validador.core.enums import EstadoPatch, Severidade' src/biblio_validador/core/__init__.py` retorna `1`
    - `grep -c '__all__ = \["EstadoPatch", "Patch", "Severidade", "Violacao"\]' src/biblio_validador/core/__init__.py` retorna `1`
    - `wc -l src/biblio_validador/core/__init__.py` retorna `<= 7` (one-line docstring + 2 imports + __all__, com blanks)
    - `grep -E 'from \.' src/biblio_validador/core/__init__.py` retorna 1 (NÃO encontra relative imports)
    - `grep -E 'logger|loguru|print\(|__version__' src/biblio_validador/core/__init__.py` retorna 1 (NÃO encontra side-effect/version)
    - `uv run ruff check src/biblio_validador/core/__init__.py` exit 0
    - `uv run mypy src/biblio_validador/core/__init__.py` exit 0
    - `uv run python -c "from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao; print('OK')"` exit 0
  </acceptance_criteria>

  <done>
    Arquivo `src/biblio_validador/core/__init__.py` criado com re-export literal dos 4 símbolos públicos (`EstadoPatch`, `Patch`, `Severidade`, `Violacao`) via `__all__` alfabético. Permite `from biblio_validador.core import ...` em validadores futuros sem expor caminho interno.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Criar tests/core/__init__.py + tests/core/test_dataclasses.py com 12+ testes (CORE-02 + CORE-03 + contract tests)</name>

  <wave>2</wave>

  <files>tests/core/__init__.py, tests/core/test_dataclasses.py</files>

  <read_first>
    - .planning/phases/03-dataclasses-core/03-CONTEXT.md (D-28, D-29, D-30 — 4 success criteria + 5 invariantes)
    - .planning/phases/03-dataclasses-core/03-RESEARCH.md (Code Examples lines 741-831 — código de teste pronto; Validation Architecture lines 832-910 — test map; Contract Tests lines 893-910)
    - .planning/phases/03-dataclasses-core/03-PATTERNS.md ("tests/core/__init__.py" section lines 183-191; "tests/core/test_dataclasses.py" section lines 195-292)
    - .planning/phases/03-dataclasses-core/03-VALIDATION.md (per-task verification map — 12 tests required)
    - tests/parser/test_markdown.py (template para signature, docstring, fixtures pattern)
    - tests/parser/__init__.py (template — arquivo vazio)
    - src/biblio_validador/core/__init__.py (Task 3 — re-export que os testes consomem)
  </read_first>

  <behavior>
    Cobertura completa dos 12 tests do VALIDATION.md per-task map + 4 contract tests:

    **CORE-02 (D-28 success criteria + D-29 invariantes):**
    - `test_violacao_instancia_com_frozen_e_slots` — D-28.1 (3 asserts: regra_id, sugestoes default, slots/no-dict)
    - `test_violacao_frozen_raises_on_mutate` — D-28.4 (FrozenInstanceError ao reatribuir regra_id)
    - `test_violacao_asdict_estrutural` — D-28.3 parte 1 (asdict retorna dict com 11 chaves)
    - `test_violacao_to_dict_json_roundtrip` — D-28.3 parte 2 (json.dumps + json.loads round-trip preserva campos)
    - `test_violacao_invariantes_raise` — D-29 parametrize com 4 casos: linha_inicio<1, linha_fim<linha_inicio, col_inicio<1, col_fim<col_inicio (cada caso tem `match=` com regra_id e o nome do campo)
    - `test_severidade_peso_monotonica` — D-15/D-16 (asserta peso INFO=0, ALERTA=1, ERRO=2, CRITICO=3 em ordem)

    **CORE-03 (D-28 success criteria + D-29 invariantes):**
    - `test_patch_mutavel_estado` — D-28.2 (3 asserts: estado default PROPOSTO, mutação OK, slots proíbe attr novo via AttributeError)
    - `test_estado_patch_default_proposto` — verifica `Patch(...).estado == EstadoPatch.PROPOSTO` E `EstadoPatch.PROPOSTO.value == "proposto"`
    - `test_patch_invariantes_raise` — D-29 parametrize com 3 casos: confianca=1.5, texto_original="", motivo=""
    - `test_patch_to_dict_json_roundtrip` — json.dumps(p.to_dict()); valida que estado é normalizado para "proposto" (str), arquivo para "a.md" (str)

    **Contract tests (RESEARCH "Contract Tests Específicos" lines 893-910):**
    - `test_aux_violacao_frozenness_contract` — `Violacao.__dataclass_params__.frozen is True` (detecta regressão)
    - `test_aux_violacao_sugestoes_eh_tuple_nao_list` — Pitfall 1: `isinstance(v.sugestoes, tuple)` E `not isinstance(v.sugestoes, list)`
    - `test_aux_violacao_hashable_patch_unhashable` — `hash(v)` funciona; `len({v, v}) == 1` (set deduplica); `hash(p)` raises TypeError (Pitfall 4)
    - `test_aux_imports_via_re_export_funcionam` — `from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao` (valida contrato do __init__.py D-03)
  </behavior>

  <action>
**Sub-task 4a:** Criar `tests/core/__init__.py` como arquivo vazio (zero bytes) — marcador
de pacote pytest. Idêntico a `tests/parser/__init__.py`.

**Sub-task 4b:** Criar `tests/core/test_dataclasses.py` com a suíte completa.

**Estrutura completa do `tests/core/test_dataclasses.py`:**

```python
"""Smoke tests Phase 3 - Violacao + Patch + invariantes (D-28/D-29)."""

import json
from dataclasses import FrozenInstanceError, asdict
from pathlib import Path

import pytest

from biblio_validador.core import (
    EstadoPatch,
    Patch,
    Severidade,
    Violacao,
)


# ---------- CORE-02: Violacao success criteria ----------


def _violacao_minima(**overrides: object) -> Violacao:
    """Helper: Violacao com defaults validos, parametrizavel via kwargs."""
    base: dict[str, object] = dict(
        arquivo=Path("a.md"),
        linha_inicio=1,
        linha_fim=1,
        col_inicio=1,
        col_fim=5,
        trecho_violador="abcd",
        regra_id="lex_001",
        regra_nome="X",
        severidade=Severidade.ERRO,
    )
    base.update(overrides)
    return Violacao(**base)  # type: ignore[arg-type]


def test_violacao_instancia_com_frozen_e_slots() -> None:
    """D-28.1: Violacao instancia, e frozen e tem __slots__."""
    v = _violacao_minima()
    assert v.regra_id == "lex_001"
    assert v.sugestoes == ()  # default D-09
    assert v.principio_canonico_violado is None  # default D-09
    assert hasattr(v, "__slots__")
    assert not hasattr(v, "__dict__")


def test_violacao_frozen_raises_on_mutate() -> None:
    """D-28.4: mutar regra_id levanta FrozenInstanceError."""
    v = _violacao_minima()
    with pytest.raises(FrozenInstanceError):
        v.regra_id = "outro"  # type: ignore[misc]


def test_violacao_asdict_estrutural() -> None:
    """D-28.3 parte 1: asdict retorna dict estruturalmente correto."""
    v = _violacao_minima()
    d = asdict(v)
    assert isinstance(d, dict)
    expected_keys = {
        "arquivo", "linha_inicio", "linha_fim", "col_inicio",
        "col_fim", "trecho_violador", "regra_id", "regra_nome",
        "severidade", "sugestoes", "principio_canonico_violado",
    }
    assert set(d.keys()) == expected_keys
    assert d["regra_id"] == "lex_001"


def test_violacao_to_dict_json_roundtrip() -> None:
    """D-28.3 parte 2: json.dumps(to_dict()) round-trip preserva campos."""
    v = _violacao_minima(sugestoes=("alt1", "alt2"))
    s = json.dumps(v.to_dict())
    parsed = json.loads(s)
    assert parsed["regra_id"] == "lex_001"
    assert parsed["arquivo"] == "a.md"  # Path -> str
    assert parsed["severidade"] == "erro"  # Enum -> value
    assert parsed["sugestoes"] == ["alt1", "alt2"]  # tuple -> list
    assert parsed["principio_canonico_violado"] is None


@pytest.mark.parametrize(
    "overrides, msg_substr",
    [
        (dict(linha_inicio=0), "linha_inicio"),
        (dict(linha_inicio=2, linha_fim=1), "linha_fim"),
        (dict(col_inicio=0), "col_inicio"),
        (dict(col_inicio=5, col_fim=2, trecho_violador="ab"), "col_fim"),
    ],
)
def test_violacao_invariantes_raise(
    overrides: dict[str, object], msg_substr: str
) -> None:
    """D-29: __post_init__ raises ValueError com regra_id no contexto."""
    with pytest.raises(ValueError, match=msg_substr):
        _violacao_minima(**overrides)


# ---------- Severidade.peso() ----------


def test_severidade_peso_monotonica() -> None:
    """D-15/D-16: peso() retorna 0..3 monotonico crescente."""
    pesos = [
        Severidade.INFO.peso(),
        Severidade.ALERTA.peso(),
        Severidade.ERRO.peso(),
        Severidade.CRITICO.peso(),
    ]
    assert pesos == [0, 1, 2, 3]


# ---------- CORE-03: Patch success criteria ----------


def _patch_minimo(**overrides: object) -> Patch:
    """Helper: Patch com defaults validos, parametrizavel via kwargs."""
    base: dict[str, object] = dict(
        arquivo=Path("a.md"),
        linha=1,
        col_inicio=1,
        col_fim=5,
        texto_original="abcd",
        texto_substituto="efgh",
        motivo="cst_012 -> forma direta",
        confianca=1.0,
    )
    base.update(overrides)
    return Patch(**base)  # type: ignore[arg-type]


def test_patch_mutavel_estado() -> None:
    """D-28.2: Patch e mutavel em estado, slots proibe atributo novo."""
    p = _patch_minimo()
    assert p.estado == EstadoPatch.PROPOSTO  # default D-17
    assert p.requer_revisao_humana is False  # default D-17
    p.estado = EstadoPatch.ACEITO  # mutacao OK (nao frozen)
    assert p.estado == EstadoPatch.ACEITO
    with pytest.raises(AttributeError):
        p.atributo_novo = 42  # type: ignore[attr-defined]


def test_estado_patch_default_proposto() -> None:
    """D-20: EstadoPatch.PROPOSTO eh default e value 'proposto'."""
    p = _patch_minimo()
    assert p.estado is EstadoPatch.PROPOSTO
    assert p.estado.value == "proposto"


@pytest.mark.parametrize(
    "overrides, msg_substr",
    [
        (dict(confianca=1.5), "confianca"),
        (dict(confianca=-0.1), "confianca"),
        (dict(texto_original=""), "texto_original"),
        (dict(motivo=""), "motivo"),
        (dict(motivo="   "), "motivo"),  # whitespace-only
    ],
)
def test_patch_invariantes_raise(
    overrides: dict[str, object], msg_substr: str
) -> None:
    """D-29: __post_init__ raises ValueError em invariantes violadas."""
    with pytest.raises(ValueError, match=msg_substr):
        _patch_minimo(**overrides)


def test_patch_to_dict_json_roundtrip() -> None:
    """CORE-03 contract: json.dumps(patch.to_dict()) preserva campos."""
    p = _patch_minimo()
    s = json.dumps(p.to_dict())
    parsed = json.loads(s)
    assert parsed["motivo"] == "cst_012 -> forma direta"
    assert parsed["arquivo"] == "a.md"  # Path -> str
    assert parsed["estado"] == "proposto"  # Enum -> value
    assert parsed["confianca"] == 1.0


# ---------- Contract tests (RESEARCH "Contract Tests Especificos") ----------


def test_aux_violacao_frozenness_contract() -> None:
    """Contract 1: detecta regressao se alguem remover frozen=True."""
    params = Violacao.__dataclass_params__  # type: ignore[attr-defined]
    assert params.frozen is True


def test_aux_violacao_sugestoes_eh_tuple_nao_list() -> None:
    """Contract 3 + Pitfall 1: sugestoes e tuple, nao list."""
    v = _violacao_minima(sugestoes=("a", "b"))
    assert isinstance(v.sugestoes, tuple)
    assert not isinstance(v.sugestoes, list)


def test_aux_violacao_hashable_patch_unhashable() -> None:
    """Contract 5: Violacao hashable (frozen); Patch unhashable (mutavel)."""
    v1 = _violacao_minima()
    v2 = _violacao_minima()
    assert hash(v1) == hash(v2)  # mesmo conteudo -> mesmo hash
    assert len({v1, v2}) == 1  # set deduplica idênticos
    p = _patch_minimo()
    with pytest.raises(TypeError):
        hash(p)  # Patch e unhashable (Pitfall 4)


def test_aux_imports_via_re_export_funcionam() -> None:
    """D-03: __init__.py re-exporta os 4 simbolos publicos."""
    from biblio_validador.core import __all__

    assert set(__all__) == {
        "EstadoPatch", "Patch", "Severidade", "Violacao"
    }
```

**Decisões aplicadas:**
- D-28.1..D-28.4: 4 success criteria do roadmap testados explicitamente
- D-29: parametrize com 4 invariantes Violacao + 5 invariantes Patch (5 = 3 do D-29 + 2 auxiliares: confianca negativa + motivo whitespace)
- D-30: target coverage >= 95% — atingível com 12+ testes para 4 símbolos
- PATTERNS.md "Test File Layout": import absoluto via re-export (`from biblio_validador.core import ...` — valida contrato D-03)
- PATTERNS.md "Test function pattern": docstring `"""D-XX.N: short description."""`; signature `-> None`
- Helper functions `_violacao_minima` / `_patch_minimo` evitam DRY de boilerplate (RESEARCH Pattern + Discretion D-29)
- Use `pytest.raises(ValueError, match=msg_substr)` para validar invariantes incluem contexto na mensagem (D-22 confirms regra_id na mensagem)
- Stdlib imports primeiro alfabéticos, blank line, third-party (pytest), blank line, project import
  </action>

  <verify>
    <automated>uv run pytest tests/core/ -x --tb=short -v</automated>
  </verify>

  <acceptance_criteria>
    - `test -f tests/core/__init__.py` retorna 0
    - `wc -c tests/core/__init__.py` retorna `0` (arquivo vazio — pytest package marker)
    - `test -f tests/core/test_dataclasses.py` retorna 0
    - `grep -c 'def test_violacao_instancia_com_frozen_e_slots' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_violacao_frozen_raises_on_mutate' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_violacao_asdict_estrutural' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_violacao_to_dict_json_roundtrip' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_violacao_invariantes_raise' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_severidade_peso_monotonica' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_patch_mutavel_estado' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_estado_patch_default_proposto' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_patch_invariantes_raise' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_patch_to_dict_json_roundtrip' tests/core/test_dataclasses.py` retorna `1`
    - `grep -c 'def test_aux_' tests/core/test_dataclasses.py` retorna `>= 4`
    - `grep -c 'from biblio_validador.core import' tests/core/test_dataclasses.py` retorna `>= 1` (re-export contract)
    - `grep -c '@pytest.mark.parametrize' tests/core/test_dataclasses.py` retorna `>= 2` (Violacao + Patch invariantes)
    - `uv run pytest tests/core/test_dataclasses.py::test_violacao_instancia_com_frozen_e_slots -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_violacao_frozen_raises_on_mutate -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_violacao_asdict_estrutural -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_violacao_to_dict_json_roundtrip -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_violacao_invariantes_raise -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_severidade_peso_monotonica -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_patch_mutavel_estado -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_estado_patch_default_proposto -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_patch_invariantes_raise -x` exit 0
    - `uv run pytest tests/core/test_dataclasses.py::test_patch_to_dict_json_roundtrip -x` exit 0
    - `uv run pytest tests/core/ -x --tb=short` exit 0 (suite completa core verde)
    - `uv run ruff check tests/core/` exit 0
    - `uv run mypy tests/core/test_dataclasses.py` exit 0
  </acceptance_criteria>

  <done>
    Suite de testes `tests/core/test_dataclasses.py` cobre os 12 testes do VALIDATION.md per-task map + 4 contract tests auxiliares. `tests/core/__init__.py` vazio. Imports via re-export `from biblio_validador.core import ...` validam contrato D-03. Suite verde em `uv run pytest tests/core/ -x`. ruff + mypy strict passam.
  </done>
</task>

<task type="auto">
  <name>Task 5: Phase gate — coverage >= 95% + mypy strict + ruff verde para módulos Phase 3</name>

  <wave>3</wave>

  <files>(read-only — executa testes; sem modificação de arquivos)</files>

  <read_first>
    - .planning/phases/03-dataclasses-core/03-CONTEXT.md (D-30, D-31, D-32 — gates)
    - .planning/phases/03-dataclasses-core/03-VALIDATION.md ("Validation Sign-Off" section lines 79-87)
    - .planning/phases/03-dataclasses-core/03-RESEARCH.md ("Sampling Rate" lines 868-877; "Wave 0 Gaps" lines 879-885)
    - src/biblio_validador/core/enums.py (Task 1)
    - src/biblio_validador/core/dataclasses.py (Task 2)
    - src/biblio_validador/core/__init__.py (Task 3)
    - tests/core/test_dataclasses.py (Task 4)
  </read_first>

  <action>
Tarefa de gate: rodar comandos de validação cumulativos sobre tudo produzido nas
Tasks 1-4 e confirmar que os critérios de finalização da Phase 3 são atendidos.
NÃO modifica arquivos — apenas valida.

**Comandos a executar (em sequência, todos devem exit 0):**

1. **Suite verde isolada Phase 3:**
   ```bash
   uv run pytest tests/core/ -x --tb=short
   ```

2. **Suite completa do projeto sem regressão (Phase 1 + Phase 2 + Phase 3):**
   ```bash
   uv run pytest --tb=short
   ```

3. **Coverage gate D-30 (>= 95%) sobre `biblio_validador.core`:**
   ```bash
   uv run pytest --cov=biblio_validador.core --cov-report=term-missing --cov-fail-under=95 tests/core/
   ```
   Se falhar com cobertura < 95%, identificar branches missing pelo report e
   adicionar `test_aux_*` em `tests/core/test_dataclasses.py` antes de re-rodar.
   Permitido (PATTERNS.md ref Phase 2): adicionar tests `test_aux_<descricao>`
   conforme convenção autorizada na STATE.md ("convenção test_aux_<descrição>
   autorizada para coverage gap fill sem violar set canônico").

4. **mypy strict gate D-31:**
   ```bash
   uv run mypy src/biblio_validador/core/
   ```

5. **ruff lint gate D-32:**
   ```bash
   uv run ruff check src/biblio_validador/core/ tests/core/
   ```

6. **ruff format gate D-32:**
   ```bash
   uv run ruff format --check src/biblio_validador/core/ tests/core/
   ```

7. **Sanity check linha 80 (D-32):**
   ```bash
   awk '{ if (length > 80) print FILENAME":"NR": "length" chars" }' \
     src/biblio_validador/core/*.py tests/core/*.py
   ```
   Esperado: saída vazia.

8. **Sanity check imports — re-export funcional:**
   ```bash
   uv run python -c "from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao; print('re-export OK')"
   ```

**Decisões aplicadas:**
- D-30: coverage gate `--cov-fail-under=95` em `biblio_validador.core`
- D-31: mypy strict herdado de Phase 1 D-13 (`pyproject.toml [tool.mypy] strict = true`)
- D-32: ruff line-length=80 herdado de Phase 1 D-12 (`pyproject.toml [tool.ruff] line-length=80`)
- VALIDATION.md "Sampling Rate": full suite green AND `pytest --cov-fail-under=95` antes de `/gsd-verify-work`

**Se algum gate falhar:**
- Coverage < 95% → adicionar `test_aux_*` cobrindo branches faltantes (convenção autorizada Phase 2 STATE.md)
- mypy → corrigir type hints faltantes ou inferência ambígua
- ruff lint → aplicar `uv run ruff check --fix` ou ajuste manual
- ruff format → aplicar `uv run ruff format` (sem `--check`)
- linha > 80 → quebrar via parens, line continuation, ou refactor docstring
  </action>

  <verify>
    <automated>uv run pytest tests/core/ -x --tb=short && uv run pytest --cov=biblio_validador.core --cov-fail-under=95 --cov-report=term-missing tests/core/ && uv run mypy src/biblio_validador/core/ && uv run ruff check src/biblio_validador/core/ tests/core/ && uv run ruff format --check src/biblio_validador/core/ tests/core/</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/core/ -x --tb=short` exit 0 (suíte Phase 3 verde)
    - `uv run pytest --tb=short` exit 0 (suíte completa Phase 1+2+3 sem regressão)
    - `uv run pytest --cov=biblio_validador.core --cov-fail-under=95 --cov-report=term-missing tests/core/` exit 0 (coverage >= 95% D-30)
    - `uv run mypy src/biblio_validador/core/` exit 0 (mypy strict D-31)
    - `uv run ruff check src/biblio_validador/core/ tests/core/` exit 0 (ruff lint D-32)
    - `uv run ruff format --check src/biblio_validador/core/ tests/core/` exit 0 (ruff format D-32)
    - `awk '{ if (length > 80) print FILENAME":"NR }' src/biblio_validador/core/*.py tests/core/*.py | wc -l` retorna `0` (zero linhas > 80 chars D-32)
    - `uv run python -c "from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao; print('OK')"` exit 0 (re-export D-03)
    - `uv run python -c "import biblio_validador.core; assert hasattr(biblio_validador.core, '__all__'); assert set(biblio_validador.core.__all__) == {'EstadoPatch','Patch','Severidade','Violacao'}"` exit 0
  </acceptance_criteria>

  <done>
    Todos os 8 gates passam: suite verde, coverage >= 95% em `biblio_validador.core`, mypy strict sem erros, ruff lint+format limpos, zero linhas > 80 chars, re-export funciona. Phase 3 está pronta para `/gsd-verify-work` e merge no roadmap.
  </done>
</task>

</tasks>

<verification>
Phase 3 é considerada concluída quando:

1. **Suíte de testes Phase 3 verde isoladamente:**
   `uv run pytest tests/core/ -x --tb=short` exit 0

2. **Suíte completa do projeto sem regressão (Phase 1 + 2 + 3):**
   `uv run pytest --tb=short` exit 0

3. **Coverage gate D-30 atingido:**
   `uv run pytest --cov=biblio_validador.core --cov-fail-under=95 tests/core/` exit 0

4. **mypy strict (D-31):**
   `uv run mypy src/biblio_validador/core/` exit 0

5. **ruff lint + format (D-32):**
   `uv run ruff check src/biblio_validador/core/ tests/core/` exit 0
   `uv run ruff format --check src/biblio_validador/core/ tests/core/` exit 0

6. **Re-export D-03 funcional:**
   `from biblio_validador.core import EstadoPatch, Patch, Severidade, Violacao` sem erros

7. **Os 4 success criteria de Phase 3 (ROADMAP) validados pelos testes:**
   - Success #1 — `test_violacao_instancia_com_frozen_e_slots`
   - Success #2 — `test_patch_mutavel_estado` + `test_estado_patch_default_proposto`
   - Success #3 — `test_violacao_asdict_estrutural` + `test_violacao_to_dict_json_roundtrip`
   - Success #4 — `test_violacao_frozen_raises_on_mutate`

8. **VALIDATION.md per-task map: 12 testes ✅ verde** (não-pendentes)
</verification>

<success_criteria>
- [ ] `src/biblio_validador/core/enums.py` existe contendo `Severidade` (4 membros + `peso()`) e `EstadoPatch` (4 membros) como `enum.Enum`
- [ ] `src/biblio_validador/core/dataclasses.py` existe contendo `Violacao` (frozen+slots), `Patch` (slots-only), `_normalize_for_json`, e métodos `to_dict()` em ambas
- [ ] `src/biblio_validador/core/__init__.py` re-exporta `EstadoPatch`, `Patch`, `Severidade`, `Violacao` via `__all__` alfabético
- [ ] `tests/core/__init__.py` existe (vazio, marcador pytest)
- [ ] `tests/core/test_dataclasses.py` cobre 12+ testes (4 D-28 + 4-5 D-29 Violacao + 3-5 D-29 Patch + 4 contract tests)
- [ ] `Violacao` tem `__post_init__` que raises `ValueError` com `regra_id` no contexto para 6 invariantes (linha_inicio<1, linha_fim<linha_inicio, col_inicio<1, col_fim<col_inicio, regra_id vazio, len(trecho_violador) != col_fim-col_inicio)
- [ ] `Patch` tem `__post_init__` que raises `ValueError` para 6 invariantes (linha<1, col_inicio<1, col_fim<col_inicio, confianca fora [0,1], texto_original vazio, motivo vazio/whitespace)
- [ ] `Patch.confianca` NÃO tem default (D-17 anti-hidden-default)
- [ ] `Violacao.sugestoes: tuple[str, ...] = ()` (NUNCA `list[str]`)
- [ ] Coverage `>= 95%` em `biblio_validador.core` (D-30)
- [ ] mypy strict, ruff lint+format, line-length 80 todos verdes
- [ ] Zero deps novas em `pyproject.toml` (stdlib only)
- [ ] Sem `loguru`/`logger`/`print` em `core/` (D-33)
- [ ] Sem `import dataclasses` bare em `core/dataclasses.py` (recursão)
- [ ] Phase 3 roadmap success criteria #1-#4 todos validados por teste automatizado
</success_criteria>

<output>
Após completar todas as 5 tasks (Tasks 1+2 em Wave 1 paralelas; Task 3 em Wave 2;
Task 4 em Wave 2 paralela com Task 3; Task 5 em Wave 3 dependendo de 1-4):

1. Atualizar `.planning/STATE.md`:
   - `Current Position` → `Phase: 03 (Dataclasses Core) - COMPLETE`
   - `Recent decisions affecting current work` → adicionar entrada
     `[Phase 03]: Phase 3: Violacao frozen+slots / Patch slots-only / Severidade.peso() via dict / __post_init__ valida invariantes com regra_id no contexto / to_dict() via _normalize_for_json recursivo`
   - Atualizar progresso (`completed_phases`, `completed_plans`, `percent`)

2. Atualizar `.planning/ROADMAP.md`:
   - Marcar `Phase 3: Dataclasses Core` como `[x]` concluída
   - Atualizar tabela de progresso `Plans Complete: 1/1`, `Status: Complete`

3. Criar `.planning/phases/03-dataclasses-core/03-01-SUMMARY.md` seguindo
   `$HOME/.claude/get-shit-done/templates/summary.md`, registrando:
   - Decisões aplicadas (33 D-XX cobertas)
   - Padrões estabelecidos (Pattern 5 `_normalize_for_json` + `to_dict()` reutilizáveis em Phase 58)
   - Arquivos criados (5)
   - Métricas (testes, coverage, tempo)
   - Próximos consumidores (Phase 4, 5, 6, 8, 19, 48, 51, 58)

4. Commit final convencional:
   `feat(03-01): dataclasses core - Violacao frozen + Patch mutable + enums`

5. Pronto para `/gsd-verify-work` (Nyquist gate sobre VALIDATION.md per-task map).
</output>
