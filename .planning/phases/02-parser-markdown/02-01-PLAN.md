---
phase: 02-parser-markdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/biblio_validador/parser/__init__.py
  - src/biblio_validador/parser/types.py
  - src/biblio_validador/parser/markdown.py
  - tests/parser/__init__.py
  - tests/parser/fixtures/__init__.py
  - tests/parser/fixtures/eolica_first_30.md
  - tests/parser/test_markdown.py
autonomous: true
requirements:
  - CORE-01
  - CORE-11
validation_compliant: true
tags:
  - parser
  - markdown
  - commonmark
  - footnotes
  - unicode-nfc
  - python
user_setup: []

must_haves:
  truths:
    - "ParserMd.parsear(path) lê arquivo .md UTF-8 e retorna list[Paragrafo] com offset_bytes/linha_inicio/linha_fim corretos (CORE-01 success criterion #1)"
    - "Parágrafos com acentos compostos (NFD) são normalizados para NFC antes do parser indexar offsets (CORE-11 success criterion #2)"
    - "Footnotes [^n] no corpo permanecem embutidas no texto do parágrafo pai; o corpo da nota ([^n]: ...) vira Paragrafo separado com tipo=NOTA_RODAPE e ref_nota='n' (success criterion #3)"
    - "Trecho do artigo Eólica Nordeste retorna parágrafos identificáveis linha a linha (success criterion #4)"
    - "Round-trip src_bytes_nfc[offset:offset+len_bytes].decode('utf-8') == p.texto vale para todos os parágrafos (invariante byte-exact que sustenta Phase 5 PatchAplicador)"
    - "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao funciona (D-22 contrato público)"
    - "uv run pytest tests/parser/ exit 0 com 6 testes verdes; coverage >= 90% no módulo parser/; mypy strict exit 0; ruff check + format exit 0"
  artifacts:
    - path: "src/biblio_validador/parser/__init__.py"
      provides: "Re-export público (D-22): ParserMd, Paragrafo, TipoSecao"
      contains: 'from biblio_validador.parser.markdown import ParserMd'
      contains_also:
        - 'from biblio_validador.parser.types import Paragrafo, TipoSecao'
        - '__all__ = ["ParserMd", "Paragrafo", "TipoSecao"]'
    - path: "src/biblio_validador/parser/types.py"
      provides: "Dataclass Paragrafo (frozen+slots, 11 campos D-02) + Enum TipoSecao (14 valores D-03)"
      contains: '@dataclass(frozen=True, slots=True)'
      contains_also:
        - 'class TipoSecao(Enum):'
        - 'class Paragrafo:'
        - 'NOTA_RODAPE = "nota_rodape"'
        - 'paragrafo_pai_idx: int | None'
    - path: "src/biblio_validador/parser/markdown.py"
      provides: "Classe ParserMd com pipeline read_bytes->decode->BOM strip->CRLF->NFC->encode->linha_offsets->parse->walk->resolver_pais (D-09/D-11/D-18/D-19/D-20/D-21)"
      contains: 'class ParserMd:'
      contains_also:
        - 'def parsear(self, path: Path) -> list[Paragrafo]:'
        - 'unicodedata.normalize("NFC"'
        - 'MarkdownIt().use(footnote_plugin)'
        - 'from loguru import logger'
        - 'def _construir_linha_offsets'
        - 'def _walk'
        - 'def _resolver_pais'
        - 'def _classificar_heading'
        - 'def _normalizar_heading'
    - path: "tests/parser/__init__.py"
      provides: "Marcador de pacote pytest (zero bytes — Phase 1 D-16 inheritance)"
    - path: "tests/parser/fixtures/__init__.py"
      provides: "Marcador de pacote (zero bytes) — previne pytest collection ambiguity"
    - path: "tests/parser/fixtures/eolica_first_30.md"
      provides: "Excerpt verbatim do artigo Eólica Nordeste real (heading h1 + autores + RESUMO + PALAVRAS-CHAVE + ABSTRACT + KEYWORDS + INTRODUÇÃO + 2 footnotes [^a][^b]) — D-26.5 fixture"
      contains: "# Correntes eólicas"
      contains_also:
        - "## RESUMO"
        - "Investiga-se"
        - "[^a]:"
        - "[^b]:"
    - path: "tests/parser/test_markdown.py"
      provides: "5 unit tests D-26 + 1 property round-trip = 6 testes que pinam todos os success criteria + invariante byte-exact"
      contains: "def test_paragrafo_simples_um_so_paragrafo"
      contains_also:
        - "def test_heading_classificacao_e_heranca"
        - "def test_normalizacao_nfc"
        - "def test_footnote_separa_e_linka"
        - "def test_artigo_eolica_excerpt"
        - "def test_round_trip_offset_bytes"
        - "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao"
  key_links:
    - from: "src/biblio_validador/parser/__init__.py"
      to: "ParserMd, Paragrafo, TipoSecao"
      via: "Re-export D-22 (absolute imports)"
      pattern: 'from biblio_validador\.parser\.(markdown|types) import'
    - from: "src/biblio_validador/parser/markdown.py"
      to: "src/biblio_validador/parser/types.py"
      via: "import Paragrafo, TipoSecao"
      pattern: 'from biblio_validador\.parser\.types import Paragrafo, TipoSecao'
    - from: "src/biblio_validador/parser/markdown.py:parsear"
      to: "MarkdownIt().use(footnote_plugin).parse(text_nfc)"
      via: "Tokenização CommonMark+footnotes após pipeline NFC byte-exact"
      pattern: 'MarkdownIt\(\)\.use\(footnote_plugin\)'
    - from: "src/biblio_validador/parser/markdown.py:_resolver_pais"
      to: "Paragrafo(NOTA_RODAPE).paragrafo_pai_idx"
      via: "regex r'\\[\\^([^\\]]+)\\]' sobre texto dos parágrafos não-NOTA"
      pattern: '_REGEX_FOOTNOTE_REF\.finditer'
    - from: "tests/parser/test_markdown.py"
      to: "biblio_validador.parser (re-export D-22)"
      via: "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao"
      pattern: 'from biblio_validador\.parser import ParserMd, Paragrafo, TipoSecao'
    - from: "tests/parser/test_markdown.py:test_round_trip_offset_bytes"
      to: "Paragrafo.offset_bytes + len_bytes"
      via: "src_bytes_nfc[offset:offset+len_bytes].decode('utf-8') == p.texto (invariante byte-exact)"
      pattern: 'chunk\.decode\("utf-8"\) == p\.texto'
---

<objective>
Phase 2 — Parser Markdown: implementar `ParserMd.parsear(path) -> list[Paragrafo]` que lê `.md` (CommonMark + footnotes via `markdown-it-py 4.0.0` + `mdit_py_plugins.footnote.footnote_plugin`), normaliza source para UTF-8 NFC source-único, segmenta em parágrafos com offsets byte-exact e classifica seções via heading-text-based map.

Purpose: cobrir 100% de CORE-01 (parser MD com offsets byte-exact) e CORE-11 (NFC source-único) do REQUIREMENTS.md. Sem este parser, todas as Phases 6+ (validador piloto cst_012 e validadores M2) ficam sem `Paragrafo[]` para consumir; e Phase 5 (PatchAplicador) fica sem invariante byte-exact que sustenta patches sem drift de offsets. Phase 2 não conecta o parser ao CLI (Phases 6/8) — entrega exclusivamente o módulo `parser/` standalone.

Output:
- `src/biblio_validador/parser/types.py` (D-02 dataclass `Paragrafo` com 11 campos + D-03 enum `TipoSecao` com 14 valores)
- `src/biblio_validador/parser/markdown.py` (D-21 classe `ParserMd` com pipeline canônico de 9 passos + 6 helpers privados)
- `src/biblio_validador/parser/__init__.py` (D-22 re-export público)
- `tests/parser/test_markdown.py` (5 unit tests D-26 + 1 property round-trip cobrindo Pitfalls 1, 2, 3, 4, 6 simultaneamente)
- `tests/parser/fixtures/eolica_first_30.md` (D-26.5 excerpt verbatim do artigo Eólica Nordeste real para teste de integração)
- `tests/parser/__init__.py` + `tests/parser/fixtures/__init__.py` (markers de pacote pytest, Phase 1 D-16 inheritance)
- Coverage >= 90% no módulo `parser/`, `mypy --strict` exit 0, `ruff check + format` exit 0, 6 testes verdes (gate Nyquist)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-parser-markdown/02-CONTEXT.md
@.planning/phases/02-parser-markdown/02-RESEARCH.md
@.planning/phases/02-parser-markdown/02-PATTERNS.md
@.planning/phases/02-parser-markdown/02-VALIDATION.md
@.planning/phases/01-bootstrap/01-01-SUMMARY.md
@.planning/phases/01-bootstrap/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/biblio_validador/__init__.py
@src/biblio_validador/cli.py
@tests/__init__.py
@tests/test_cli.py
@pyproject.toml
@.gitignore

<interfaces>
<!-- Phase 2 cria 7 arquivos novos. Não há interfaces pré-existentes a importar do projeto além do pacote raiz biblio_validador (Phase 1). Os contratos canônicos abaixo foram VALIDADOS EMPIRICAMENTE em RESEARCH.md (linha 5: "Confidence: HIGH ... verificadas empiricamente em markdown-it-py 4.0.0 + mdit-py-plugins>=0.5.0 instalados via Phase 1 uv sync") e cruzados em PATTERNS.md (todos os 7 arquivos têm "Match Quality: exact"). -->

## Contrato 1: tests/parser/__init__.py (Phase 1 D-16 inheritance)

Arquivo VAZIO (zero bytes). Apenas marcador de pacote pytest.

## Contrato 2: tests/parser/fixtures/__init__.py

Arquivo VAZIO (zero bytes). Marcador de pacote — previne pytest collection ambiguity quando fixtures crescerem (PATTERNS.md §"tests/parser/fixtures/__init__.py").

## Contrato 3: tests/parser/fixtures/eolica_first_30.md (D-26.5)

Conteúdo VERBATIM (UTF-8, LF, NFC) — copiado das primeiras linhas de `/home/kalyllamarck/projetos/Doutorado/Artigos/Professora Gina Pompeu/Energia Eólica Nordeste/artigo_final/artigo.md` com truncations marcadas e dois corpos de footnote anexados ao final. Crítico: a numeração de linhas do fixture é load-bearing — Test 5 asserta `linha_inicio == 9` para heading "## RESUMO" e `linha_inicio == 11` para o parágrafo do resumo.

```
# Correntes eólicas sobre a (ir)racionalidade ambiental, energia renovável sem transição energética no Nordeste brasileiro

***Wind currents over the (ir)rationality of the environment, renewable energy without energy transition in the Brazilian Northeast***

**GINA VIDAL MARCÍLIO POMPEU**[^a]

**KALYL LAMARCK**[^b]

## RESUMO

Investiga-se se a expansão da energia eólica no Ceará e no Rio Grande do Norte, entre 2004 e 2025, constitui transição energética no sentido da racionalidade ambiental proposta por Leff, ou se reproduz, sob forma renovável, a racionalidade econômica formalizada por Summers.

## PALAVRAS-CHAVE

Transição energética; racionalidade ambiental; racismo ambiental; energia eólica; Nordeste brasileiro.

## ABSTRACT

This study investigates whether the expansion of wind energy in Ceará and Rio Grande do Norte constitutes an energy transition under Leff's environmental rationality.

## KEYWORDS

Energy transition; environmental rationality; environmental racism; wind energy; Brazilian Northeast.

## INTRODUÇÃO

Os sertões e as caatingas do Ceará e do Rio Grande do Norte abrigam, entre 2004 e 2025, uma das mais extensas expansões de parques eólicos do continente americano.

[^a]: Doutorado em Direito Constitucional pela Universidade de Fortaleza.
[^b]: Doutorando em Direito Constitucional pelo Programa de Pós-Graduação em Direito da Universidade de Fortaleza.
```

CRÍTICO — número de linhas calibrado: linha 1 = título h1; linha 9 = `## RESUMO`; linha 11 = parágrafo do resumo (`Investiga-se...`). Test 5 (`test_artigo_eolica_excerpt`) asserta exatamente esses números. Qualquer reformatação (linha em branco extra, mudança de heading level) quebra Test 5. NÃO copiar o artigo full (~102KB) — apenas o excerpt acima cobre D-26.5.

## Contrato 4: src/biblio_validador/parser/types.py (D-02 + D-03 — RESEARCH.md Example 3 lines 938-982)

```python
"""Tipos parser-internal: Paragrafo + TipoSecao."""
from dataclasses import dataclass
from enum import Enum
from pathlib import Path


class TipoSecao(Enum):
    """Vocabulário fechado de tipos de seção/parágrafo (D-03)."""
    TITULO = "titulo"
    AUTORES = "autores"
    RESUMO = "resumo"
    PALAVRAS_CHAVE = "palavras_chave"
    ABSTRACT = "abstract"
    KEYWORDS = "keywords"
    SUMARIO = "sumario"
    INTRODUCAO = "introducao"
    SECAO = "secao"
    CONCLUSAO = "conclusao"
    REFERENCIAS = "referencias"
    NOTA_RODAPE = "nota_rodape"
    CORPO = "corpo"
    DESCONHECIDO = "desconhecido"


@dataclass(frozen=True, slots=True)
class Paragrafo:
    """Bloco de texto identificável linha a linha (D-02).

    Offsets relativos ao source UTF-8 NFC.
    """
    arquivo: Path
    indice: int                       # 0-based, ordem topológica
    tipo: TipoSecao
    nivel_heading: int | None         # 1-6 se heading; None se corpo
    texto: str                        # slice raw NFC, com markup preservado
    linha_inicio: int                 # 1-based
    linha_fim: int                    # 1-based, inclusivo
    offset_bytes: int                 # absoluto em UTF-8 NFC
    len_bytes: int                    # bytes UTF-8 NFC
    ref_nota: str | None              # label da footnote ("a", "1", ...)
    paragrafo_pai_idx: int | None     # idx do parágrafo que cita [^ref_nota]
```

CRÍTICO (PATTERNS.md §"types.py"):
- `enum.Enum`, NÃO `enum.StrEnum` (D-03 explicit veto).
- 14 valores exatos no enum, sem extras.
- `frozen=True, slots=True` ambos obrigatórios (D-02).
- 11 campos exatos na ordem acima — downstream `Violacao` (Phase 3) espelha `linha_inicio`/`linha_fim`/`col_inicio`/`col_fim` verbatim.
- PEP 604 union `int | None`, NUNCA `Optional[int]` (ruff `UP` rule).
- Inline `# comment` após cada campo (RESEARCH.md uses, mypy aceita).
- NÃO importar `pydantic` (RESEARCH.md "What NOT to Use" forbids).
- NÃO adicionar `__post_init__`, `__str__`, `__repr__` (default dataclass repr é suficiente).

## Contrato 5: src/biblio_validador/parser/markdown.py (D-09 a D-25 — RESEARCH.md Example 2 lines 693-936)

```python
"""biblio_validador/parser/markdown.py — parser CommonMark + footnotes.

Offsets byte-exact sobre source UTF-8 NFC; ver D-09/D-11 do CONTEXT.md.
"""
import dataclasses
import re
import unicodedata
from pathlib import Path

from loguru import logger
from markdown_it import MarkdownIt
from markdown_it.token import Token
from mdit_py_plugins.footnote import footnote_plugin

from biblio_validador.parser.types import Paragrafo, TipoSecao

_REGEX_FOOTNOTE_REF = re.compile(r"\[\^([^\]]+)\]")
_RE_PUNCT = re.compile(r"[^\w\s-]", re.UNICODE)
_RE_SPACES = re.compile(r"\s+")


class ParserMd:
    """Parser CommonMark + footnotes para biblio_validador.

    Offsets são relativos ao source UTF-8 NFC. Arquivos em NFD
    (ex.: macOS) são convertidos silenciosamente. Validadores
    operam sempre em NFC.
    """

    def __init__(self) -> None:
        self._md = MarkdownIt().use(footnote_plugin)
        self._mapa_secao = self._carregar_mapa_secao()

    def parsear(self, path: Path) -> list[Paragrafo]:
        logger.info(f"parsear: {path}")
        try:
            raw = path.read_bytes()
        except FileNotFoundError:
            logger.error(f"arquivo não encontrado: {path}")
            raise

        try:
            text = raw.decode("utf-8")
        except UnicodeDecodeError:
            logger.error(f"encoding inválido em {path}")
            raise

        # BOM strip (Pitfall 6 — antes de NFC)
        if text.startswith("\ufeff"):
            text = text[1:]
        # CRLF -> LF (D-20)
        text = text.replace("\r\n", "\n")
        # NFC source-único (D-11, CORE-11)
        text_nfc = unicodedata.normalize("NFC", text)
        if text != text_nfc:
            logger.info(
                f"source NFD detectado, convertido para NFC: {path}"
            )

        if not text_nfc.strip():
            return []

        src_bytes = text_nfc.encode("utf-8")
        linha_offsets = self._construir_linha_offsets(src_bytes)

        tokens = self._md.parse(text_nfc)
        paragrafos = self._walk(tokens, src_bytes, linha_offsets, path)
        paragrafos = self._resolver_pais(paragrafos)
        logger.info(f"parsear: {len(paragrafos)} parágrafos")
        return paragrafos

    @staticmethod
    def _construir_linha_offsets(src_bytes: bytes) -> list[int]:
        """Tabela cumulativa de offsets de início de cada linha.

        linha_offsets[k] = offset em bytes do início da linha
        (0-indexed). Sentinela final = len(src_bytes) garante que
        slicing do último bloco funcione mesmo sem trailing newline.
        """
        offsets = [0]
        for i, b in enumerate(src_bytes):
            if b == 0x0A:
                offsets.append(i + 1)
        if offsets[-1] != len(src_bytes):
            offsets.append(len(src_bytes))
        return offsets

    @classmethod
    def _carregar_mapa_secao(cls) -> dict[str, TipoSecao]:
        return {
            "resumo": TipoSecao.RESUMO,
            "palavras-chave": TipoSecao.PALAVRAS_CHAVE,
            "palavras chave": TipoSecao.PALAVRAS_CHAVE,
            "palavraschave": TipoSecao.PALAVRAS_CHAVE,
            "abstract": TipoSecao.ABSTRACT,
            "keywords": TipoSecao.KEYWORDS,
            "sumario": TipoSecao.SUMARIO,
            "introducao": TipoSecao.INTRODUCAO,
            "consideracoes finais": TipoSecao.CONCLUSAO,
            "conclusao": TipoSecao.CONCLUSAO,
            "referencias": TipoSecao.REFERENCIAS,
            "notas": TipoSecao.NOTA_RODAPE,
            "notas de rodape": TipoSecao.NOTA_RODAPE,
        }

    def _classificar_heading(
        self, texto: str, nivel: int, is_first_h1: bool
    ) -> TipoSecao:
        # Ordem load-bearing (D-06): is_first_h1 → mapa → SECAO → DESCONHECIDO
        if nivel == 1 and is_first_h1:
            return TipoSecao.TITULO
        chave = self._normalizar_heading(texto)
        if chave in self._mapa_secao:
            return self._mapa_secao[chave]
        if nivel >= 2:
            return TipoSecao.SECAO
        return TipoSecao.DESCONHECIDO

    @staticmethod
    def _normalizar_heading(texto: str) -> str:
        t = unicodedata.normalize("NFKD", texto)
        t = "".join(c for c in t if not unicodedata.combining(c))
        t = t.casefold()
        t = _RE_PUNCT.sub(" ", t)
        t = _RE_SPACES.sub(" ", t).strip()
        return t

    def _build_paragrafo(
        self,
        token: Token,
        src_bytes: bytes,
        linha_offsets: list[int],
        arquivo: Path,
        indice: int,
        tipo: TipoSecao,
        nivel_heading: int | None,
        ref_nota: str | None,
    ) -> Paragrafo:
        # token.map = [start, end] (0-indexed half-open)
        # → linha_inicio = map[0] + 1 ; linha_fim = map[1] (Pitfall 2)
        assert token.map is not None  # mypy strict narrowing (Pitfall 8)
        start, end = token.map
        offset_bytes = linha_offsets[start]
        end_offset = linha_offsets[end]
        # Strip trailing newline do conteúdo, mantendo offset_bytes
        # intacto (Pitfall 3)
        chunk = src_bytes[offset_bytes:end_offset].rstrip(b"\n")
        texto = chunk.decode("utf-8")
        return Paragrafo(
            arquivo=arquivo,
            indice=indice,
            tipo=tipo,
            nivel_heading=nivel_heading,
            texto=texto,
            linha_inicio=start + 1,
            linha_fim=end,
            offset_bytes=offset_bytes,
            len_bytes=len(chunk),
            ref_nota=ref_nota,
            paragrafo_pai_idx=None,
        )

    def _walk(
        self,
        tokens: list[Token],
        src_bytes: bytes,
        linha_offsets: list[int],
        arquivo: Path,
    ) -> list[Paragrafo]:
        out: list[Paragrafo] = []
        current_secao = TipoSecao.DESCONHECIDO
        is_first_h1 = True
        in_footnote: str | None = None

        i = 0
        while i < len(tokens):
            t = tokens[i]
            if t.type == "footnote_open":
                in_footnote = t.meta.get("label")
                i += 1
                continue
            if t.type == "footnote_close":
                in_footnote = None
                i += 1
                continue
            if t.type == "heading_open":
                nivel = int(t.tag[1])
                inline = tokens[i + 1]
                secao = self._classificar_heading(
                    inline.content, nivel, is_first_h1
                )
                if nivel == 1 and is_first_h1:
                    is_first_h1 = False
                current_secao = secao
                out.append(self._build_paragrafo(
                    t, src_bytes, linha_offsets, arquivo,
                    indice=len(out), tipo=secao,
                    nivel_heading=nivel, ref_nota=None,
                ))
                i += 3  # heading_open + inline + heading_close
                continue
            if t.type == "paragraph_open":
                # Pitfall 1: paragraph_open dentro de footnote_open
                # tem map; footnote_open tem map=None
                tipo = (TipoSecao.NOTA_RODAPE if in_footnote
                        else current_secao)
                out.append(self._build_paragrafo(
                    t, src_bytes, linha_offsets, arquivo,
                    indice=len(out), tipo=tipo,
                    nivel_heading=None,
                    ref_nota=in_footnote,
                ))
                i += 1
                continue
            if t.type == "fence":
                out.append(self._build_paragrafo(
                    t, src_bytes, linha_offsets, arquivo,
                    indice=len(out), tipo=TipoSecao.DESCONHECIDO,
                    nivel_heading=None, ref_nota=None,
                ))
                i += 1
                continue
            # Pitfall 7: bullet_list_open / list_item_open /
            # blockquote_open são SILENCIOSAMENTE pulados — seu
            # paragraph_open interno será emitido na próxima iteração
            i += 1
        return out

    @staticmethod
    def _resolver_pais(
        paragrafos: list[Paragrafo],
    ) -> list[Paragrafo]:
        """Pass 2: resolve paragrafo_pai_idx para Paragrafo(NOTA_RODAPE)."""
        label_to_idx: dict[str, int] = {}
        for p in paragrafos:
            if p.tipo == TipoSecao.NOTA_RODAPE:
                continue
            for m in _REGEX_FOOTNOTE_REF.finditer(p.texto):
                label = m.group(1)
                if label not in label_to_idx:
                    label_to_idx[label] = p.indice

        final: list[Paragrafo] = []
        for p in paragrafos:
            if p.tipo == TipoSecao.NOTA_RODAPE and p.ref_nota:
                pai_idx = label_to_idx.get(p.ref_nota)
                if pai_idx is None:
                    logger.warning(
                        f"footnote órfã: [^{p.ref_nota}] sem ref "
                        f"localizável em {p.arquivo}"
                    )
                final.append(
                    dataclasses.replace(p, paragrafo_pai_idx=pai_idx)
                )
            else:
                final.append(p)
        return final
```

CRÍTICO (PATTERNS.md §"markdown.py" + RESEARCH.md Pitfalls 1-8):
- Ordem do pipeline `parsear()` é LOAD-BEARING: read_bytes → decode → strip BOM → CRLF→LF → NFC → encode UTF-8 → linha_offsets → parse → walk → resolver. Inverter qualquer passo quebra os offsets (Pitfalls 4 e 6 explicitamente).
- `assert token.map is not None` em `_build_paragrafo` é NECESSÁRIO para mypy strict (Pitfall 8) — `Token.map: list[int] | None` na lib.
- Conversão half-open → 1-based inclusive: `linha_inicio = map[0] + 1; linha_fim = map[1]` (NÃO `+1` no fim — Pitfall 2).
- Trailing `\n` strip em `_build_paragrafo` (Pitfall 3): `chunk.rstrip(b"\n")` antes de decode; mantém `offset_bytes` original.
- `dataclasses.replace(p, paragrafo_pai_idx=pai_idx)` é a ÚNICA forma de "atualizar" frozen dataclass (D-16).
- Walker emite `Paragrafo` SOMENTE para `paragraph_open`, `heading_open`, `fence`. Ignora `bullet_list_open`, `ordered_list_open`, `list_item_open`, `blockquote_open` (Pitfall 7 — duplicação).
- 3 import groups separados por blank line: stdlib (`dataclasses`, `re`, `unicodedata`, `pathlib`) → third-party (`loguru`, `markdown_it`, `mdit_py_plugins`) → local (`biblio_validador.parser.types`). Ruff `I` automatiza.
- NÃO usar `read_text(encoding="utf-8")` (perde controle do BOM). NÃO usar `token.children[*].content` para reconstruir texto (perde markup). NÃO normalizar NFC depois de tokenizar.

## Contrato 6: src/biblio_validador/parser/__init__.py (D-22 — RESEARCH.md user_constraints lines 128-133)

```python
"""Sub-pacote parser — segmentação de .md/.tex em list[Paragrafo]."""

from biblio_validador.parser.markdown import ParserMd
from biblio_validador.parser.types import Paragrafo, TipoSecao

__all__ = ["ParserMd", "Paragrafo", "TipoSecao"]
```

CRÍTICO (PATTERNS.md §"__init__.py"):
- Docstring PT-BR linha 1, blank line, então re-exports (estilo de Phase 1 `src/biblio_validador/__init__.py`).
- Imports ABSOLUTOS `biblio_validador.parser.markdown` (NÃO relativos `.markdown`) — match Phase 1 `tests/test_cli.py` style.
- `__all__` obrigatório — silencia ruff `F401` nos re-exports e torna o contrato `from biblio_validador.parser import *` explícito.
- NÃO adicionar `__version__` aqui (single source of truth é `src/biblio_validador/__init__.py` raiz).
- Ordem alfabética dos imports: `markdown` antes de `types` (ruff `I` enforces).

## Contrato 7: tests/parser/test_markdown.py (D-26 + property — RESEARCH.md Test Strategy lines 1010-1160)

```python
"""Smoke tests Phase 2 — parser CommonMark + footnotes (D-26)."""
import unicodedata
from pathlib import Path

import pytest

from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao


EOLICA_FIXTURE = Path(__file__).parent / "fixtures" / "eolica_first_30.md"


@pytest.fixture
def parser() -> ParserMd:
    return ParserMd()


def test_paragrafo_simples_um_so_paragrafo(
    tmp_path: Path, parser: ParserMd
) -> None:
    """D-26.1: arquivo com 1 parágrafo simples."""
    f = tmp_path / "simples.md"
    f.write_text("Texto simples.\n", encoding="utf-8")
    paragrafos = parser.parsear(f)
    assert len(paragrafos) == 1
    p = paragrafos[0]
    assert p.linha_inicio == 1
    assert p.linha_fim == 1  # Pitfall 2 — half-open → 1-based inclusivo
    assert p.offset_bytes == 0
    assert p.tipo == TipoSecao.DESCONHECIDO
    assert p.texto == "Texto simples."  # Pitfall 3 — sem trailing \n
    assert p.nivel_heading is None
    assert p.ref_nota is None


def test_heading_classificacao_e_heranca(
    tmp_path: Path, parser: ParserMd
) -> None:
    """D-26.2: 3 headings canônicos com herança de tipo (D-05/D-06/D-07)."""
    src = (
        "# Título do Artigo\n"
        "\n"
        "## RESUMO\n"
        "\n"
        "Parágrafo do resumo.\n"
        "\n"
        "## INTRODUÇÃO\n"
        "\n"
        "Parágrafo da intro.\n"
    )
    f = tmp_path / "headings.md"
    f.write_text(src, encoding="utf-8")
    paragrafos = parser.parsear(f)

    tipos = [(p.tipo, p.nivel_heading, p.texto[:20]) for p in paragrafos]
    assert tipos[0] == (TipoSecao.TITULO, 1, "# Título do Artigo")
    assert tipos[1] == (TipoSecao.RESUMO, 2, "## RESUMO")
    assert tipos[2] == (TipoSecao.RESUMO, None, "Parágrafo do resumo")
    assert tipos[3] == (TipoSecao.INTRODUCAO, 2, "## INTRODUÇÃO")
    assert tipos[4][0] == TipoSecao.INTRODUCAO


def test_normalizacao_nfc(tmp_path: Path, parser: ParserMd) -> None:
    """D-26.3: NFD → NFC normalization (CORE-11)."""
    nfd = "cafe\u0301"  # NFD: e + combining acute (U+0301)
    src = f"Texto com {nfd}.\n"
    f = tmp_path / "nfd.md"
    f.write_bytes(src.encode("utf-8"))
    paragrafos = parser.parsear(f)
    assert len(paragrafos) == 1
    # NFC composed: "caf" + precomposed e-acute (U+00E9)
    assert "café" in paragrafos[0].texto
    # Inverse check: combining acute MUST NOT survive NFC (Pitfall 4)
    assert "\u0301" not in paragrafos[0].texto


def test_footnote_separa_e_linka(
    tmp_path: Path, parser: ParserMd
) -> None:
    """D-26.4: footnote separation + linkage (D-14/D-15/D-16)."""
    src = (
        "Parágrafo com referência[^1].\n"
        "\n"
        "[^1]: Corpo da nota.\n"
    )
    f = tmp_path / "fn.md"
    f.write_text(src, encoding="utf-8")
    paragrafos = parser.parsear(f)

    notas = [p for p in paragrafos if p.tipo == TipoSecao.NOTA_RODAPE]
    corpos = [p for p in paragrafos if p.tipo != TipoSecao.NOTA_RODAPE]
    assert len(notas) == 1
    nota = notas[0]
    assert nota.ref_nota == "1"
    assert nota.texto == "Corpo da nota."
    pai = corpos[0]
    assert "[^1]" in pai.texto
    assert nota.paragrafo_pai_idx == pai.indice


def test_artigo_eolica_excerpt(parser: ParserMd) -> None:
    """D-26.5: artigo Eólica real (integration sub-fixture)."""
    if not EOLICA_FIXTURE.exists():
        pytest.skip("fixture não disponível")
    paragrafos = parser.parsear(EOLICA_FIXTURE)
    assert len(paragrafos) >= 5

    # Primeiro parágrafo: heading h1 = TITULO
    p0 = paragrafos[0]
    assert p0.linha_inicio == 1
    assert p0.tipo == TipoSecao.TITULO
    assert p0.nivel_heading == 1
    assert "Correntes eólicas" in p0.texto

    # heading "## RESUMO" na linha 9 do fixture
    resumos = [p for p in paragrafos
               if p.tipo == TipoSecao.RESUMO
               and p.nivel_heading == 2]
    assert len(resumos) == 1
    assert resumos[0].linha_inicio == 9

    # parágrafo do resumo na linha 11 herda RESUMO
    paragrafos_resumo = [p for p in paragrafos
                         if p.tipo == TipoSecao.RESUMO
                         and p.nivel_heading is None]
    assert len(paragrafos_resumo) == 1
    assert paragrafos_resumo[0].linha_inicio == 11
    assert "Investiga-se" in paragrafos_resumo[0].texto


def test_round_trip_offset_bytes(
    tmp_path: Path, parser: ParserMd
) -> None:
    """Property: src_bytes_nfc[offset:offset+len_bytes] == texto.encode().

    Pin do invariante byte-exact (Phase 5 PatchAplicador depende).
    Catches Pitfalls 2, 3, 4, 6 simultaneously.
    """
    src = (
        "# Title\n\nFirst para with é.\n\nSecond para[^1].\n\n"
        "[^1]: nota com **bold**.\n"
    )
    f = tmp_path / "rt.md"
    f.write_text(src, encoding="utf-8")
    paragrafos = parser.parsear(f)

    raw = f.read_bytes()
    nfc = unicodedata.normalize(
        "NFC", raw.decode("utf-8")
    ).encode("utf-8")

    for p in paragrafos:
        chunk = nfc[p.offset_bytes:p.offset_bytes + p.len_bytes]
        assert chunk.decode("utf-8") == p.texto, (
            f"round-trip falhou para {p.indice}: "
            f"chunk={chunk!r} texto={p.texto!r}"
        )
```

CRÍTICO (PATTERNS.md §"test_markdown.py" + RESEARCH.md Test Strategy):
- 6 testes EXATAMENTE com os nomes acima (sem variação) — VALIDATION.md `Per-Task Verification Map` referencia cada nome.
- `from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao` — exercita o re-export D-22 (contrato público).
- Test 5 usa `pytest.skip("fixture não disponível")` se fixture ausente — mantém suite verde durante reorder de tasks.
- Test 3 usa `f.write_bytes(src.encode("utf-8"))` NÃO `f.write_text` (write_text pode silenciosamente re-normalizar em alguns FS).
- Test 6 (round-trip) é THE most important — single test catches Pitfalls 2, 3, 4, 6 simultaneamente.
- Type hints em todas as assinaturas: `(tmp_path: Path, parser: ParserMd) -> None` (consistência com Phase 1 `tests/test_cli.py`).
- Imports em 3 grupos: stdlib (`unicodedata`, `pathlib`) → third-party (`pytest`) → local (`biblio_validador.parser`).

</interfaces>

<sequenced_action_summary>
Execução em 3 tarefas sequenciais (Wave 1) seguindo PATTERNS.md §"Sequenced Action Pattern" lines 768-778:

- Task 1 (Scaffold + produção): cria os 5 arquivos do módulo `parser/` em ordem topológica (markers vazios → fixture → types.py → markdown.py → __init__.py). Não roda testes ainda.
- Task 2 (Test suite): cria `tests/parser/test_markdown.py` com 6 testes; roda `uv run pytest tests/parser/ -x` até verde.
- Task 3 (Nyquist gate): roda coverage >= 90%, mypy strict, ruff check + format. Phase 2 declara COMPLETA.

Bloco de commit é responsabilidade do orchestrator `gsd-execute-phase` após Task 3.
</sequenced_action_summary>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold módulo parser/ + fixture Eólica + types.py + markdown.py + __init__.py</name>
  <files>
    src/biblio_validador/parser/__init__.py,
    src/biblio_validador/parser/types.py,
    src/biblio_validador/parser/markdown.py,
    tests/parser/__init__.py,
    tests/parser/fixtures/__init__.py,
    tests/parser/fixtures/eolica_first_30.md
  </files>
  <read_first>
    - .planning/phases/02-parser-markdown/02-CONTEXT.md (D-01 a D-27 — 27 decisões locked NÃO-NEGOCIÁVEIS; especialmente §"Localização e shape do Paragrafo" D-01/D-02, §"Enum TipoSecao" D-03/D-04, §"Classificação de seção" D-05/D-06/D-07/D-08, §"Offsets byte-exact" D-09/D-10, §"NFC normalization" D-11/D-12/D-13, §"Footnotes" D-14/D-15/D-16, §"Encoding e bordas" D-18/D-19/D-20, §"API pública" D-21/D-22)
    - .planning/phases/02-parser-markdown/02-RESEARCH.md (§"Code Examples" Example 2 lines 693-936 — VERBATIM canonical para markdown.py; Example 3 lines 938-982 — VERBATIM canonical para types.py; §"Common Pitfalls" lines 511-628 — Pitfalls 1, 2, 3, 4, 5, 6, 7, 8 com mitigações; §"Architecture Patterns" Pattern 1 lines 312-380 token walk, Pattern 2 lines 382-426 footnote linkage, Pattern 3 lines 428-470 heading normalization)
    - .planning/phases/02-parser-markdown/02-PATTERNS.md (§"types.py" lines 64-138, §"markdown.py" lines 141-435, §"__init__.py" lines 30-60, §"tests/parser/fixtures/eolica_first_30.md" lines 605-648, §"Sequenced Action Pattern" lines 768-778)
    - .planning/phases/02-parser-markdown/02-VALIDATION.md (§"Wave 0 Requirements" — confirma a lista exata dos 7 arquivos)
    - .planning/phases/01-bootstrap/01-PATTERNS.md §"Style enforcement" + §"Test invocation contract" (estilo herdado: line-length 80, ruff I/UP/B, mypy strict, type hints obrigatórios)
    - src/biblio_validador/__init__.py (estilo de docstring PT-BR linha 1)
    - src/biblio_validador/cli.py (estilo de imports + docstring multi-linha)
    - tests/test_cli.py (estilo de imports em 3 grupos, type hints `-> None`)
    - pyproject.toml (deps já instaladas via Phase 1: markdown-it-py, mdit-py-plugins, loguru — confirmar presença antes de codar)
    - /home/kalyllamarck/projetos/Doutorado/Artigos/Professora Gina Pompeu/Energia Eólica Nordeste/artigo_final/artigo.md (FONTE do excerpt — primeiras ~10 linhas + footnotes)
    - CLAUDE.md raiz biblioteca_canonica + /home/kalyllamarck/projetos/Doutorado/CLAUDE.md (regras: Python 3.13+, type hints, max 80 chars/linha, PT-BR, uv, loguru, NÃO inventar referências)
  </read_first>
  <behavior>
    - **Test 1 (`test_paragrafo_simples_um_so_paragrafo`):** parser de arquivo com `"Texto simples.\n"` retorna 1 `Paragrafo` com `linha_inicio == 1`, `linha_fim == 1`, `offset_bytes == 0`, `tipo == DESCONHECIDO`, `texto == "Texto simples."` (sem trailing `\n`), `nivel_heading is None`, `ref_nota is None`. Cobre Pitfalls 2 (off-by-one) + 3 (trailing newline).
    - **Test 2 (`test_heading_classificacao_e_heranca`):** parser de 3 headings canônicos (`# Título do Artigo`, `## RESUMO`, `## INTRODUÇÃO`) com 1 parágrafo abaixo de cada um retorna 5 entradas com tipos `[TITULO/h1, RESUMO/h2, RESUMO/None, INTRODUCAO/h2, INTRODUCAO/None]` — cobre D-05 (mapa heading), D-06 (1º h1 → TITULO), D-07 (parágrafos herdam tipo da seção pai).
    - **Test 3 (`test_normalizacao_nfc`):** parser de `"Texto com cafe\u0301.\n"` (NFD) retorna `Paragrafo.texto` contendo `"café"` (NFC composed) e SEM `"\u0301"` (combining mark eliminado). Cobre CORE-11 + Pitfall 4 (NFD macOS).
    - **Test 4 (`test_footnote_separa_e_linka`):** parser de `"Parágrafo com referência[^1].\n\n[^1]: Corpo da nota.\n"` retorna ≥ 2 parágrafos: o pai contém `"[^1]"` no texto (D-15 — referência embutida), a nota é `Paragrafo(tipo=NOTA_RODAPE, ref_nota="1", texto="Corpo da nota.")` com `paragrafo_pai_idx` = índice do pai (D-16 — linkage).
    - **Test 5 (`test_artigo_eolica_excerpt`):** parser do fixture `eolica_first_30.md` retorna ≥ 5 parágrafos; primeiro é `TITULO/h1` com `"Correntes eólicas"` no texto e `linha_inicio == 1`; existe exatamente 1 heading `RESUMO/h2` com `linha_inicio == 9`; existe exatamente 1 parágrafo `RESUMO/None` com `linha_inicio == 11` e `"Investiga-se"` no texto. Cobre integração com artigo real (D-26.5 + success criterion #4).
    - **Test 6 (`test_round_trip_offset_bytes`):** para cada `Paragrafo` retornado, `src_bytes_nfc[p.offset_bytes:p.offset_bytes + p.len_bytes].decode("utf-8") == p.texto`. Pin do invariante byte-exact que Phase 5 PatchAplicador depende. Catches Pitfalls 2, 3, 4, 6 simultaneamente.
    - **Estado de erro esperado durante Task 1:** Os 6 testes ainda NÃO existem (Task 2 cria `test_markdown.py`). `uv run pytest tests/parser/ -v` em Task 1 retornaria "no tests collected" (esperado — Task 1 só cria scaffolding + módulo de produção, não executa pytest).
  </behavior>
  <action>
    Execute em ordem (PATTERNS.md §"Sequenced Action Pattern" lines 768-778). Cada falha aborta a tarefa.

    **Passo 1: Criar diretórios novos.**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    mkdir -p src/biblio_validador/parser
    mkdir -p tests/parser/fixtures
    ```

    **Passo 2: Criar `tests/parser/__init__.py` VAZIO (zero bytes — Phase 1 D-16 inheritance).**

    Escrever arquivo via Write tool com conteúdo vazio (string `""`). NUNCA usar `cat`/`echo` para criar — use Write. Esperado: `wc -c tests/parser/__init__.py` retorna 0.

    **Passo 3: Criar `tests/parser/fixtures/__init__.py` VAZIO (zero bytes).**

    Mesma regra do passo 2. Esperado: `wc -c tests/parser/fixtures/__init__.py` retorna 0.

    **Passo 4: Criar `tests/parser/fixtures/eolica_first_30.md` (D-26.5).**

    Escrever via Write tool EXATAMENTE este conteúdo (UTF-8, LF — sem `\r\n`; o conteúdo termina com `\n` na última linha):

    ```
    # Correntes eólicas sobre a (ir)racionalidade ambiental, energia renovável sem transição energética no Nordeste brasileiro

    ***Wind currents over the (ir)rationality of the environment, renewable energy without energy transition in the Brazilian Northeast***

    **GINA VIDAL MARCÍLIO POMPEU**[^a]

    **KALYL LAMARCK**[^b]

    ## RESUMO

    Investiga-se se a expansão da energia eólica no Ceará e no Rio Grande do Norte, entre 2004 e 2025, constitui transição energética no sentido da racionalidade ambiental proposta por Leff, ou se reproduz, sob forma renovável, a racionalidade econômica formalizada por Summers.

    ## PALAVRAS-CHAVE

    Transição energética; racionalidade ambiental; racismo ambiental; energia eólica; Nordeste brasileiro.

    ## ABSTRACT

    This study investigates whether the expansion of wind energy in Ceará and Rio Grande do Norte constitutes an energy transition under Leff's environmental rationality.

    ## KEYWORDS

    Energy transition; environmental rationality; environmental racism; wind energy; Brazilian Northeast.

    ## INTRODUÇÃO

    Os sertões e as caatingas do Ceará e do Rio Grande do Norte abrigam, entre 2004 e 2025, uma das mais extensas expansões de parques eólicos do continente americano.

    [^a]: Doutorado em Direito Constitucional pela Universidade de Fortaleza.
    [^b]: Doutorando em Direito Constitucional pelo Programa de Pós-Graduação em Direito da Universidade de Fortaleza.
    ```

    CRÍTICO — numeração de linhas é load-bearing para Test 5:
    - Linha 1: heading h1 (`# Correntes eólicas...`)
    - Linha 9: `## RESUMO`
    - Linha 11: parágrafo `Investiga-se...`
    - Linhas 13, 15, 17, 19, 21, 23, 25, 27: demais headings + parágrafos
    - Linhas 30-31: corpos de footnotes `[^a]:` e `[^b]:`

    Verificar com `awk 'NR==1||NR==9||NR==11{print NR": "$0}' tests/parser/fixtures/eolica_first_30.md` — deve mostrar:
    ```
    1: # Correntes eólicas sobre a (ir)racionalidade ambiental...
    9: ## RESUMO
    11: Investiga-se se a expansão...
    ```

    NÃO copiar o artigo full (~102KB) — o excerpt acima é o mínimo necessário para D-26.5. Conteúdo é PT-BR/EN; preservar verbatim os asteriscos `***Wind currents...***` e a footnote syntax `[^a]`/`[^b]`.

    **Passo 5: Criar `src/biblio_validador/parser/types.py` (D-02 + D-03).**

    Escrever via Write tool EXATAMENTE o conteúdo do "Contrato 4" do `<interfaces>` block acima (RESEARCH.md Example 3 lines 938-982 verbatim). Pontos críticos:
    - Linha 1: `"""Tipos parser-internal: Paragrafo + TipoSecao."""`
    - Imports stdlib em ordem alfabética: `dataclasses` → `enum` → `pathlib`
    - Enum `TipoSecao(Enum)` (NÃO `StrEnum`) com EXATAMENTE 14 valores na ordem listada — D-03 não admite extras nem reordenação
    - Dataclass `Paragrafo` com `@dataclass(frozen=True, slots=True)` e EXATAMENTE 11 campos na ordem D-02 + comentários inline
    - PEP 604 union: `int | None`, `str | None` — NUNCA `Optional[int]`
    - NÃO importar `pydantic`, NÃO adicionar `__post_init__`, `__str__`, `__repr__`, `__eq__` custom

    Após gravar, rodar para sanity check:
    ```bash
    uv run python -c "from biblio_validador.parser.types import Paragrafo, TipoSecao; print(len(TipoSecao)); print(len(Paragrafo.__dataclass_fields__))"
    ```
    Esperado: `14` e `11`.

    **Passo 6: Criar `src/biblio_validador/parser/markdown.py` (D-09 a D-25).**

    Escrever via Write tool EXATAMENTE o conteúdo do "Contrato 5" do `<interfaces>` block acima (RESEARCH.md Example 2 lines 693-936 + ajustes de PATTERNS.md). Pontos críticos:
    - Docstring multi-linha PT-BR identifica módulo + invariante NFC
    - 3 grupos de imports separados por blank line: stdlib (`dataclasses`, `re`, `unicodedata`, `pathlib`) → third-party (`loguru`, `markdown_it`, `mdit_py_plugins`) → local (`biblio_validador.parser.types`)
    - 3 regex compiladas em module-level constants prefixadas com `_`: `_REGEX_FOOTNOTE_REF`, `_RE_PUNCT`, `_RE_SPACES`
    - Classe `ParserMd` com docstring contendo o invariante NFC verbatim (D-12 mandate): "Offsets são relativos ao source UTF-8 NFC. Arquivos em NFD (ex.: macOS) são convertidos silenciosamente. Validadores operam sempre em NFC."
    - `__init__(self) -> None`: `self._md = MarkdownIt().use(footnote_plugin)` + `self._mapa_secao = self._carregar_mapa_secao()`
    - `parsear(self, path: Path) -> list[Paragrafo]`: pipeline de 9 passos NA ORDEM (Pitfall 4 + 6): `read_bytes` → `decode("utf-8")` → `lstrip("\ufeff")` → `replace("\r\n", "\n")` → `unicodedata.normalize("NFC", text)` → `if not text_nfc.strip(): return []` → `text_nfc.encode("utf-8")` → `_construir_linha_offsets` → `self._md.parse(text_nfc)` → `_walk(...)` → `_resolver_pais(...)`
    - `_construir_linha_offsets(src_bytes)` retorna `list[int]` com sentinela final `len(src_bytes)` (Pitfall 6)
    - `_carregar_mapa_secao()` é `@classmethod` retornando `dict[str, TipoSecao]` com EXATAMENTE 13 entradas listadas em D-05 (chaves normalizadas SEM acento, lowercase)
    - `_classificar_heading(texto, nivel, is_first_h1)`: ordem load-bearing D-06 (is_first_h1 PRIMEIRO → mapa → SECAO se h2+ → DESCONHECIDO)
    - `_normalizar_heading`: NFKD → strip combining marks → casefold → strip pontuação → colapsar espaços
    - `_build_paragrafo`: `assert token.map is not None` (Pitfall 8 — mypy strict narrowing) + comentário tabulado da regra `linha_inicio = map[0]+1; linha_fim = map[1]` (Pitfall 2) + `chunk.rstrip(b"\n")` antes de decode (Pitfall 3)
    - `_walk`: 5 branches (`footnote_open` toggle, `footnote_close` toggle, `heading_open` emit + `i += 3`, `paragraph_open` emit (+ NOTA_RODAPE se `in_footnote`, ref_nota=`in_footnote`), `fence` emit DESCONHECIDO). DEFAULT branch: `i += 1` (Pitfall 7 — bullet_list_open / list_item_open / blockquote_open silenciosamente pulados)
    - `_resolver_pais`: dict `label_to_idx`, depois `dataclasses.replace(p, paragrafo_pai_idx=pai_idx)` (D-16 — única forma de "atualizar" frozen)
    - Logging via `from loguru import logger` (D-25 primeiro consumidor real): `logger.info` em entry/exit de `parsear`, `logger.error` antes de re-raise em `except FileNotFoundError`/`UnicodeDecodeError`, `logger.info` em NFD→NFC conversion (Pitfall 4 audit trail), `logger.warning` em footnote órfã (`_resolver_pais`)
    - NÃO chamar `logger.add(...)` — orchestrator (Phase 8+) configura handler. Phase 2 só emite.

    Após gravar, sanity check:
    ```bash
    uv run python -c "from biblio_validador.parser.markdown import ParserMd; p = ParserMd(); print(type(p).__name__); print(len(p._mapa_secao))"
    ```
    Esperado: `ParserMd` e `13`.

    **Passo 7: Criar `src/biblio_validador/parser/__init__.py` (D-22).**

    Escrever via Write tool EXATAMENTE este conteúdo (5 linhas + 1 blank line):
    ```python
    """Sub-pacote parser — segmentação de .md/.tex em list[Paragrafo]."""

    from biblio_validador.parser.markdown import ParserMd
    from biblio_validador.parser.types import Paragrafo, TipoSecao

    __all__ = ["ParserMd", "Paragrafo", "TipoSecao"]
    ```

    CRÍTICO:
    - Imports ABSOLUTOS `biblio_validador.parser.markdown` (NÃO relativos `.markdown`)
    - Ordem alfabética: `markdown` antes de `types` (ruff `I` enforces)
    - `__all__` literal list com 3 nomes — silencia ruff `F401` nos re-exports
    - NÃO adicionar `__version__` (single source of truth é `src/biblio_validador/__init__.py` raiz)

    Após gravar, sanity check do contrato público D-22:
    ```bash
    uv run python -c "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao; print('ok')"
    ```
    Esperado: `ok`.

    **Passo 8: NÃO rodar pytest ainda.** Task 2 cria `test_markdown.py` e roda os 6 testes. Task 1 só grava arquivos.
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 1.1 — Os 6 arquivos existem
      test -f src/biblio_validador/parser/__init__.py || { echo "FAIL: parser/__init__.py"; exit 1; }
      test -f src/biblio_validador/parser/types.py || { echo "FAIL: parser/types.py"; exit 1; }
      test -f src/biblio_validador/parser/markdown.py || { echo "FAIL: parser/markdown.py"; exit 1; }
      test -f tests/parser/__init__.py || { echo "FAIL: tests/parser/__init__.py"; exit 1; }
      test -f tests/parser/fixtures/__init__.py || { echo "FAIL: tests/parser/fixtures/__init__.py"; exit 1; }
      test -f tests/parser/fixtures/eolica_first_30.md || { echo "FAIL: eolica fixture"; exit 1; }

      # 1.2 — Markers vazios (Phase 1 D-16 inheritance)
      [ "$(wc -c < tests/parser/__init__.py)" = "0" ] || { echo "FAIL: tests/parser/__init__.py não-vazio"; exit 1; }
      [ "$(wc -c < tests/parser/fixtures/__init__.py)" = "0" ] || { echo "FAIL: fixtures/__init__.py não-vazio"; exit 1; }

      # 1.3 — Fixture com numeração calibrada para Test 5 (load-bearing)
      LINHA_1=$(awk "NR==1" tests/parser/fixtures/eolica_first_30.md)
      LINHA_9=$(awk "NR==9" tests/parser/fixtures/eolica_first_30.md)
      LINHA_11=$(awk "NR==11" tests/parser/fixtures/eolica_first_30.md)
      echo "$LINHA_1" | grep -qF "Correntes eólicas" || { echo "FAIL: fixture linha 1 não é o título h1"; exit 1; }
      [ "$LINHA_9" = "## RESUMO" ] || { echo "FAIL: fixture linha 9 não é \"## RESUMO\" (got: $LINHA_9)"; exit 1; }
      echo "$LINHA_11" | grep -qF "Investiga-se" || { echo "FAIL: fixture linha 11 não começa com Investiga-se (got: $LINHA_11)"; exit 1; }
      grep -qF "[^a]:" tests/parser/fixtures/eolica_first_30.md || { echo "FAIL: fixture sem footnote [^a]:"; exit 1; }
      grep -qF "[^b]:" tests/parser/fixtures/eolica_first_30.md || { echo "FAIL: fixture sem footnote [^b]:"; exit 1; }

      # 1.4 — types.py: enum TipoSecao com 14 valores + dataclass Paragrafo com 11 campos
      grep -qF "from dataclasses import dataclass" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem dataclass import"; exit 1; }
      grep -qF "from enum import Enum" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem Enum import"; exit 1; }
      grep -qF "from pathlib import Path" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem Path import"; exit 1; }
      grep -qF "class TipoSecao(Enum):" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem TipoSecao(Enum)"; exit 1; }
      ! grep -qF "class TipoSecao(StrEnum)" src/biblio_validador/parser/types.py || { echo "FAIL: D-03 violado — usou StrEnum"; exit 1; }
      grep -qF "@dataclass(frozen=True, slots=True)" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem dataclass frozen+slots"; exit 1; }
      grep -qF "class Paragrafo:" src/biblio_validador/parser/types.py || { echo "FAIL: types.py sem class Paragrafo"; exit 1; }
      # 14 valores de TipoSecao
      for valor in TITULO AUTORES RESUMO PALAVRAS_CHAVE ABSTRACT KEYWORDS SUMARIO INTRODUCAO SECAO CONCLUSAO REFERENCIAS NOTA_RODAPE CORPO DESCONHECIDO; do
        grep -qE "^    $valor = " src/biblio_validador/parser/types.py || { echo "FAIL: TipoSecao.$valor ausente"; exit 1; }
      done
      # 11 campos de Paragrafo
      for campo in arquivo: indice: tipo: nivel_heading: texto: linha_inicio: linha_fim: offset_bytes: len_bytes: ref_nota: paragrafo_pai_idx:; do
        grep -qF "    $campo" src/biblio_validador/parser/types.py || { echo "FAIL: Paragrafo.$campo ausente"; exit 1; }
      done
      # PEP 604 syntax (UP rule)
      ! grep -qE "Optional\[" src/biblio_validador/parser/types.py || { echo "FAIL: Optional[] usado (deve ser PEP 604 X | None)"; exit 1; }

      # 1.5 — markdown.py: imports + classe + 6+ helpers + pipeline NFC + logging
      grep -qF "from loguru import logger" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem loguru"; exit 1; }
      grep -qF "from markdown_it import MarkdownIt" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem MarkdownIt"; exit 1; }
      grep -qF "from mdit_py_plugins.footnote import footnote_plugin" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem footnote_plugin"; exit 1; }
      grep -qF "from biblio_validador.parser.types import Paragrafo, TipoSecao" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem import Paragrafo/TipoSecao"; exit 1; }
      grep -qF "import unicodedata" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem unicodedata"; exit 1; }
      grep -qF "import dataclasses" src/biblio_validador/parser/markdown.py || { echo "FAIL: markdown.py sem dataclasses"; exit 1; }
      grep -qF "class ParserMd:" src/biblio_validador/parser/markdown.py || { echo "FAIL: ParserMd class ausente"; exit 1; }
      grep -qF "def parsear(self, path: Path) -> list[Paragrafo]:" src/biblio_validador/parser/markdown.py || { echo "FAIL: parsear signature errada"; exit 1; }
      grep -qF "MarkdownIt().use(footnote_plugin)" src/biblio_validador/parser/markdown.py || { echo "FAIL: footnote_plugin não registrado"; exit 1; }
      grep -qF "unicodedata.normalize(\"NFC\"" src/biblio_validador/parser/markdown.py || { echo "FAIL: NFC normalize ausente"; exit 1; }
      grep -qF "replace(\"\\r\\n\", \"\\n\")" src/biblio_validador/parser/markdown.py || { echo "FAIL: CRLF→LF normalize ausente (D-20)"; exit 1; }
      # 6 helpers presentes
      for helper in _construir_linha_offsets _carregar_mapa_secao _classificar_heading _normalizar_heading _build_paragrafo _walk _resolver_pais; do
        grep -qE "def $helper" src/biblio_validador/parser/markdown.py || { echo "FAIL: helper $helper ausente"; exit 1; }
      done
      # Invariante NFC documentada na docstring de ParserMd (D-12)
      grep -qF "Offsets são relativos ao source UTF-8 NFC" src/biblio_validador/parser/markdown.py || { echo "FAIL: invariante NFC não documentada (D-12)"; exit 1; }
      # Pitfall 8 — assert para mypy strict narrowing
      grep -qF "assert token.map is not None" src/biblio_validador/parser/markdown.py || { echo "FAIL: assert token.map is not None ausente (Pitfall 8)"; exit 1; }
      # Pitfall 6 — strip BOM antes de NFC
      grep -qF "lstrip(\"\\ufeff\")" src/biblio_validador/parser/markdown.py || grep -qF "startswith(\"\\ufeff\")" src/biblio_validador/parser/markdown.py || { echo "FAIL: BOM strip ausente (Pitfall 6)"; exit 1; }
      # D-16: dataclasses.replace
      grep -qF "dataclasses.replace" src/biblio_validador/parser/markdown.py || { echo "FAIL: dataclasses.replace ausente (D-16)"; exit 1; }

      # 1.6 — __init__.py: re-export D-22
      grep -qF "from biblio_validador.parser.markdown import ParserMd" src/biblio_validador/parser/__init__.py || { echo "FAIL: __init__.py sem ParserMd re-export"; exit 1; }
      grep -qF "from biblio_validador.parser.types import Paragrafo, TipoSecao" src/biblio_validador/parser/__init__.py || { echo "FAIL: __init__.py sem types re-export"; exit 1; }
      grep -qF "__all__ = [\"ParserMd\", \"Paragrafo\", \"TipoSecao\"]" src/biblio_validador/parser/__init__.py || { echo "FAIL: __init__.py sem __all__ canônico"; exit 1; }
      ! grep -qF "__version__" src/biblio_validador/parser/__init__.py || { echo "FAIL: __version__ leak em parser/__init__.py"; exit 1; }

      # 1.7 — Smoke import via re-export D-22
      uv run python -c "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao; assert len(TipoSecao) == 14; assert len(Paragrafo.__dataclass_fields__) == 11" || { echo "FAIL: smoke import re-export D-22"; exit 1; }

      # 1.8 — ParserMd().__init__ funciona + mapa_secao tem 13 entradas
      uv run python -c "from biblio_validador.parser import ParserMd; p = ParserMd(); assert len(p._mapa_secao) == 13" || { echo "FAIL: ParserMd init ou _mapa_secao errado"; exit 1; }

      echo PASS: Task 1 scaffold + módulo parser/ + fixture + types + markdown + __init__ ok
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-1.1:** Comando `test -f src/biblio_validador/parser/__init__.py && test -f src/biblio_validador/parser/types.py && test -f src/biblio_validador/parser/markdown.py && test -f tests/parser/__init__.py && test -f tests/parser/fixtures/__init__.py && test -f tests/parser/fixtures/eolica_first_30.md` retorna exit 0 (6 arquivos criados).
    - **AC-1.2:** `wc -c tests/parser/__init__.py` retorna `0` E `wc -c tests/parser/fixtures/__init__.py` retorna `0` (markers vazios — Phase 1 D-16 inheritance).
    - **AC-1.3:** `awk 'NR==1' tests/parser/fixtures/eolica_first_30.md | grep -F "Correntes eólicas"` retorna 1 linha (heading h1 na linha 1).
    - **AC-1.4:** `awk 'NR==9' tests/parser/fixtures/eolica_first_30.md` retorna exatamente `## RESUMO` (heading RESUMO na linha 9 — load-bearing para Test 5).
    - **AC-1.5:** `awk 'NR==11' tests/parser/fixtures/eolica_first_30.md | grep -F "Investiga-se"` retorna 1 linha (parágrafo do resumo na linha 11 — load-bearing para Test 5).
    - **AC-1.6:** `grep -F "[^a]:" tests/parser/fixtures/eolica_first_30.md && grep -F "[^b]:" tests/parser/fixtures/eolica_first_30.md` exit 0 (footnotes presentes para test_footnote_separa_e_linka via integration).
    - **AC-1.7:** `grep -c "^    [A-Z_]\\+ = \"" src/biblio_validador/parser/types.py` retorna `14` (TipoSecao tem exatamente 14 valores — D-03).
    - **AC-1.8:** `grep -F "@dataclass(frozen=True, slots=True)" src/biblio_validador/parser/types.py` retorna 1 linha (D-02 — frozen + slots ambos ativos).
    - **AC-1.9:** `grep -F "class TipoSecao(Enum):" src/biblio_validador/parser/types.py` retorna 1 linha E `grep -F "StrEnum" src/biblio_validador/parser/types.py` retorna 0 linhas (D-03 — Enum, NÃO StrEnum).
    - **AC-1.10:** `grep -E "Optional\[" src/biblio_validador/parser/types.py src/biblio_validador/parser/markdown.py` retorna 0 linhas (PEP 604 union syntax — ruff `UP` rule).
    - **AC-1.11:** `grep -F "MarkdownIt().use(footnote_plugin)" src/biblio_validador/parser/markdown.py` retorna 1 linha (D-14 — plugin obrigatório).
    - **AC-1.12:** `grep -F 'unicodedata.normalize("NFC"' src/biblio_validador/parser/markdown.py` retorna ≥ 1 linha (CORE-11 + D-11).
    - **AC-1.13:** `grep -F "Offsets são relativos ao source UTF-8 NFC" src/biblio_validador/parser/markdown.py` retorna 1 linha (D-12 — invariante NFC documentada na docstring de ParserMd).
    - **AC-1.14:** `grep -F "assert token.map is not None" src/biblio_validador/parser/markdown.py` retorna 1 linha (Pitfall 8 — mypy strict narrowing).
    - **AC-1.15:** `grep -F "dataclasses.replace" src/biblio_validador/parser/markdown.py` retorna 1 linha (D-16 — única forma de "atualizar" frozen).
    - **AC-1.16:** `grep -E "def (_construir_linha_offsets|_carregar_mapa_secao|_classificar_heading|_normalizar_heading|_build_paragrafo|_walk|_resolver_pais)" src/biblio_validador/parser/markdown.py | wc -l` retorna `7` (todos os 7 helpers privados presentes).
    - **AC-1.17:** `uv run python -c "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao"` exit 0 (re-export D-22 funcional).
    - **AC-1.18:** `uv run python -c "from biblio_validador.parser import ParserMd; p = ParserMd(); assert len(p._mapa_secao) == 13"` exit 0 (mapa heading tem 13 entradas — D-05).
    - **AC-1.19:** `uv run python -c "from biblio_validador.parser import TipoSecao, Paragrafo; assert len(TipoSecao) == 14 and len(Paragrafo.__dataclass_fields__) == 11"` exit 0 (estrutura completa).
    - **AC-1.20:** O verify script automatizado roda e imprime `PASS: Task 1 scaffold + módulo parser/ + fixture + types + markdown + __init__ ok`.
  </acceptance_criteria>
  <done>
    Os 6 arquivos do scaffolding existem com conteúdo canônico verificado por grep. `types.py` tem `TipoSecao(Enum)` com 14 valores + `Paragrafo` com 11 campos frozen+slots. `markdown.py` tem classe `ParserMd` com 7 helpers, pipeline NFC byte-exact, `MarkdownIt().use(footnote_plugin)`, `assert token.map is not None`, `dataclasses.replace`, logging via loguru. `__init__.py` re-exporta os 3 nomes canônicos. Fixture Eólica tem heading h1 na linha 1, `## RESUMO` na linha 9, parágrafo `Investiga-se` na linha 11, e 2 footnotes `[^a]`/`[^b]`. Re-export D-22 funcional via smoke import. NÃO rodar pytest ainda — Task 2 cria `test_markdown.py` e roda os 6 testes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar tests/parser/test_markdown.py com 6 testes (D-26 + property) e rodar até verde</name>
  <files>
    tests/parser/test_markdown.py
  </files>
  <read_first>
    - .planning/phases/02-parser-markdown/02-RESEARCH.md (§"Test Strategy" lines 996-1160 — VERBATIM canonical para os 5 unit tests + 1 property test)
    - .planning/phases/02-parser-markdown/02-PATTERNS.md (§"tests/parser/test_markdown.py" lines 449-595 — estilo de fixture, type hints, imports)
    - .planning/phases/02-parser-markdown/02-VALIDATION.md (§"Per-Task Verification Map" — referencia os 6 nomes exatos: test_paragrafo_simples_um_so_paragrafo, test_heading_classificacao_e_heranca, test_normalizacao_nfc, test_footnote_separa_e_linka, test_artigo_eolica_excerpt, test_round_trip_offset_bytes; §"Test Infrastructure" — comando canônico `uv run pytest tests/parser/ -x`)
    - .planning/phases/02-parser-markdown/02-CONTEXT.md §"Testes (smoke obrigatório)" D-26 + D-27
    - .planning/phases/01-bootstrap/01-PATTERNS.md §"Test invocation contract" (sempre `uv run pytest`, nunca `pytest` direto)
    - tests/test_cli.py (estilo Phase 1: docstring PT-BR linha 1, 3 grupos de imports, type hints `-> None`)
    - src/biblio_validador/parser/__init__.py (criado em Task 1 — confirma o contrato D-22 que o test exercita)
    - tests/parser/fixtures/eolica_first_30.md (criado em Task 1 — Test 5 depende de numeração exata)
  </read_first>
  <behavior>
    Mesmas 6 expectativas de Task 1 (Tests 1-6 listados em `<behavior>` daquela tarefa). A diferença é: Task 2 GRAVA `test_markdown.py` com os 6 testes E roda `uv run pytest tests/parser/ -x` até EXIT 0 com `6 passed`. Se algum teste falhar, é REGRESSÃO de Task 1 (parser produção) — Task 2 fixa o teste OU volta para Task 1 corrigir o módulo.

    **Falhas esperadas e diagnóstico:**
    - `assert p.linha_inicio == 1, p.linha_fim == 1` falha em Test 1 → Pitfall 2 em `_build_paragrafo` (provavelmente `linha_fim = end + 1` em vez de `linha_fim = end`).
    - `assert p.texto == "Texto simples."` falha em Test 1 (texto contém `\n`) → Pitfall 3 — `chunk.rstrip(b"\n")` não aplicado.
    - `assert "café" in paragrafos[0].texto` falha em Test 3 → Pitfall 4 — NFC normalize não rodou ou rodou depois do encode.
    - `assert nota.paragrafo_pai_idx == pai.indice` falha em Test 4 → `_resolver_pais` não rodou ou regex `_REGEX_FOOTNOTE_REF` errado.
    - `assert resumos[0].linha_inicio == 9` falha em Test 5 → fixture com numeração errada (volta para Task 1 passo 4) OU walker errado.
    - `assert chunk.decode("utf-8") == p.texto` falha em Test 6 → falha simultânea de Pitfalls 2/3/4/6 — diagnóstico requer ler `chunk!r` e `texto!r` do erro.
  </behavior>
  <action>
    **Passo 1: Criar `tests/parser/test_markdown.py` via Write tool com EXATAMENTE o conteúdo do "Contrato 7" do `<interfaces>` block acima.**

    Pontos críticos:
    - Docstring linha 1 PT-BR: `"""Smoke tests Phase 2 — parser CommonMark + footnotes (D-26)."""`
    - 3 grupos de imports separados por blank line: stdlib (`unicodedata`, `pathlib.Path`) → third-party (`pytest`) → local (`biblio_validador.parser`)
    - Constante module-level `EOLICA_FIXTURE = Path(__file__).parent / "fixtures" / "eolica_first_30.md"` (após imports, antes da fixture)
    - Fixture pytest decorada: `@pytest.fixture\ndef parser() -> ParserMd: return ParserMd()` (PATTERNS.md §"Fixture pattern" lines 496-504)
    - 6 testes com nomes EXATOS (sem variação) — VALIDATION.md `Per-Task Verification Map` linha 42-47 lista cada um:
      - `test_paragrafo_simples_um_so_paragrafo`
      - `test_heading_classificacao_e_heranca`
      - `test_normalizacao_nfc`
      - `test_footnote_separa_e_linka`
      - `test_artigo_eolica_excerpt`
      - `test_round_trip_offset_bytes`
    - Type hints em todas as assinaturas: `(tmp_path: Path, parser: ParserMd) -> None` (Phase 1 style — `tests/test_cli.py` linha 13). Quebra vertical com line-length 80:
      ```python
      def test_paragrafo_simples_um_so_paragrafo(
          tmp_path: Path, parser: ParserMd
      ) -> None:
      ```
    - Test 3 `test_normalizacao_nfc` usa `f.write_bytes(src.encode("utf-8"))` — NÃO `f.write_text` (write_text pode silenciosamente re-normalizar em alguns FS).
    - Test 5 `test_artigo_eolica_excerpt` recebe somente `parser: ParserMd` (sem `tmp_path`) e usa `pytest.skip("fixture não disponível")` se `EOLICA_FIXTURE.exists()` retornar False.
    - Test 6 `test_round_trip_offset_bytes` SEM type hints na docstring; assertion message via f-string com `chunk!r` e `texto!r` para debugging.

    NÃO adicionar testes extras além dos 6 listados (VALIDATION.md trava o set). NÃO adicionar `parametrize` (PATTERNS.md não menciona). NÃO usar `mocker`/`monkeypatch` (testes são puros — só filesystem temp + parser).

    **Passo 2: Rodar os 6 testes via `uv run pytest tests/parser/ -v`.**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    uv run pytest tests/parser/ -v
    ```

    Esperado:
    - Exit 0
    - Output contém `6 passed` (5 unit + 1 property)
    - Tempo total < 2s (smoke local sem rede)

    **Diagnóstico de falhas (cross-check com Task 1):**
    - Se `ModuleNotFoundError: No module named 'biblio_validador.parser'` → Task 1 não criou os arquivos no caminho certo. Verificar `ls src/biblio_validador/parser/`.
    - Se Test 1 `assert p.linha_fim == 1` falhar reportando `2` → Pitfall 2 em `_build_paragrafo` (corrigir para `linha_fim = end`, NÃO `end + 1`).
    - Se Test 1 `assert p.texto == "Texto simples."` falhar reportando texto com `\n` → Pitfall 3 — adicionar `chunk = src_bytes[offset_bytes:end_offset].rstrip(b"\n")` antes de decode.
    - Se Test 3 `assert "café" in p.texto` falhar com `cafe\u0301` → Pitfall 4 — verificar ordem do pipeline (NFC ANTES de encode + linha_offsets).
    - Se Test 4 `assert nota.paragrafo_pai_idx == pai.indice` falhar com `None` → `_resolver_pais` não foi chamado, ou regex não encontrou `[^1]` no texto do pai (verificar Pitfall 1: `paragraph_open` interno ao `footnote_open` é o que tem `map`).
    - Se Test 5 `assert resumos[0].linha_inicio == 9` falhar → fixture com numeração errada (volta para Task 1 corrigir `eolica_first_30.md`).
    - Se Test 6 round-trip falhar → ler output `chunk!r` vs `texto!r`; provavelmente Pitfall 2 (off-by-one) ou Pitfall 3 (trailing newline).

    **Passo 3: Confirmar que a suite Phase 1 NÃO regrediu.**

    ```bash
    uv run pytest tests/ -v
    ```

    Esperado: 3 testes Phase 1 (`test_cli.py`) + 6 testes Phase 2 (`test_markdown.py`) = `9 passed` total.

    Se algum dos 3 testes Phase 1 falhar → REGRESSÃO inesperada (Phase 2 não deveria afetar Phase 1). Investigar antes de prosseguir para Task 3.

    **Passo 4: NÃO rodar mypy/ruff/coverage ainda.** Esses gates são Task 3.
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 2.1 — Arquivo test_markdown.py existe
      test -f tests/parser/test_markdown.py || { echo "FAIL: test_markdown.py missing"; exit 1; }

      # 2.2 — 6 testes definidos com nomes exatos (VALIDATION.md trava nomes)
      grep -qE "^def test_paragrafo_simples_um_so_paragrafo" tests/parser/test_markdown.py || { echo "FAIL: test_paragrafo_simples_um_so_paragrafo ausente"; exit 1; }
      grep -qE "^def test_heading_classificacao_e_heranca" tests/parser/test_markdown.py || { echo "FAIL: test_heading_classificacao_e_heranca ausente"; exit 1; }
      grep -qE "^def test_normalizacao_nfc" tests/parser/test_markdown.py || { echo "FAIL: test_normalizacao_nfc ausente"; exit 1; }
      grep -qE "^def test_footnote_separa_e_linka" tests/parser/test_markdown.py || { echo "FAIL: test_footnote_separa_e_linka ausente"; exit 1; }
      grep -qE "^def test_artigo_eolica_excerpt" tests/parser/test_markdown.py || { echo "FAIL: test_artigo_eolica_excerpt ausente"; exit 1; }
      grep -qE "^def test_round_trip_offset_bytes" tests/parser/test_markdown.py || { echo "FAIL: test_round_trip_offset_bytes ausente"; exit 1; }
      [ "$(grep -cE "^def test_" tests/parser/test_markdown.py)" = "6" ] || { echo "FAIL: número de testes != 6 (esperado: 6 únicos)"; exit 1; }

      # 2.3 — Re-export D-22 exercitado nos imports
      grep -qF "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao" tests/parser/test_markdown.py || { echo "FAIL: import via re-export D-22 ausente"; exit 1; }

      # 2.4 — Fixture pytest decorada
      grep -qF "@pytest.fixture" tests/parser/test_markdown.py || { echo "FAIL: @pytest.fixture decorator ausente"; exit 1; }
      grep -qF "def parser() -> ParserMd:" tests/parser/test_markdown.py || { echo "FAIL: fixture parser() signature errada"; exit 1; }

      # 2.5 — Teste 3 usa write_bytes (NÃO write_text — Pitfall 4)
      grep -qF "f.write_bytes(src.encode" tests/parser/test_markdown.py || { echo "FAIL: test_normalizacao_nfc não usa write_bytes (regressão Pitfall 4)"; exit 1; }

      # 2.6 — Teste 5 usa pytest.skip se fixture ausente
      grep -qF "pytest.skip" tests/parser/test_markdown.py || { echo "FAIL: pytest.skip ausente em test_artigo_eolica_excerpt"; exit 1; }
      grep -qF "EOLICA_FIXTURE" tests/parser/test_markdown.py || { echo "FAIL: EOLICA_FIXTURE constant ausente"; exit 1; }

      # 2.7 — Teste 6 (round-trip) compara chunk decodificado com p.texto
      grep -qF "chunk.decode(\"utf-8\") == p.texto" tests/parser/test_markdown.py || { echo "FAIL: round-trip assertion ausente"; exit 1; }

      # 2.8 — Type hints nas assinaturas (Phase 1 style)
      grep -qE "tmp_path: Path" tests/parser/test_markdown.py || { echo "FAIL: type hint tmp_path: Path ausente"; exit 1; }
      grep -qE "parser: ParserMd" tests/parser/test_markdown.py || { echo "FAIL: type hint parser: ParserMd ausente"; exit 1; }
      grep -qE "-> None:" tests/parser/test_markdown.py || { echo "FAIL: return type hints ausentes"; exit 1; }

      # 2.9 — pytest verde (gate principal de Task 2)
      uv run pytest tests/parser/ -v 2>&1 | tee /tmp/phase2_pytest.log
      grep -qE "6 passed" /tmp/phase2_pytest.log || { echo "FAIL: 6 testes Phase 2 não passaram"; exit 1; }

      # 2.10 — Suite Phase 1 não regrediu (3 + 6 = 9 passed)
      uv run pytest tests/ 2>&1 | tee /tmp/phase2_full.log
      grep -qE "9 passed" /tmp/phase2_full.log || { echo "FAIL: suite total != 9 testes verdes (Phase 1 regrediu?)"; exit 1; }

      echo PASS: Task 2 6 testes Phase 2 + 3 Phase 1 = 9 passed
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-2.1:** `test -f tests/parser/test_markdown.py` exit 0.
    - **AC-2.2:** `grep -cE "^def test_" tests/parser/test_markdown.py` retorna `6` (exatamente 6 testes — VALIDATION.md trava o set).
    - **AC-2.3:** Os 6 nomes EXATOS aparecem como definição: `test_paragrafo_simples_um_so_paragrafo`, `test_heading_classificacao_e_heranca`, `test_normalizacao_nfc`, `test_footnote_separa_e_linka`, `test_artigo_eolica_excerpt`, `test_round_trip_offset_bytes`. Cada `grep -qE "^def <nome>"` exit 0.
    - **AC-2.4:** `grep -F "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao" tests/parser/test_markdown.py` retorna 1 linha (re-export D-22 exercitado).
    - **AC-2.5:** `grep -F "@pytest.fixture" tests/parser/test_markdown.py` retorna 1 linha E `grep -F "def parser() -> ParserMd:" tests/parser/test_markdown.py` retorna 1 linha (fixture pytest canônica).
    - **AC-2.6:** `grep -F "f.write_bytes(src.encode" tests/parser/test_markdown.py` retorna 1 linha (Test 3 usa write_bytes — Pitfall 4 mitigation).
    - **AC-2.7:** `grep -F "pytest.skip" tests/parser/test_markdown.py && grep -F "EOLICA_FIXTURE" tests/parser/test_markdown.py` exit 0 (Test 5 tem skip se fixture ausente).
    - **AC-2.8:** `grep -F 'chunk.decode("utf-8") == p.texto' tests/parser/test_markdown.py` retorna ≥ 1 linha (Test 6 round-trip assertion presente).
    - **AC-2.9:** `grep -E "tmp_path: Path" tests/parser/test_markdown.py && grep -E "parser: ParserMd" tests/parser/test_markdown.py && grep -E "-> None:" tests/parser/test_markdown.py` exit 0 (type hints Phase 1 style).
    - **AC-2.10:** `uv run pytest tests/parser/ -v` exit 0 com `6 passed` no output (gate central de Task 2).
    - **AC-2.11:** `uv run pytest tests/ -v` exit 0 com `9 passed` no output (3 Phase 1 + 6 Phase 2 — Phase 1 não regrediu).
    - **AC-2.12:** Verify script imprime `PASS: Task 2 6 testes Phase 2 + 3 Phase 1 = 9 passed`.
  </acceptance_criteria>
  <done>
    `tests/parser/test_markdown.py` existe com EXATAMENTE 6 testes nomeados conforme VALIDATION.md, exercitando re-export D-22, fixture pytest canônica, write_bytes em Test 3, pytest.skip em Test 5, round-trip em Test 6, type hints em todas as assinaturas. `uv run pytest tests/parser/ -v` retorna `6 passed`. Suite total 9 passed (Phase 1 + Phase 2). Pronto para Task 3 (gates ruff/mypy/coverage).
  </done>
</task>

<task type="auto">
  <name>Task 3: Nyquist gate — coverage >= 90% no parser/, mypy strict, ruff check + format</name>
  <files>
    (sem novos arquivos — só executa ferramentas)
  </files>
  <read_first>
    - .planning/phases/02-parser-markdown/02-VALIDATION.md (§"Sampling Rate" — gate antes de `/gsd-verify-work`: full suite + coverage >= 90% + mypy strict; §"Per-Task Verification Map" 02-XX-08 (cov-fail-under=90) e 02-XX-09 (mypy src/biblio_validador/parser))
    - .planning/phases/01-bootstrap/01-PATTERNS.md §"Style enforcement" (line-length 80, ruff E/F/I/UP/B, mypy strict py313 herdado em pyproject.toml)
    - pyproject.toml (configuração `[tool.ruff]`, `[tool.mypy]`, `[tool.coverage.run]` herdada de Phase 1)
    - tests/parser/test_markdown.py (criado em Task 2 — gate principal)
    - src/biblio_validador/parser/ (módulo criado em Task 1)
  </read_first>
  <action>
    Execute em ordem. Qualquer falha aborta o gate de phase.

    **Passo 1: Coverage >= 90% no módulo parser/.**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    uv run pytest --cov=biblio_validador.parser --cov-report=term-missing --cov-fail-under=90 tests/parser/
    ```

    Esperado:
    - Exit 0
    - Output contém `6 passed`
    - Tabela de coverage exibe `parser/__init__.py`, `parser/markdown.py`, `parser/types.py`
    - Linha `TOTAL` com `Cover` >= `90%`
    - Sem `Required test coverage of 90% not reached`

    Se coverage < 90%:
    - Identificar linhas não cobertas via `--cov-report=term-missing` (terminal mostra `Missing` column).
    - Caminhos comuns não cobertos: branch `fence` em `_walk` (sem teste de bloco de código), branch `FileNotFoundError`/`UnicodeDecodeError` em `parsear` (sem teste de arquivo ausente/inválido), branch heading `nivel == 1 and not is_first_h1` em `_classificar_heading` (sem teste de h1 duplicado).
    - Decisão: se gap é < 5 linhas e cobre branches de erro/edge, ADICIONAR teste extra em `test_markdown.py` (mantendo nome distinto dos 6 canônicos) — a regra "6 testes exatos" da VALIDATION.md trava os NOMES dos 6 testes principais; testes auxiliares para coverage gap são permitidos sob convenção `test_aux_<descrição>` (ex.: `test_aux_arquivo_inexistente_propaga`, `test_aux_arquivo_vazio_retorna_lista_vazia`).
    - Re-rodar coverage até `>= 90%`.

    Se coverage gap > 10 linhas: REGRESSÃO de Task 1 (provavelmente código morto inserido por engano). Voltar para Task 1 e remover.

    **Passo 2: mypy strict no módulo parser/.**

    ```bash
    uv run mypy src/biblio_validador/parser/
    ```

    Esperado:
    - Exit 0
    - Output `Success: no issues found in 3 source files` (`__init__.py`, `markdown.py`, `types.py`)

    Falhas comuns:
    - `error: Function is missing a return type annotation` → adicionar `-> None`/`-> list[Paragrafo]`/etc
    - `error: Item "None" of "list[int] | None" has no attribute "..."` → falta `assert token.map is not None` (Pitfall 8)
    - `error: Argument 1 to "replace" has incompatible type "Paragrafo"` → `dataclasses.replace` precisa de `dataclasses` import explícito (D-16)

    **Passo 3: ruff lint + format check.**

    ```bash
    uv run ruff check .
    uv run ruff format --check .
    ```

    Esperado: ambos exit 0, zero violações.

    Falhas comuns:
    - `E501: line too long (>80)` em `markdown.py` (especialmente em `_walk` branches) → quebrar verticalmente conforme PATTERNS.md §"markdown.py" excerpts
    - `F401: 'X' imported but unused` em `parser/__init__.py` → adicionar `__all__` (já no contrato D-22)
    - `I001: Import block is unsorted` → reordenar imports nos 3 grupos (stdlib → third-party → local)
    - `UP007: Use 'X | Y' instead of 'Optional[X]'` → trocar `Optional[int]` por `int | None`
    - `B006: Do not use mutable data structures for argument defaults` → não aplicável a Phase 2 (todos defaults são `None`)

    **Passo 4: Snapshot dos 4 success criteria de Phase 2 (informacional — não bloqueante).**

    ```bash
    # Criterion 1: ParserMd retorna list[Paragrafo] com offsets corretos
    uv run python -c "
    from pathlib import Path
    from biblio_validador.parser import ParserMd
    p = ParserMd().parsear(Path('tests/parser/fixtures/eolica_first_30.md'))
    print(f'#1 OK: {len(p)} parágrafos retornados, primeiro com linha_inicio={p[0].linha_inicio}, offset_bytes={p[0].offset_bytes}')
    "

    # Criterion 2: NFC normalization
    uv run python -c "
    import tempfile, unicodedata
    from pathlib import Path
    from biblio_validador.parser import ParserMd
    nfd = 'caf' + 'e\u0301'
    with tempfile.NamedTemporaryFile(suffix='.md', delete=False, mode='wb') as f:
        f.write((nfd + '.\n').encode('utf-8'))
        tmp = Path(f.name)
    p = ParserMd().parsear(tmp)
    print(f'#2 OK: NFC presente={chr(0x00e9) in p[0].texto}, NFD ausente={chr(0x0301) not in p[0].texto}')
    "

    # Criterion 3: Footnotes separados
    uv run python -c "
    from pathlib import Path
    from biblio_validador.parser import ParserMd, TipoSecao
    p = ParserMd().parsear(Path('tests/parser/fixtures/eolica_first_30.md'))
    notas = [x for x in p if x.tipo == TipoSecao.NOTA_RODAPE]
    print(f'#3 OK: {len(notas)} notas detectadas: {[n.ref_nota for n in notas]}')
    "

    # Criterion 4: Artigo Eólica retorna parágrafos identificáveis linha a linha
    uv run python -c "
    from pathlib import Path
    from biblio_validador.parser import ParserMd, TipoSecao
    p = ParserMd().parsear(Path('tests/parser/fixtures/eolica_first_30.md'))
    print(f'#4 OK: titulo na linha {p[0].linha_inicio} ({p[0].tipo.value}); resumo heading na linha {[x.linha_inicio for x in p if x.tipo==TipoSecao.RESUMO and x.nivel_heading==2][0]}')
    "
    ```

    **Passo 5: NÃO commitar manualmente.** O orchestrator `gsd-execute-phase` faz commit ao fim do plano.
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 3.1 — Coverage gate (VALIDATION.md task 02-XX-08)
      uv run pytest --cov=biblio_validador.parser --cov-report=term-missing --cov-fail-under=90 tests/parser/ 2>&1 | tee /tmp/phase2_cov.log
      grep -qE "6 passed" /tmp/phase2_cov.log || { echo "FAIL: 6 testes não passaram em cov run"; exit 1; }
      ! grep -qF "Required test coverage of 90% not reached" /tmp/phase2_cov.log || { echo "FAIL: coverage < 90% no parser/"; exit 1; }

      # 3.2 — mypy strict no módulo parser/ (VALIDATION.md task 02-XX-09)
      uv run mypy src/biblio_validador/parser/ 2>&1 | tee /tmp/phase2_mypy.log
      grep -qE "Success: no issues found" /tmp/phase2_mypy.log || { echo "FAIL: mypy strict no parser/"; exit 1; }

      # 3.3 — mypy strict no projeto inteiro (Phase 1 + Phase 2 não regrediu)
      uv run mypy src/ 2>&1 | tee /tmp/phase2_mypy_full.log
      grep -qE "Success: no issues found" /tmp/phase2_mypy_full.log || { echo "FAIL: mypy strict no src/ inteiro"; exit 1; }

      # 3.4 — ruff lint
      uv run ruff check . 2>&1 | tee /tmp/phase2_ruff.log
      ! grep -qE "(error|warning):" /tmp/phase2_ruff.log || { echo "FAIL: ruff check encontrou erros"; cat /tmp/phase2_ruff.log; exit 1; }

      # 3.5 — ruff format check
      uv run ruff format --check . 2>&1 | tee /tmp/phase2_ruff_fmt.log
      ! grep -qE "(would be reformatted)" /tmp/phase2_ruff_fmt.log || { echo "FAIL: ruff format encontrou drift"; cat /tmp/phase2_ruff_fmt.log; exit 1; }

      # 3.6 — Snapshot 4 success criteria
      uv run python -c "
      from pathlib import Path
      from biblio_validador.parser import ParserMd, TipoSecao
      p = ParserMd().parsear(Path(\"tests/parser/fixtures/eolica_first_30.md\"))
      assert len(p) >= 5, f\"#1 FAIL: só {len(p)} parágrafos\"
      assert p[0].linha_inicio == 1, f\"#1 FAIL: primeiro parágrafo linha_inicio={p[0].linha_inicio}\"
      assert p[0].offset_bytes == 0, f\"#1 FAIL: offset_bytes={p[0].offset_bytes}\"
      assert p[0].tipo == TipoSecao.TITULO, f\"#1 FAIL: primeiro tipo={p[0].tipo}\"
      resumos = [x for x in p if x.tipo == TipoSecao.RESUMO and x.nivel_heading == 2]
      assert len(resumos) == 1, f\"#4 FAIL: {len(resumos)} headings RESUMO\"
      assert resumos[0].linha_inicio == 9, f\"#4 FAIL: RESUMO linha_inicio={resumos[0].linha_inicio}\"
      notas = [x for x in p if x.tipo == TipoSecao.NOTA_RODAPE]
      assert len(notas) == 2, f\"#3 FAIL: {len(notas)} footnotes\"
      print(\"PASS: 4 success criteria snapshot ok\")
      "

      echo PASS: Phase 2 gate completo — coverage >= 90%, mypy strict, ruff verde, 4 success criteria verificados.
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-3.1:** `uv run pytest --cov=biblio_validador.parser --cov-report=term-missing --cov-fail-under=90 tests/parser/` exit 0 com `6 passed` (VALIDATION.md task 02-XX-08).
    - **AC-3.2:** Output do comando AC-3.1 NÃO contém `Required test coverage of 90% not reached` (gate de coverage cumprido).
    - **AC-3.3:** `uv run mypy src/biblio_validador/parser/` exit 0 com `Success: no issues found in 3 source files` (VALIDATION.md task 02-XX-09).
    - **AC-3.4:** `uv run mypy src/` exit 0 com `Success: no issues found` (Phase 1 + Phase 2 — projeto inteiro mypy strict OK).
    - **AC-3.5:** `uv run ruff check .` exit 0 com zero erros/warnings (line-length 80, E/F/I/UP/B clean).
    - **AC-3.6:** `uv run ruff format --check .` exit 0 sem `would be reformatted` (formatação canônica aplicada).
    - **AC-3.7:** Snapshot Python: `ParserMd().parsear("tests/parser/fixtures/eolica_first_30.md")` retorna ≥ 5 parágrafos, primeiro com `linha_inicio=1, offset_bytes=0, tipo=TITULO` (success criterion #1).
    - **AC-3.8:** Snapshot Python: heading `## RESUMO` aparece exatamente 1× com `linha_inicio == 9` (success criterion #4 — parágrafos identificáveis linha a linha).
    - **AC-3.9:** Snapshot Python: 2 footnotes (`[^a]` + `[^b]`) detectadas como `tipo=NOTA_RODAPE` (success criterion #3 — footnotes segmentados separadamente).
    - **AC-3.10:** Test `test_normalizacao_nfc` (executado em Task 2) confirma success criterion #2 (NFC) — não re-executado em Task 3 mas registrado em `/tmp/phase2_pytest.log`.
    - **AC-3.11:** O verify script imprime `PASS: Phase 2 gate completo — coverage >= 90%, mypy strict, ruff verde, 4 success criteria verificados.`
  </acceptance_criteria>
  <done>
    Os 4 success criteria de Phase 2 (ROADMAP §"Phase 2: Parser Markdown") estão verificados via 4 ferramentas independentes (pytest + coverage + mypy + ruff) + snapshot Python ad-hoc. Cobertura ≥ 90% no módulo `parser/`. Mypy strict exit 0 nos 3 arquivos novos + 2 herdados de Phase 1. Ruff check + format limpos. Phase 2 está pronta para commit pelo orchestrator `gsd-execute-phase`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem ↔ ParserMd input | Único cruzamento: `Path.read_bytes()` lê arquivos `.md` do disco. Sem rede em runtime. Sem input de usuário direto (CLI plug-in vem em Phase 6/8). |
| ParserMd ↔ markdown-it-py + mdit-py-plugins | Bibliotecas third-party processam string em memória. Sem sandbox necessária — input é texto editorial confiável (artigos do próprio doutorado), nunca user-supplied web content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | UTF-8 decode strict | mitigate | `raw.decode("utf-8")` SEM fallback `latin-1` (D-18). Encoding inválido propaga `UnicodeDecodeError` após `logger.error` — falha rápida e auditável, evita interpretação ambígua de bytes. Verificável por test futuro (não obrigatório em Phase 2). |
| T-02-02 | Denial of Service | Pathological markdown — deep nesting / huge files | accept | `markdown-it-py` 4.0.0 é mantida pelo Google Assured OSS com fuzz testing oficial; default config não tem limite de nesting mas cada token tem `O(1)` cost. Artigos do doutorado são ≤ 30k palavras (~100KB) — três ordens de grandeza abaixo de qualquer payload DoS prático. RESEARCH.md confirmou ~19ms para parsear o artigo Eólica completo (~14.7k palavras). Aceitar — risk surface não justifica adicionar `MAX_FILE_SIZE` arbitrário em Phase 2; se vier necessidade, Phase 17 (VAL-08 benchmark) revisita. |
| T-02-03 | Tampering | Path traversal via `Path(path)` (input do orchestrator/CLI) | accept | Em Phase 2 o `parsear(path: Path)` recebe `Path` direto; não há escape ou normalização de path. Phase 2 NÃO é exposta a CLI ainda — chamadores são código interno (Phase 6 piloto, Phase 8 orchestrator). Quando o CLI for plugado (Phase 8/52), o orquestrador resolve `Path(arg).resolve()` antes de invocar o parser. Aceitar para Phase 2 com nota: revisitar em Phase 8 quando CLI conectar com user input. |
| T-02-04 | Information Disclosure | `loguru.logger.info(f"parsear: {path}")` em logs | accept | Path do arquivo `.md` aparece em logs INFO. Em ambiente single-user local (CLAUDE.md do doutorado), logs ficam em stderr do dev. Sem secrets nos paths (artigos são públicos por contrato — produção acadêmica). Aceitar. |
| T-02-05 | Denial of Service | Recursão profunda em `_walk` por markdown patológico | mitigate | `_walk` é ITERATIVO (`while i < len(tokens)`), não recursivo — sem stack overflow possível. Pitfall 7 mitigation explicita: bullet/blockquote/list NÃO são processados recursivamente, apenas seu `paragraph_open` interno é emitido na próxima iteração linear. Mitigado por design. |
| T-02-06 | Tampering | NFC drift entre source em disco e bytes processados | mitigate | D-11/D-13 + Pitfall 4 cobrem: source SEMPRE normalizado para NFC source-único antes de QUALQUER cálculo de offset. PatchAplicador (Phase 5) HERDA essa decisão (D-13 — grava sempre em NFC). Test 3 (`test_normalizacao_nfc`) + Test 6 (`test_round_trip_offset_bytes`) pinam o invariante. |
| T-02-07 | Spoofing | Heading text manipulado para classificar errado (ex.: heading "## RESUMO ATÉ AGORA" virar RESUMO) | mitigate | `_classificar_heading` aplica `_normalizar_heading` (NFKD + casefold + strip pontuação + colapsar espaços) — chave canônica. Heading "## RESUMO ATÉ AGORA" normaliza para "resumo ate agora" (não "resumo") e cai em SECAO (h2+ fallback). Mapping é exact-match no dict — sem regex difuso, sem partial match. Aceito como design correto. |
| T-02-08 | Repudiation | Falta de audit trail de quais arquivos foram parseados | mitigate | `logger.info(f"parsear: {path}")` em entry + `logger.info(f"parsear: {N} parágrafos")` em exit + `logger.info(f"source NFD detectado, convertido para NFC: {path}")` em conversão. Audit trail completo via loguru default handler (stderr). Suficiente para Phase 2 (orchestrator de Phase 8 adicionará handlers de arquivo se necessário). |

## Phase 2 Risk Posture

Phase 2 é puramente um parser standalone (zero código de runtime exposto a rede ou input externo direto). Threat surface efetivo:
- **Sem network code** (apenas filesystem read via `pathlib`)
- **Sem user input parsing direto** (CLI vem em Phase 8/52; chamador é sempre código interno)
- **Sem auth/auth surface**
- **Sem secrets handling** (`ANTHROPIC_API_KEY` é Phase 42+)
- **Sem persistência de estado** (parser determinístico, sem cache stateful)
- **Sem subprocess/exec** (apenas chamadas diretas a `markdown-it-py` e `unicodedata` em memória)

Todos os 8 threats mapeados são LOW (mitigados ou aceitos com rationale documentado). Nenhum HIGH/MEDIUM ativo. `block_on=high` config não dispara em Phase 2.

Reverificação: per CONTEXT.md `<planner_authority_limits>` da carta planning_context, "para um pure-Python file parser with no network surface, threats are limited to: malformed UTF-8 input causing crash (T-02-01), pathological markdown (T-02-02), filesystem path injection (T-02-03), recursion depth (T-02-05)" — todos cobertos acima.
</threat_model>

<verification>
## Phase-level checks (run after Task 3)

1. `uv run pytest --cov=biblio_validador.parser --cov-report=term-missing --cov-fail-under=90 tests/parser/` → 6 passed, coverage >= 90% (Nyquist gate via VALIDATION.md task 02-XX-08).
2. `uv run pytest tests/ -v` → 9 passed (3 Phase 1 + 6 Phase 2 — sem regressão).
3. `uv run mypy src/biblio_validador/parser/` → `Success: no issues found in 3 source files` (VALIDATION.md task 02-XX-09).
4. `uv run mypy src/` → `Success: no issues found` (projeto inteiro mypy strict — Phase 1 + Phase 2).
5. `uv run ruff check . && uv run ruff format --check .` → exit 0 (line-length 80, E/F/I/UP/B clean — D-23/D-24).
6. `uv run python -c "from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao; print('ok')"` → exit 0 (re-export D-22 funcional).
7. Snapshot success criteria: `ParserMd().parsear("tests/parser/fixtures/eolica_first_30.md")` retorna ≥ 5 parágrafos com `linha_inicio` correto para o título h1, heading RESUMO h2 (linha 9), parágrafo RESUMO/None (linha 11), e 2 footnotes (`[^a]`, `[^b]`).
8. `git ls-files --others --exclude-standard | wc -l` → 0 (todos os artefatos esperados estão tracked, nada untracked do tooling vazou).

## Validation Architecture compliance

- VALIDATION.md "Wave 0 Requirements" — fechados pelos 7 arquivos criados em Task 1 + Task 2.
- VALIDATION.md "Per-Task Verification Map" — cada task ID 02-XX-NN referencia AC-3.1 (cov-fail-under=90), AC-3.3 (mypy strict parser/), AC-2.10 (6 testes verdes), e os 6 nomes exatos dos testes em AC-2.3 mapeiam diretamente para 02-XX-02 a 02-XX-07.
- VALIDATION.md "Manual-Only Verifications" — inspeção visual de logs loguru DEBUG é OPT-IN (não-bloqueante, documentado em VALIDATION.md como informacional).
- CONTEXT.md D-01 a D-27 — todas as 27 decisões locked aplicadas; cada AC referencia o D-XX que pina.
</verification>

<success_criteria>
Phase 2 declarada COMPLETA quando os 4 success criteria de ROADMAP §"Phase 2: Parser Markdown" estão verificados de forma independente:

1. **`ParserMd.parsear("artigo.md")` retorna `list[Paragrafo]` com `linha_inicio`, `linha_fim`, `offset_bytes` corretos** — verificado em Task 2 (`test_paragrafo_simples_um_so_paragrafo` + `test_round_trip_offset_bytes`) + Task 3 (snapshot Python sobre fixture Eólica). Pin de Pitfalls 2 + 3.
2. **Parágrafo com acento composto (NFD) é normalizado para NFC antes de indexar** — verificado em Task 2 (`test_normalizacao_nfc` — pin direto de CORE-11 + Pitfall 4) + Task 3 (snapshot inverso `"\u0301" not in p[0].texto`).
3. **Footnotes `[^n]` são segmentados como `TipoSecao.NOTA_RODAPE` separado do corpo** — verificado em Task 2 (`test_footnote_separa_e_linka` — pin de D-14/D-15/D-16) + Task 3 (snapshot detecta 2 footnotes na fixture Eólica).
4. **Teste com trecho do artigo Eólica Nordeste retorna parágrafos identificáveis linha a linha** — verificado em Task 2 (`test_artigo_eolica_excerpt` — fixture com numeração calibrada para linhas 1, 9, 11) + Task 3 (snapshot final).

Cada criterion tem ≥ 2 signals independentes (Nyquist 2× cumprido). Adicionalmente, o property test `test_round_trip_offset_bytes` pina o invariante byte-exact que Phase 5 PatchAplicador depende — single test catches Pitfalls 2, 3, 4, 6 simultaneously.
</success_criteria>

<output>
After completion, create `.planning/phases/02-parser-markdown/02-01-SUMMARY.md` listing:
- Files created (7 new — `src/biblio_validador/parser/{__init__,types,markdown}.py`, `tests/parser/{__init__,test_markdown}.py`, `tests/parser/fixtures/{__init__.py,eolica_first_30.md}`)
- Verification commands executed and outputs (snippets das saídas relevantes — `pytest 6 passed`, `coverage TOTAL %`, `mypy Success`, `ruff check exit 0`)
- Decisions actually applied (cross-reference D-01 a D-27 — todas as 27 locked)
- Pitfalls actively mitigated (Pitfalls 1, 2, 3, 4, 5, 6, 7, 8 — referenciar mitigation aplicada por arquivo/linha)
- Carryover into Phase 3 (Dataclasses Core — `Paragrafo` é parser-internal mas `linha_inicio`/`linha_fim` shape herdado por `Violacao`; `Violacao.col_inicio`/`col_fim` calculados pelos validadores sobre `Paragrafo.texto` + `offset_bytes`)
- Carryover into Phase 5 (PatchAplicador — herda invariante byte-exact: arquivo lido/normalizado para NFC, patches sobre offsets em bytes, gravado em NFC; D-13 já decidido aqui)
- Carryover into Phase 6 (Validador piloto cst_012 — primeiro consumidor real de `list[Paragrafo]` via `from biblio_validador.parser import ParserMd, Paragrafo, TipoSecao`)
- Next phase: Phase 3 (Dataclasses Core — CORE-02 + CORE-03)
</output>
</content>
</invoke>