---
phase: 04-contratos-abc
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/biblio_validador/core/__init__.py
  - tests/core/test_contracts.py
autonomous: true
requirements:
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "core/__init__.py re-exporta 10 símbolos alfabeticamente em __all__"
    - "tests/core/test_contracts.py contém 15 testes cobrindo CORE-04 e CORE-05 (4 abstract + 6 carregar_regras [normalizes_str_and_list cobre str+list+alt_key+skip-branch num único teste] + 2 pode_corrigir + 1 frozen + 1 eq=False + 1 smoke)"
    - "Os 4 success criteria do ROADMAP Phase 4 estão cada um mapeados a ao menos 1 teste passando"
    - "Coverage de biblio_validador.core >= 95% (D-34)"
    - "mypy --strict passa em src/biblio_validador/core/"
    - "ruff check passa em src/biblio_validador/core/ e tests/core/"
  artifacts:
    - path: "src/biblio_validador/core/__init__.py"
      provides: "Re-export do pacote core (Phase 3 + Phase 4) com 10 símbolos públicos"
      contains: "ValidadorBase"
    - path: "src/biblio_validador/core/__init__.py"
      provides: "Re-export Plan 04 production"
      contains: "AplicacaoResultado"
    - path: "tests/core/test_contracts.py"
      provides: "Suite de testes CORE-04 + CORE-05 (15 testes)"
      contains: "test_validador_base_abstract_validar"
    - path: "tests/core/test_contracts.py"
      provides: "Pitfall 2 mitigation test"
      contains: "test_carregar_regras_per_subclass_isolation"
    - path: "tests/core/test_contracts.py"
      provides: "Pitfall 4 mitigation test"
      contains: "test_contexto_fixer_eq_false_unhashable_safe"
  key_links:
    - from: "tests/core/test_contracts.py"
      to: "tests/core/fixtures/*.json (Plan 01)"
      via: "FIXT = Path(__file__).parent / 'fixtures'"
      pattern: "FIXT / 'regras_"
    - from: "tests/core/test_contracts.py"
      to: "src/biblio_validador/core/contracts.py (Plan 02)"
      via: "from biblio_validador.core import ..."
      pattern: "ValidadorBase, FixerBase"
---

<objective>
Fechar Phase 4: estender `core/__init__.py` re-exportando os 6 símbolos novos
(somando 10 totais com Phase 3), criar `tests/core/test_contracts.py` cobrindo
os 4 success criteria do roadmap + 3 testes de erro (D-33) + testes para os 2
pitfalls verificados na pesquisa (per-subclass cache + eq=False), e rodar os
gates finais (mypy strict, ruff, pytest com coverage >= 95%).

**Purpose:** Tornar Phase 4 verificável de ponta a ponta. Sem este Plan, há
código sem evidência automatizada de que cumpre os 4 success criteria.

**Why third:** Depende do Plan 01 (fixtures JSON existirem em
`tests/core/fixtures/`) e do Plan 02 (contracts.py + enums extension
existirem). Plan 03 é o único Plan da Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contratos-abc/04-CONTEXT.md
@.planning/phases/04-contratos-abc/04-RESEARCH.md
@.planning/phases/04-contratos-abc/04-PATTERNS.md
@.planning/phases/04-contratos-abc/04-VALIDATION.md
@src/biblio_validador/core/__init__.py
@src/biblio_validador/core/contracts.py
@src/biblio_validador/core/dataclasses.py
@src/biblio_validador/core/enums.py
@src/biblio_validador/parser/__init__.py
@tests/core/test_dataclasses.py

<interfaces>
<!-- Após este Plan, o ponto de entrada público do core é:                          -->
<!--   from biblio_validador.core import (                                          -->
<!--       AplicacaoResultado, ContextoFixer, EstadoPatch, FixerBase, ModoFixer,    -->
<!--       Patch, Scope, Severidade, ValidadorBase, Violacao,                       -->
<!--   )                                                                            -->
<!-- Símbolos privados (`_canonical_root`, `_carregar_json_simples`) NÃO são        -->
<!-- re-exportados; consumidores futuros (M2+) importam diretamente de              -->
<!-- biblio_validador.core.contracts.                                               -->
</interfaces>
</context>

<threat_model>
**Phase 4 ASVS L1 — risco do Plan 03 (testes + re-exports):**

- T-04-01 (LOW) — Test fixtures sob `tests/core/fixtures/` são read-only
  versionadas no repo. Sem superfície adicional.
- T-04-02 (LOW) — N/A para testes (regex DoS é risco de validators concretos
  M2+, não da ABC nem dos testes de contrato).

Plan 03 só toca `__init__.py` (re-export, sem lógica) e arquivo de testes.
Sem user input, sem network, sem file write em produção.
</threat_model>

<tasks>

<task id="T-04-03-01">
<title>Estender core/__init__.py re-exportando 10 símbolos alfabeticamente</title>

<read_first>
- src/biblio_validador/core/__init__.py (estado atual: 4 símbolos da Phase 3)
- src/biblio_validador/parser/__init__.py (analog: padrão re-export)
- .planning/phases/04-contratos-abc/04-CONTEXT.md (D-03)
- .planning/phases/04-contratos-abc/04-PATTERNS.md (seção __init__.py)
</read_first>

<action>
Substituir o conteúdo de `src/biblio_validador/core/__init__.py` pelo bloco abaixo
(re-export estendido com símbolos Phase 4 + preservação dos da Phase 3). Conteúdo
exato:

```python
"""Pacote core — tipos, contratos e utilitarios compartilhados.

Phase 3 (Dataclasses Core): Violacao, Patch, EstadoPatch, Severidade.
Phase 4 (Contratos ABC): ValidadorBase, FixerBase, AplicacaoResultado,
ContextoFixer, Scope, ModoFixer.
"""

from biblio_validador.core.contracts import (
    AplicacaoResultado,
    ContextoFixer,
    FixerBase,
    ValidadorBase,
)
from biblio_validador.core.dataclasses import Patch, Violacao
from biblio_validador.core.enums import (
    EstadoPatch,
    ModoFixer,
    Scope,
    Severidade,
)

__all__ = [
    "AplicacaoResultado",
    "ContextoFixer",
    "EstadoPatch",
    "FixerBase",
    "ModoFixer",
    "Patch",
    "Scope",
    "Severidade",
    "ValidadorBase",
    "Violacao",
]
```

Convencoes preservadas (PATTERNS.md):
- One-line PT-BR module docstring + linha em branco + lista de exports por phase
- Absolute imports (nunca relativos)
- `__all__` em ordem alfabética (10 símbolos)
- Sem `__version__`, sem side-effect, sem logging
</action>

<acceptance_criteria>
- `wc -l src/biblio_validador/core/__init__.py` retorna ao menos 25 (10 nomes + imports + docstring)
- `grep -c '"AplicacaoResultado"' src/biblio_validador/core/__init__.py` retorna `1`
- `grep -c '"ContextoFixer"' src/biblio_validador/core/__init__.py` retorna `1`
- `grep -c '"FixerBase"' src/biblio_validador/core/__init__.py` retorna `1`
- `grep -c '"ValidadorBase"' src/biblio_validador/core/__init__.py` retorna `1`
- `grep -c '"ModoFixer"' src/biblio_validador/core/__init__.py` retorna `1`
- `grep -c '"Scope"' src/biblio_validador/core/__init__.py` retorna `1`
- Phase 3 preservados: `grep -c '"EstadoPatch"\|"Patch"\|"Severidade"\|"Violacao"' src/biblio_validador/core/__init__.py` retorna `4`
- `uv run python -c "from biblio_validador.core import AplicacaoResultado, ContextoFixer, EstadoPatch, FixerBase, ModoFixer, Patch, Scope, Severidade, ValidadorBase, Violacao; print('all 10 OK')"` imprime `all 10 OK`
- `uv run mypy --strict src/biblio_validador/core/__init__.py` exits 0
- `uv run ruff check src/biblio_validador/core/__init__.py` exits 0
</acceptance_criteria>

<verification>
```bash
uv run python -c "
import biblio_validador.core as core
expected = {
    'AplicacaoResultado', 'ContextoFixer', 'EstadoPatch', 'FixerBase',
    'ModoFixer', 'Patch', 'Scope', 'Severidade', 'ValidadorBase', 'Violacao',
}
got = set(core.__all__)
assert got == expected, f'mismatch: missing={expected-got}, extra={got-expected}'
print('all_match OK')
"
uv run mypy --strict src/biblio_validador/core/__init__.py
uv run ruff check src/biblio_validador/core/__init__.py
```
Todos exit 0; o primeiro imprime `all_match OK`.
</verification>

<success_criteria>
- `__all__` tem exatamente 10 strings, ordem alfabética
- Todos os 10 símbolos importáveis a partir de `biblio_validador.core`
- mypy strict + ruff passam
</success_criteria>
</task>

<task id="T-04-03-02">
<title>Criar tests/core/test_contracts.py — header, fakes, 4 testes de contrato (success #1)</title>

<read_first>
- tests/core/test_dataclasses.py (analog: imports, helpers, fixtures)
- src/biblio_validador/core/contracts.py (estado pós-Plan 02)
- .planning/phases/04-contratos-abc/04-CONTEXT.md (D-31..D-33)
- .planning/phases/04-contratos-abc/04-RESEARCH.md (Pattern 8 TypeError test)
- .planning/phases/04-contratos-abc/04-VALIDATION.md (test name list)
</read_first>

<action>
Criar arquivo NOVO `tests/core/test_contracts.py` com header, imports, helpers de
construção mínima de Violacao/Paragrafo, e os primeiros 4 testes (success
criterion #1 do roadmap). Conteúdo exato:

```python
"""Testes para core/contracts.py — CORE-04 (ValidadorBase) + CORE-05 (FixerBase).

Cobertura mapeada aos 4 success criteria do roadmap Phase 4:
    #1: TypeError em instanciacao de subclasse incompleta — testes
        test_validador_base_abstract_* + test_fixer_base_abstract_*
    #2: carregar_regras() compila regex 1x — test_carregar_regras_cache_hit
    #3: pode_corrigir(v) retorna False fora de VIOLACAO_IDS —
        test_pode_corrigir_*
    #4: testes unitarios passam — esta suite e a evidencia

Convencoes herdadas (Phase 3 D-26):
    - Helper de construcao com **overrides para reduzir duplicacao
    - test_aux_<descricao> autorizado para coverage gap fill
"""

from __future__ import annotations

import json
import re
from collections.abc import Mapping
from dataclasses import FrozenInstanceError
from pathlib import Path
from typing import ClassVar

import pytest

from biblio_validador.core import (
    AplicacaoResultado,
    ContextoFixer,
    EstadoPatch,
    FixerBase,
    ModoFixer,
    Patch,
    Scope,
    Severidade,
    ValidadorBase,
    Violacao,
)
from biblio_validador.core.contracts import _carregar_json_simples
from biblio_validador.parser.types import Paragrafo, TipoSecao

FIXT = Path(__file__).parent / "fixtures"


def _make_violacao(**overrides: object) -> Violacao:
    """Builder minimo de Violacao para testes (Phase 3 convention)."""
    base: dict[str, object] = {
        "arquivo": Path("a.md"),
        "linha_inicio": 1,
        "linha_fim": 1,
        "col_inicio": 1,
        "col_fim": 5,
        "trecho_violador": "abcd",
        "regra_id": "cst_999",
        "regra_nome": "fake",
        "severidade": Severidade.ERRO,
    }
    base.update(overrides)
    return Violacao(**base)  # type: ignore[arg-type]


def _make_paragrafo() -> Paragrafo:
    """Builder minimo de Paragrafo para ContextoFixer.

    Campos seguem `src/biblio_validador/parser/types.py` (Phase 2):
        arquivo, indice, tipo, nivel_heading, texto, linha_inicio,
        linha_fim, offset_bytes, len_bytes, ref_nota, paragrafo_pai_idx
    """
    return Paragrafo(
        arquivo=Path("a.md"),
        indice=0,
        tipo=TipoSecao.CORPO,
        nivel_heading=None,
        texto="abcd",
        linha_inicio=1,
        linha_fim=1,
        offset_bytes=0,
        len_bytes=4,
        ref_nota=None,
        paragrafo_pai_idx=None,
    )


# ============================================================================
# Success criterion #1: TypeError em instanciacao de subclasse incompleta
# ============================================================================


def test_validador_base_abstract_validar() -> None:
    """Subclasse omitindo validar() deve levantar TypeError em ()."""

    class _Incomplete(ValidadorBase):
        JSON_SOURCE = FIXT / "regras_fake.json"
        SCOPE = Scope.PARAGRAFO

        @classmethod
        def carregar_regras(cls) -> Mapping[str, re.Pattern[str]]:
            return _carregar_json_simples(cls.JSON_SOURCE)

    with pytest.raises(TypeError, match="abstract"):
        _Incomplete()  # type: ignore[abstract]


def test_validador_base_abstract_carregar_regras() -> None:
    """Subclasse omitindo carregar_regras() deve levantar TypeError."""

    class _Incomplete(ValidadorBase):
        JSON_SOURCE = FIXT / "regras_fake.json"
        SCOPE = Scope.PARAGRAFO

        def validar(self, paragrafos: list[Paragrafo]) -> list[Violacao]:
            return []

    with pytest.raises(TypeError, match="abstract"):
        _Incomplete()  # type: ignore[abstract]


def test_fixer_base_abstract_propor_patches() -> None:
    """Subclasse de FixerBase omitindo propor_patches deve levantar TypeError."""

    class _Incomplete(FixerBase):
        VIOLACAO_IDS = frozenset({"cst_999"})
        MODO = ModoFixer.AUTO

        def aplicar(
            self,
            patches: list[Patch],
            modo_interativo: bool,
        ) -> AplicacaoResultado:
            return AplicacaoResultado((), (), (), 0)

    with pytest.raises(TypeError, match="abstract"):
        _Incomplete()  # type: ignore[abstract]


def test_fixer_base_abstract_aplicar() -> None:
    """Subclasse de FixerBase omitindo aplicar deve levantar TypeError."""

    class _Incomplete(FixerBase):
        VIOLACAO_IDS = frozenset({"cst_999"})
        MODO = ModoFixer.AUTO

        def propor_patches(
            self,
            v: Violacao,
            contexto: ContextoFixer,
        ) -> list[Patch]:
            return []

    with pytest.raises(TypeError, match="abstract"):
        _Incomplete()  # type: ignore[abstract]
```

NOTA: estes 4 testes mapeam diretamente ao success criterion #1 do roadmap.
Tasks T-04-03-03 e T-04-03-04 anexam os demais.
</action>

<acceptance_criteria>
- `tests/core/test_contracts.py` existe
- `grep -c "from biblio_validador.core import" tests/core/test_contracts.py` retorna `1`
- `grep -c "FIXT = Path" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def _make_violacao" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def _make_paragrafo" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_validador_base_abstract_validar" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_validador_base_abstract_carregar_regras" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_fixer_base_abstract_propor_patches" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_fixer_base_abstract_aplicar" tests/core/test_contracts.py` retorna `1`
- `grep -c 'with pytest.raises(TypeError, match="abstract")' tests/core/test_contracts.py` retorna `4`
- `uv run pytest tests/core/test_contracts.py -k "abstract" -x` passa (4 testes)
</acceptance_criteria>

<verification>
```bash
uv run pytest tests/core/test_contracts.py -k "abstract" -x -v
uv run mypy --strict tests/core/test_contracts.py 2>&1 | grep -v "^Success" || true
uv run ruff check tests/core/test_contracts.py
```
Os 4 testes que casam `-k abstract` devem passar. mypy/ruff exit 0.
</verification>

<success_criteria>
- 4 testes de contract violation passam (success criterion #1)
- Helpers `_make_violacao` e `_make_paragrafo` reusáveis pelas próximas tasks
- mypy strict + ruff passam
</success_criteria>
</task>

<task id="T-04-03-03">
<title>Anexar testes de carregar_regras: cache hit, per-subclass isolation, 3 erros, schema heterogêneo</title>

<read_first>
- tests/core/test_contracts.py (estado pós-T-04-03-02)
- tests/core/fixtures/ (Plan 01: 4 fixtures JSON)
- .planning/phases/04-contratos-abc/04-CONTEXT.md (D-19..D-22 D-31..D-33)
- .planning/phases/04-contratos-abc/04-RESEARCH.md (Pattern 4 cache via cls.__dict__, Pattern 5 schema normalization, Pitfall 2)
</read_first>

<action>
Append ao final de `tests/core/test_contracts.py` o seguinte bloco de testes
(success criterion #2 + cobertura de erros D-33 + edge case schema heterogêneo).
Conteúdo exato:

```python


# ============================================================================
# Success criterion #2: carregar_regras() compila regex 1x (cache class-level)
# ============================================================================


class _ValidadorComCache(ValidadorBase):
    """Subclasse concreta usando o pattern cache via cls.__dict__.get."""

    JSON_SOURCE: ClassVar[Path] = FIXT / "regras_fake.json"
    SCOPE: ClassVar[Scope] = Scope.PARAGRAFO

    @classmethod
    def carregar_regras(cls) -> Mapping[str, re.Pattern[str]]:
        cached = cls.__dict__.get("_regras_compiladas")
        if cached is not None:
            return cached  # type: ignore[no-any-return]
        result = _carregar_json_simples(cls.JSON_SOURCE)
        cls._regras_compiladas = result
        return result

    def validar(self, paragrafos: list[Paragrafo]) -> list[Violacao]:
        return []


class _ValidadorSubA(_ValidadorComCache):
    """Cache per-subclass — JSON_SOURCE_A."""

    JSON_SOURCE: ClassVar[Path] = FIXT / "regras_fake.json"


class _ValidadorSubB(_ValidadorComCache):
    """Cache per-subclass — JSON_SOURCE_B (str_e_lista)."""

    JSON_SOURCE: ClassVar[Path] = FIXT / "regras_str_e_lista.json"


def test_carregar_regras_cache_hit() -> None:
    """Chamar carregar_regras() 2x retorna o mesmo objeto (id() identico)."""
    # Limpar cache de outros testes
    _ValidadorComCache._regras_compiladas = None
    r1 = _ValidadorComCache.carregar_regras()
    r2 = _ValidadorComCache.carregar_regras()
    assert id(r1) == id(r2), "cache miss — recompilou"


def test_carregar_regras_per_subclass_isolation() -> None:
    """Subclasses A e B com JSON_SOURCE distintos NAO compartilham cache.

    RESEARCH Pitfall 2: getattr(cls, '_x') traversaria MRO; cls.__dict__.get
    e' per-subclass.
    """
    # Limpar caches herdados
    for kls in (_ValidadorComCache, _ValidadorSubA, _ValidadorSubB):
        kls._regras_compiladas = None
    rA = _ValidadorSubA.carregar_regras()
    rB = _ValidadorSubB.carregar_regras()
    assert id(rA) != id(rB), "Sub A e B compartilharam cache (Pitfall 2)"
    # rA tem cst_999 (regras_fake); rB tem cst_str/cst_lista/cst_alt_key
    assert "cst_999" in rA
    assert {"cst_str", "cst_lista", "cst_alt_key"}.issubset(rB.keys())


# ============================================================================
# Cobertura de erros (D-22, D-33)
# ============================================================================


def test_carregar_regras_file_not_found() -> None:
    """JSON_SOURCE inexistente propaga FileNotFoundError."""
    with pytest.raises(FileNotFoundError):
        _carregar_json_simples(Path("/does/not/exist.json"))


def test_carregar_regras_json_decode_error() -> None:
    """JSON malformado propaga json.JSONDecodeError."""
    with pytest.raises(json.JSONDecodeError):
        _carregar_json_simples(FIXT / "regras_malformadas.json")


def test_carregar_regras_regex_error() -> None:
    """Regex invalido propaga re.error com regra_id no message."""
    with pytest.raises(re.error, match="cst_invalido"):
        _carregar_json_simples(FIXT / "regras_regex_invalido.json")


def test_carregar_regras_normalizes_str_and_list() -> None:
    """RESEARCH Pattern 5 + skip-branch: aceita str, list, chave alternativa,
    e PULA entradas sem regex (espelha cst_005 com so exemplos_proibidos).

    Coverage do branch `if raw is None: continue` em _carregar_json_simples.
    """
    result = _carregar_json_simples(FIXT / "regras_str_e_lista.json")
    # cst_str, cst_lista, cst_alt_key compilam; cst_skip e' pulado
    assert set(result.keys()) == {"cst_str", "cst_lista", "cst_alt_key"}
    assert "cst_skip" not in result, "entrada sem regex deveria ser pulada"
    # Cada Pattern e' um re.Pattern compilado
    for rid, pattern in result.items():
        assert isinstance(pattern, re.Pattern), f"{rid} nao e' Pattern"
```

Notas:
- 6 testes nesta task: 1 cache_hit + 1 per_subclass_isolation + 1 file_not_found
  + 1 json_decode_error + 1 regex_error + 1 normalizes_str_and_list
  (este último cobre str + list[str] + chave alternativa + branch `continue`/skip
  num único teste).
- Combinados aos 4 da task anterior (4 abstract), total acumulado = 10.
- O test_carregar_regras_normalizes_str_and_list cobre AGORA tambem o branch
  `continue` (cst_skip) — fecha o gap de coverage 95% identificado pelo plan-checker.
</action>

<acceptance_criteria>
- `grep -c "^class _ValidadorComCache" tests/core/test_contracts.py` retorna `1`
- `grep -c "^class _ValidadorSubA" tests/core/test_contracts.py` retorna `1`
- `grep -c "^class _ValidadorSubB" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_cache_hit" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_per_subclass_isolation" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_file_not_found" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_json_decode_error" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_regex_error" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_carregar_regras_normalizes_str_and_list" tests/core/test_contracts.py` retorna `1`
- `grep -c "cls.__dict__.get" tests/core/test_contracts.py` retorna `1` (no helper de cache)
- `uv run pytest tests/core/test_contracts.py -k "carregar_regras" -x` passa (6 testes)
- `uv run pytest tests/core/test_contracts.py -x` passa (10 testes acumulados após T-04-03-03)
</acceptance_criteria>

<verification>
```bash
uv run pytest tests/core/test_contracts.py -k "carregar_regras" -x -v
uv run pytest tests/core/test_contracts.py -x -v
```
Os 6 testes que casam `-k carregar_regras` devem passar. Suite acumulada após esta task: 10 testes.
</verification>

<success_criteria>
- Cache class-level via `cls.__dict__.get` validado em 2 dimensoes (hit + isolation)
- 3 caminhos de erro cobertos (FileNotFoundError, JSONDecodeError, re.error)
- Schema heterogêneo + skip-branch (Pattern 5) validado com fixture `regras_str_e_lista.json`
- 10 testes acumulados (4 abstract + 6 carregar_regras)
</success_criteria>
</task>

<task id="T-04-03-04">
<title>Anexar testes pode_corrigir, AplicacaoResultado frozen, ContextoFixer eq=False</title>

<read_first>
- tests/core/test_contracts.py (estado pós-T-04-03-03)
- src/biblio_validador/core/contracts.py (Pitfall 4 mitigation: ContextoFixer eq=False)
- .planning/phases/04-contratos-abc/04-CONTEXT.md (D-23 D-25 D-26)
- .planning/phases/04-contratos-abc/04-RESEARCH.md (Pitfall 4 detalhado)
</read_first>

<action>
Append ao final de `tests/core/test_contracts.py` o seguinte bloco. Conteúdo exato:

```python


# ============================================================================
# Success criterion #3: pode_corrigir retorna False fora de VIOLACAO_IDS
# ============================================================================


class _FixerCompleto(FixerBase):
    """Subclasse concreta com VIOLACAO_IDS limitado a {cst_999}."""

    VIOLACAO_IDS: ClassVar[frozenset[str]] = frozenset({"cst_999"})
    MODO: ClassVar[ModoFixer] = ModoFixer.AUTO

    def propor_patches(
        self,
        v: Violacao,
        contexto: ContextoFixer,
    ) -> list[Patch]:
        return []

    def aplicar(
        self,
        patches: list[Patch],
        modo_interativo: bool,
    ) -> AplicacaoResultado:
        return AplicacaoResultado((), (), (), 0)


def test_pode_corrigir_returns_true_for_known_id() -> None:
    """v.regra_id em VIOLACAO_IDS -> True."""
    fixer = _FixerCompleto()
    v = _make_violacao(regra_id="cst_999")
    assert fixer.pode_corrigir(v) is True


def test_pode_corrigir_returns_false_for_unknown_id() -> None:
    """v.regra_id fora de VIOLACAO_IDS -> False (success criterion #3)."""
    fixer = _FixerCompleto()
    v = _make_violacao(regra_id="cst_001")
    assert fixer.pode_corrigir(v) is False


# ============================================================================
# Dataclasses auxiliares: AplicacaoResultado (frozen+slots) + ContextoFixer (eq=False)
# ============================================================================


def test_aplicacao_resultado_frozen_slots() -> None:
    """AplicacaoResultado e' frozen+slots — reatribuir levanta FrozenInstanceError."""
    ar = AplicacaoResultado(
        patches_aceitos=(),
        patches_rejeitados=(),
        patches_suprimidos=(),
        bytes_modificados=42,
    )
    assert ar.bytes_modificados == 42
    assert hasattr(AplicacaoResultado, "__slots__")
    with pytest.raises(FrozenInstanceError):
        ar.bytes_modificados = 0  # type: ignore[misc]


def test_contexto_fixer_eq_false_unhashable_safe() -> None:
    """ContextoFixer com eq=False e' instanciavel mesmo com Mapping field.

    RESEARCH Pitfall 4: sem eq=False, frozen+slots+dict-field gera __hash__
    auto que tenta hashear Mapping (dict) -> TypeError em hash() ou em
    qualquer uso como key. Com eq=False:
        - instancia normalmente
        - ContextoFixer.__hash__ e' herdado de object (id-based) OU None
        - eq usa identidade (id())
    """
    paragrafo = _make_paragrafo()
    pat = re.compile(r"\bfake\b")
    ctx = ContextoFixer(
        paragrafo=paragrafo,
        todas_violacoes_paragrafo=(),
        regras_compiladas={"cst_999": pat},
    )
    # Instanciacao bem-sucedida e' a evidencia primaria de Pitfall 4 mitigado
    assert ctx.paragrafo == paragrafo
    assert ctx.regras_compiladas["cst_999"] is pat
    # Nao acessamos hash(ctx) — eq=False com frozen=True pode ainda dar
    # __hash__ herdado de object (id-based) que NAO levanta TypeError.
    # O contrato e': eq=False evita que dataclass tente sintetizar __eq__/__hash__
    # baseado em fields (que falharia com Mapping).


# ============================================================================
# Smoke test: re-export do core/__init__.py
# ============================================================================


def test_core_reexport_smoke() -> None:
    """Todos os 10 simbolos da Phase 3 + Phase 4 estao em biblio_validador.core."""
    import biblio_validador.core as core_mod

    expected = {
        "AplicacaoResultado",
        "ContextoFixer",
        "EstadoPatch",
        "FixerBase",
        "ModoFixer",
        "Patch",
        "Scope",
        "Severidade",
        "ValidadorBase",
        "Violacao",
    }
    assert set(core_mod.__all__) == expected
```

Notas:
- 5 testes nesta task: 2 pode_corrigir + 1 frozen_slots + 1 eq=False + 1 smoke.
- Combinados ao Total acumulado (10 da T-04-03-03): 15 testes.
</action>

<acceptance_criteria>
- `grep -c "^class _FixerCompleto" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_pode_corrigir_returns_true_for_known_id" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_pode_corrigir_returns_false_for_unknown_id" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_aplicacao_resultado_frozen_slots" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_contexto_fixer_eq_false_unhashable_safe" tests/core/test_contracts.py` retorna `1`
- `grep -c "^def test_core_reexport_smoke" tests/core/test_contracts.py` retorna `1`
- `uv run pytest tests/core/test_contracts.py -x` passa todos os 15 testes acumulados
- mypy strict passa em `tests/core/test_contracts.py`
- ruff passa em `tests/core/test_contracts.py`
</acceptance_criteria>

<verification>
```bash
uv run pytest tests/core/test_contracts.py -x -v
uv run pytest tests/core/test_contracts.py --collect-only -q | tail -5
uv run mypy --strict tests/core/test_contracts.py 2>&1 | tail -3
uv run ruff check tests/core/test_contracts.py
```
Suite passa com 15 testes; mypy/ruff exit 0.
</verification>

<success_criteria>
- pode_corrigir testado em ambas direções (known id -> True, unknown -> False)
- AplicacaoResultado frozen+slots verificado via FrozenInstanceError
- ContextoFixer eq=False instancia com Mapping field (Pitfall 4 mitigation evidence)
- Smoke test do core/__init__.py confirma 10 símbolos re-exportados
</success_criteria>
</task>

<task id="T-04-03-05">
<title>Final gates: mypy strict + ruff + coverage >= 95%</title>

<read_first>
- pyproject.toml (config de pytest e coverage)
- .planning/phases/04-contratos-abc/04-VALIDATION.md (Sign-Off section)
- src/biblio_validador/core/ (todo o pacote — verificar consistência)
- tests/core/ (todo o subpacote)
</read_first>

<action>
Executar as 4 verificações finais de Phase 4 e confirmar exit 0 em cada.
Estes comandos formam o gate de aceitação do roadmap success criterion #4
(testes unitários dos ABCs passam + coverage 95%).

Comandos a rodar EXATAMENTE como listados:

```bash
# 1. mypy strict em todo o pacote core
uv run mypy --strict src/biblio_validador/core/

# 2. ruff em src e tests do core
uv run ruff check src/biblio_validador/core/ tests/core/

# 3. Suite completa de testes (sem coverage ainda — apenas correção)
uv run pytest tests/core/ -x -v

# 4. Coverage do core com gate >= 95%
uv run pytest tests/core/ \
    --cov=biblio_validador.core \
    --cov-report=term-missing \
    --cov-fail-under=95
```

Se qualquer comando falhar, NÃO commitar. Investigar:
- mypy fail: provavelmente type hint faltando ou ClassVar esquecido
- ruff fail: provavelmente line >80 chars ou import desordenado
- pytest fail: provavelmente ordem de testes (tests of cache state share class state) — adicionar pytest fixture autouse para limpar `_regras_compiladas`
- coverage fail: provavelmente branch heterogeneous schema (skip de entrada sem regex) não exercitado — confirmar que fixture `regras_str_e_lista.json` tem entrada `cst_skip` (Plan 01)

5. Após confirmar todos os 4 gates verdes, atualizar VALIDATION.md frontmatter
   para refletir conclusão. Editar `.planning/phases/04-contratos-abc/04-VALIDATION.md`:
   - `status: draft` -> `status: green`
   - `nyquist_compliant: false` -> `nyquist_compliant: true`
   - `wave_0_complete: false` -> `wave_0_complete: true`
   - linha `**Approval:** pending` -> `**Approval:** approved 2026-05-05`
   Não tocar nas linhas da tabela de Per-Task Verification Map (status `⬜ pending`
   é atualizado pelo orchestrator de Phase 4 verify, não por este plan).

Ao confirmar todos os 5 itens (4 gates + frontmatter flip), este task é completo.
</action>

<acceptance_criteria>
- `uv run mypy --strict src/biblio_validador/core/` exits 0
- `uv run ruff check src/biblio_validador/core/ tests/core/` exits 0
- `uv run pytest tests/core/ -x` exits 0
- `uv run pytest tests/core/ --cov=biblio_validador.core --cov-fail-under=95` exits 0
- `uv run pytest tests/core/ --collect-only -q | tail -1` mostra exatamente 15 testes em `test_contracts.py` (4 abstract + 6 carregar_regras + 2 pode_corrigir + 1 frozen + 1 eq=False + 1 smoke)
- `uv run pytest tests/core/test_dataclasses.py -x` passa (Phase 3 sem regressão)
- VALIDATION.md frontmatter: `grep -c "^nyquist_compliant: true$" .planning/phases/04-contratos-abc/04-VALIDATION.md` retorna `1`
- VALIDATION.md frontmatter: `grep -c "^status: green$" .planning/phases/04-contratos-abc/04-VALIDATION.md` retorna `1`
</acceptance_criteria>

<verification>
```bash
echo "=== mypy strict ==="
uv run mypy --strict src/biblio_validador/core/

echo "=== ruff ==="
uv run ruff check src/biblio_validador/core/ tests/core/

echo "=== full suite ==="
uv run pytest tests/core/ -v

echo "=== coverage gate ==="
uv run pytest tests/core/ --cov=biblio_validador.core --cov-report=term-missing --cov-fail-under=95
```
Todos os 4 blocos exit 0.
</verification>

<success_criteria>
- 4 gates passam (mypy strict, ruff, pytest, coverage 95%)
- Coverage de `biblio_validador.core` >= 95%
- Zero regressão em Phase 3 (test_dataclasses.py ainda passa)
- Phase 4 success criterion #4 do roadmap satisfeito com evidência automatizada
</success_criteria>
</task>

</tasks>

<verification>
Após todas as 5 tasks deste plan, Phase 4 está completa:

```bash
# 1. Imports completos do core
uv run python -c "
from biblio_validador.core import (
    AplicacaoResultado, ContextoFixer, EstadoPatch, FixerBase,
    ModoFixer, Patch, Scope, Severidade, ValidadorBase, Violacao,
)
print('all 10 imports OK')
"

# 2. Suite full do core
uv run pytest tests/core/ -v

# 3. Coverage gate
uv run pytest tests/core/ --cov=biblio_validador.core --cov-fail-under=95

# 4. Mypy strict
uv run mypy --strict src/biblio_validador/core/

# 5. Ruff
uv run ruff check src/biblio_validador/core/ tests/core/
```

Todos exit 0. CORE-04 e CORE-05 satisfeitos com evidência testada.
</verification>

<success_criteria>
- core/__init__.py re-exporta exatamente 10 símbolos alfabeticamente
- tests/core/test_contracts.py contém 15 testes passando
- 4 success criteria do ROADMAP Phase 4 satisfeitos com evidência testada
- Coverage de core/ >= 95% (D-34)
- mypy strict + ruff passam em todo o pacote
- Phase 3 sem regressão
- Plano agora pronto para Phase 5 (PatchAplicador), que consumirá `Patch` e
  implementará a semântica que `FixerBase.aplicar()` declara
</success_criteria>

<output>
After completion, create `.planning/phases/04-contratos-abc/04-03-SUMMARY.md` com:
- Confirmação de re-export de 10 símbolos em core/__init__.py
- Total de testes em test_contracts.py (15) e mapeamento aos 4 success criteria
- Coverage final de biblio_validador.core (deve ser >= 95%)
- Tempo de execução total da suite (esperado < 5s)
- Note that Phase 4 está completa e Phase 5 (PatchAplicador) pode começar
</output>
