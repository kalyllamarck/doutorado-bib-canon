---
phase: 04-contratos-abc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/core/fixtures/regras_fake.json
  - tests/core/fixtures/regras_malformadas.json
  - tests/core/fixtures/regras_regex_invalido.json
  - tests/core/fixtures/regras_str_e_lista.json
  - tests/core/fixtures/__init__.py
autonomous: true
requirements:
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "Diretório tests/core/fixtures/ existe com 4 fixtures JSON e __init__.py vazio"
    - "regras_fake.json é JSON válido contendo entrada cst_999 com regex_deteccao str"
    - "regras_malformadas.json contém JSON sintaticamente inválido capaz de gerar json.JSONDecodeError"
    - "regras_regex_invalido.json contém entrada cst_invalido com regex que dispara re.error em re.compile"
    - "regras_str_e_lista.json cobre as três variações de schema heterogêneo (str, list[str], chave alternativa regex_deteccao_aproximada)"
  artifacts:
    - path: "tests/core/fixtures/regras_fake.json"
      provides: "Fixture happy-path para _carregar_json_simples (D-32, RESEARCH Pattern 9)"
      contains: '"cst_999"'
    - path: "tests/core/fixtures/regras_malformadas.json"
      provides: "Fixture que dispara json.JSONDecodeError em _carregar_json_simples (D-33)"
      contains: '{ "entradas": ['
    - path: "tests/core/fixtures/regras_regex_invalido.json"
      provides: "Fixture que dispara re.error em _carregar_json_simples (D-33)"
      contains: '"cst_invalido"'
    - path: "tests/core/fixtures/regras_str_e_lista.json"
      provides: "Fixture que exercita o branch heterogeneous-schema (RESEARCH Pattern 9)"
      contains: '"cst_lista"'
    - path: "tests/core/fixtures/__init__.py"
      provides: "Marker vazio espelhando tests/parser/fixtures/__init__.py"
      min_lines: 0
  key_links:
    - from: "tests/core/test_contracts.py (Plan 03)"
      to: "tests/core/fixtures/*.json"
      via: "FIXT = Path(__file__).parent / 'fixtures'"
      pattern: "FIXT / \"regras_"
---

<objective>
Criar a base de fixtures JSON que `tests/core/test_contracts.py` (Plan 03) usará para
exercitar `_carregar_json_simples` em quatro caminhos: happy path, JSON malformado,
regex inválido e schema heterogêneo (str / list[str] / chave alternativa).

**Purpose:** Plans 02 e 03 dependem destes arquivos para que os tests de erro (D-33)
e o test do branch de normalização (RESEARCH Pattern 9) sejam executáveis. Sem estas
fixtures, a coverage do branch heterogeneous-schema cai abaixo de 95% (D-34) e o
contrato de propagação de erros (D-22) fica sem evidência automatizada.

**Output:**
- `tests/core/fixtures/regras_fake.json` (1 entrada cst_999, regex str)
- `tests/core/fixtures/regras_malformadas.json` (truncado, dispara JSONDecodeError)
- `tests/core/fixtures/regras_regex_invalido.json` (entrada cst_invalido, regex `(?P<unfinished`)
- `tests/core/fixtures/regras_str_e_lista.json` (3 entradas cobrindo str, list[str], chave alternativa)
- `tests/core/fixtures/__init__.py` (vazio, marker espelhando `tests/parser/fixtures/__init__.py`)

**Why first:** Estas fixtures são puro JSON estático sem dependência Python. Podem
ser criadas em paralelo com Plan 02 (production code) — file_modified disjuntos.
Plan 03 (testes + gates) depende de ambos para escrever asserts contra fixtures
que sabidamente existem no disco.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contratos-abc/04-CONTEXT.md
@.planning/phases/04-contratos-abc/04-RESEARCH.md
@.planning/phases/04-contratos-abc/04-PATTERNS.md
@.planning/phases/04-contratos-abc/04-VALIDATION.md
@tests/parser/fixtures/eolica_first_30.md
@tests/parser/fixtures/__init__.py

<interfaces>
<!-- Schema esperado pelo helper _carregar_json_simples (Plan 02 production). -->
<!-- Verificado empiricamente contra 02_escrita/termos_proibidos/04_construcoes_sintaticas_proibidas.json -->

Schema canônico aceito por _carregar_json_simples (RESEARCH Pattern 5/9):
```json
{
  "tipo": "<descritor>",
  "entradas": [
    {
      "id": "<rid>",
      "regex_deteccao": "<str>" | ["<str>", ...]
    },
    {
      "id": "<rid>",
      "regex_deteccao_aproximada": ["<str>", ...]
    }
  ]
}
```

Helper signature alvo (Plan 02 cria, Plan 03 testa):
```python
def _carregar_json_simples(
    json_source: Path,
) -> Mapping[str, re.Pattern[str]]: ...
# Raises: FileNotFoundError, json.JSONDecodeError, re.error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tests/core/fixtures/ com __init__.py vazio + fixture happy-path regras_fake.json</name>
  <files>
    tests/core/fixtures/__init__.py
    tests/core/fixtures/regras_fake.json
  </files>
  <read_first>
    - .planning/phases/04-contratos-abc/04-CONTEXT.md (D-32: fixture minúscula em tests/core/fixtures/)
    - .planning/phases/04-contratos-abc/04-PATTERNS.md (§"Test fixtures tests/core/fixtures/*.json")
    - .planning/phases/04-contratos-abc/04-RESEARCH.md (Pattern 9 — schema verificado contra 04_construcoes_sintaticas_proibidas.json)
    - tests/parser/fixtures/__init__.py (precedente: marker vazio 0 bytes)
    - tests/parser/fixtures/eolica_first_30.md (precedente de fixture-file pattern)
  </read_first>
  <action>
1. Criar diretório `tests/core/fixtures/` se ainda não existir.

2. Criar `tests/core/fixtures/__init__.py` como arquivo VAZIO (0 bytes). Espelha
   exatamente `tests/parser/fixtures/__init__.py` (verificado: 0 bytes via `wc -l`).
   Não adicionar docstring nem código — é puro marker pytest.

3. Criar `tests/core/fixtures/regras_fake.json` com EXATAMENTE este conteúdo
   (PATTERNS.md §"Fixture content patterns" + CONTEXT D-32):
```json
{
  "tipo": "fake",
  "entradas": [
    {"id": "cst_999", "regex_deteccao": "\\bfake\\b"}
  ]
}
```
   Notas:
   - `\\bfake\\b` no JSON encoding → string Python `\bfake\b` → regex válido (word boundary).
   - 1 entrada apenas (mínima possível, suficiente para happy path).
   - Schema espelha `04_construcoes_sintaticas_proibidas.json` (chaves `tipo` + `entradas` + `id` + `regex_deteccao`).
   - Por D-32: fixture vive em `tests/`, NUNCA em `02_escrita/` (não poluir dataset canônico).

4. Validar que o JSON é parseável:
```bash
uv run python -c "import json, pathlib; print(json.loads(pathlib.Path('tests/core/fixtures/regras_fake.json').read_text(encoding='utf-8')))"
```
Saída esperada: `{'tipo': 'fake', 'entradas': [{'id': 'cst_999', 'regex_deteccao': '\\bfake\\b'}]}`.

5. Validar que o regex compila:
```bash
uv run python -c "import re; print(re.compile(r'\bfake\b'))"
```
Saída esperada: `re.compile('\\bfake\\b')`.
  </action>
  <verify>
    <automated>test -f tests/core/fixtures/__init__.py && test ! -s tests/core/fixtures/__init__.py && test -f tests/core/fixtures/regras_fake.json && uv run python -c "import json; data=json.loads(open('tests/core/fixtures/regras_fake.json').read()); assert data['entradas'][0]['id']=='cst_999'; assert data['entradas'][0]['regex_deteccao']==r'\bfake\b'"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/core/fixtures/__init__.py` exists with size 0 bytes
    - File `tests/core/fixtures/regras_fake.json` exists and is valid JSON
    - JSON contains key `"tipo": "fake"`
    - JSON contains entrada with `"id": "cst_999"` and `"regex_deteccao": "\\bfake\\b"`
    - `re.compile(data["entradas"][0]["regex_deteccao"])` succeeds without raising
    - File path matches exactly `tests/core/fixtures/regras_fake.json` (no rename)
  </acceptance_criteria>
  <done>
    Fixture happy-path criada em local correto, JSON válido, regex compila. Pronta
    para ser carregada por `_carregar_json_simples(FIXT / "regras_fake.json")` no
    Plan 03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar fixture de erro regras_malformadas.json</name>
  <files>
    tests/core/fixtures/regras_malformadas.json
  </files>
  <read_first>
    - .planning/phases/04-contratos-abc/04-CONTEXT.md (D-33: 3 testes de erro inclusive JSONDecodeError)
    - .planning/phases/04-contratos-abc/04-PATTERNS.md (§"regras_malformadas.json")
    - .planning/phases/04-contratos-abc/04-RESEARCH.md (D-22: erros propagam sem silenciador)
  </read_first>
  <action>
1. Criar `tests/core/fixtures/regras_malformadas.json` com EXATAMENTE este conteúdo
   (PATTERNS.md §"Fixture content patterns" — verificado: dispara JSONDecodeError):
```
{ "entradas": [
```
   Notas:
   - Arquivo intencionalmente truncado: abre `{`, abre array, sem fechar.
   - NÃO terminar com newline final é aceitável (JSON é truncado de propósito).
   - NÃO é JSON válido — é exatamente o que o teste D-33.2 precisa para verificar
     que `_carregar_json_simples` propaga `json.JSONDecodeError` sem silenciar.

2. Confirmar que dispara `JSONDecodeError`:
```bash
uv run python -c "import json; json.loads(open('tests/core/fixtures/regras_malformadas.json').read())"
```
Saída esperada: traceback terminando em `json.decoder.JSONDecodeError: Expecting value: line 1 column ... (char ...)` (ou similar — qualquer JSONDecodeError serve).
  </action>
  <verify>
    <automated>test -f tests/core/fixtures/regras_malformadas.json && ! uv run python -c "import json; json.loads(open('tests/core/fixtures/regras_malformadas.json').read())" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/core/fixtures/regras_malformadas.json` exists
    - File content begins with `{ "entradas": [` (truncated)
    - `json.loads(content)` raises `json.JSONDecodeError` (not any other exception)
    - File size > 0 bytes (not empty)
    - File path matches exactly `tests/core/fixtures/regras_malformadas.json`
  </acceptance_criteria>
  <done>
    Fixture malformed JSON criada e dispara `json.JSONDecodeError` quando lida.
    Pronta para teste `test_carregar_regras_json_decode_error` no Plan 03.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar fixtures de regex inválido e schema heterogêneo (regras_regex_invalido.json + regras_str_e_lista.json)</name>
  <files>
    tests/core/fixtures/regras_regex_invalido.json
    tests/core/fixtures/regras_str_e_lista.json
  </files>
  <read_first>
    - .planning/phases/04-contratos-abc/04-CONTEXT.md (D-33: re.error com regra_id no contexto; D-22: mensagem inclui regra_id)
    - .planning/phases/04-contratos-abc/04-PATTERNS.md (§"regras_regex_invalido.json" + §"regras_str_e_lista.json")
    - .planning/phases/04-contratos-abc/04-RESEARCH.md (Pattern 5: list → join com `|`; Pattern 9: schema heterogêneo verificado)
  </read_first>
  <action>
1. Criar `tests/core/fixtures/regras_regex_invalido.json` com EXATAMENTE este
   conteúdo (PATTERNS.md §"Fixture content patterns" — verificado: `re.compile("(?P<unfinished")` dispara `re.error: missing >`):
```json
{
  "tipo": "fake",
  "entradas": [
    {"id": "cst_invalido", "regex_deteccao": "(?P<unfinished"}
  ]
}
```
   Notas:
   - O regex `(?P<unfinished` é named group sem fechar `>` — dispara `re.error: missing >, unterminated name`.
   - O `id` "cst_invalido" será embutido na mensagem de erro re-raised pelo helper
     (D-22: `raise re.error(f"regra {rid}: {e}") from e`). Plan 03 testa que `match=cst_invalido` aparece na exception.

2. Confirmar que o regex dispara `re.error`:
```bash
uv run python -c "import re; re.compile('(?P<unfinished')"
```
Saída esperada: `re.error: missing >, unterminated name at position ...` (ou similar — qualquer re.error serve).

3. Criar `tests/core/fixtures/regras_str_e_lista.json` com EXATAMENTE este conteúdo
   (PATTERNS.md §"Fixture content patterns" — REQUIRED para coverage do branch
   heterogeneous-schema, RESEARCH Pattern 9 + branch coverage do `continue`):
```json
{
  "tipo": "fake",
  "entradas": [
    {"id": "cst_str", "regex_deteccao": "\\bsimples\\b"},
    {"id": "cst_lista", "regex_deteccao": ["\\ba\\b", "\\bb\\b"]},
    {"id": "cst_alt_key", "regex_deteccao_aproximada": ["\\bc\\b"]},
    {"id": "cst_skip", "exemplos_proibidos": ["foo", "bar"]}
  ]
}
```
   Notas:
   - Entrada 1 (`cst_str`): regex_deteccao é `str` → branch `padroes = [raw]`.
   - Entrada 2 (`cst_lista`): regex_deteccao é `list[str]` com 2 itens → branch `padroes = list(raw)` + join com `(?:...)|...`.
   - Entrada 3 (`cst_alt_key`): chave alternativa `regex_deteccao_aproximada` (com value como list — espelha cst_011 do JSON real).
   - Entrada 4 (`cst_skip`): SEM `regex_deteccao` E SEM `regex_deteccao_aproximada` (espelha cst_005 que tem só `exemplos_proibidos`) → branch `continue` no helper. CHAVE para coverage 95% (D-34): sem este caso o branch `if raw is None: continue` fica untested.
   - Esta fixture NÃO está em CONTEXT D-32 mas é OBRIGATÓRIA para coverage 95% (D-34) e Phase 6 (cst_012 com `regex_deteccao: list[str]`).

4. Validar parsing JSON e regex compilam:
```bash
uv run python -c "
import json, re
data = json.loads(open('tests/core/fixtures/regras_str_e_lista.json').read())
patterns = []
for e in data['entradas']:
    raw = e.get('regex_deteccao') or e.get('regex_deteccao_aproximada')
    if raw is None:
        continue  # entrada cst_skip e' pulada (branch coverage)
    pads = [raw] if isinstance(raw, str) else list(raw)
    if len(pads) == 1:
        patterns.append(re.compile(pads[0]))
    else:
        patterns.append(re.compile('|'.join(f'(?:{p})' for p in pads)))
print(f'compiled {len(patterns)} patterns; skipped {len(data[\"entradas\"]) - len(patterns)}')
"
```
Saída esperada: `compiled 3 patterns; skipped 1` (cst_skip pulado).
  </action>
  <verify>
    <automated>test -f tests/core/fixtures/regras_regex_invalido.json && test -f tests/core/fixtures/regras_str_e_lista.json && uv run python -c "import json, re; d1=json.loads(open('tests/core/fixtures/regras_regex_invalido.json').read()); assert d1['entradas'][0]['id']=='cst_invalido'; assert d1['entradas'][0]['regex_deteccao']=='(?P<unfinished'; d2=json.loads(open('tests/core/fixtures/regras_str_e_lista.json').read()); ids=[e['id'] for e in d2['entradas']]; assert ids==['cst_str','cst_lista','cst_alt_key','cst_skip'], f'got {ids}'; assert isinstance(d2['entradas'][0]['regex_deteccao'], str); assert isinstance(d2['entradas'][1]['regex_deteccao'], list); assert 'regex_deteccao_aproximada' in d2['entradas'][2]; assert 'regex_deteccao' not in d2['entradas'][3] and 'regex_deteccao_aproximada' not in d2['entradas'][3]"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/core/fixtures/regras_regex_invalido.json` exists with valid JSON
    - regras_regex_invalido.json contains entrada with `"id": "cst_invalido"` and `"regex_deteccao": "(?P<unfinished"`
    - `re.compile("(?P<unfinished")` raises `re.error` (verifiable via shell)
    - File `tests/core/fixtures/regras_str_e_lista.json` exists with valid JSON
    - regras_str_e_lista.json contains EXACTLY 4 entradas with ids `cst_str`, `cst_lista`, `cst_alt_key`, `cst_skip` in that order
    - cst_str.regex_deteccao is str type (`isinstance(..., str)`)
    - cst_lista.regex_deteccao is list type with 2 items
    - cst_alt_key uses key `regex_deteccao_aproximada` (NOT `regex_deteccao`) and value is list with 1 item
    - cst_skip has NEITHER `regex_deteccao` NOR `regex_deteccao_aproximada` (only `exemplos_proibidos`) — drives branch coverage do `continue`
    - All regex patterns inside regras_str_e_lista.json compile successfully with `re.compile` (excluindo cst_skip que é pulado)
  </acceptance_criteria>
  <done>
    As duas fixtures criadas. `regras_regex_invalido.json` dispara re.error com
    regra_id rastreável (D-22). `regras_str_e_lista.json` cobre 4 variações
    (RESEARCH Pattern 9 + branch coverage do `continue`), garantindo cobertura
    completa do `_carregar_json_simples` quando o helper for testado no Plan 03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read | Helper `_carregar_json_simples` (Plan 02) lê paths controlados pelo desenvolvedor; sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Information Disclosure | Untrusted JSON em JSON_SOURCE | accept | LOW: fixtures vivem em `tests/core/fixtures/` versionadas no repo, não há input externo. Plan 02 propaga `FileNotFoundError`/`JSONDecodeError`/`re.error` ruidosamente (D-22) — falha visível, sem silenciar corrupção. |
| T-04-02 | Denial of Service (regex) | Catastrophic backtracking | accept | LOW: Phase 4 não fornece regex próprio; só compila o que está no JSON. Validadores concretos (M2) é que selecionam padrões e serão code-reviewed individualmente. Out of scope para esta phase. |

Phase 4 não tem parsing de input do usuário, sem rede, sem escrita de arquivos —
threat surface é minimal. Fixtures Plan 01 são JSON estático versionado.
</threat_model>

<verification>
- Diretório `tests/core/fixtures/` existe com 5 arquivos: `__init__.py` (vazio), `regras_fake.json`, `regras_malformadas.json`, `regras_regex_invalido.json`, `regras_str_e_lista.json`.
- Cada JSON sintaticamente válido OU sintaticamente inválido conforme propósito documentado.
- Comando combinado de smoke:
```bash
uv run python -c "
import json, pathlib, re
fixt = pathlib.Path('tests/core/fixtures')
# 1. fake: JSON válido + regex válido
d = json.loads((fixt/'regras_fake.json').read_text(encoding='utf-8'))
assert d['entradas'][0]['id'] == 'cst_999'
re.compile(d['entradas'][0]['regex_deteccao'])
# 2. malformadas: JSON inválido
try: json.loads((fixt/'regras_malformadas.json').read_text(encoding='utf-8')); raise AssertionError('expected JSONDecodeError')
except json.JSONDecodeError: pass
# 3. regex_invalido: JSON válido + regex inválido
d = json.loads((fixt/'regras_regex_invalido.json').read_text(encoding='utf-8'))
assert d['entradas'][0]['id'] == 'cst_invalido'
try: re.compile(d['entradas'][0]['regex_deteccao']); raise AssertionError('expected re.error')
except re.error: pass
# 4. str_e_lista: 4 variantes de schema (3 com regex + 1 skip)
d = json.loads((fixt/'regras_str_e_lista.json').read_text(encoding='utf-8'))
ids = [e['id'] for e in d['entradas']]
assert ids == ['cst_str', 'cst_lista', 'cst_alt_key', 'cst_skip']
# cst_skip nao tem regex_deteccao nem regex_deteccao_aproximada (branch coverage)
e_skip = d['entradas'][3]
assert 'regex_deteccao' not in e_skip and 'regex_deteccao_aproximada' not in e_skip
print('ALL FIXTURES OK')
"
```
- ruff não toca em arquivos JSON; nenhum gate de lint para este plan.
</verification>

<success_criteria>
- 5 arquivos criados em `tests/core/fixtures/` (incluindo `__init__.py` vazio)
- 3 JSON válidos parseáveis (regras_fake, regras_regex_invalido, regras_str_e_lista)
- 1 JSON intencionalmente malformado (regras_malformadas) que dispara `json.JSONDecodeError`
- Comando combinado de verificação imprime `ALL FIXTURES OK`
- Nenhum dos arquivos vive em `02_escrita/termos_proibidos/` (D-32: não poluir dataset canônico)
- Coverage do branch heterogeneous-schema garantido pelas 3 entradas de `regras_str_e_lista.json` (RESEARCH Pattern 9)
</success_criteria>

<output>
After completion, create `.planning/phases/04-contratos-abc/04-01-SUMMARY.md` with:
- 5 file paths created
- Confirmation that each fixture serves its target test (D-32 D-33 D-34 mapping)
- Note that Plan 02 e Plan 03 podem agora referenciar `tests/core/fixtures/` com confiança
</output>
