---
phase: 01-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - uv.lock
  - src/biblio_validador/__init__.py
  - src/biblio_validador/cli.py
  - tests/__init__.py
  - tests/test_cli.py
  - .gitignore
autonomous: true
requirements:
  - CORE-12
validation_compliant: true
tags:
  - bootstrap
  - python
  - uv
  - typer
  - packaging
user_setup: []

must_haves:
  truths:
    - "`uv sync` em diretório limpo instala todas as dependências sem erro"
    - "`validar --help` retorna usage sem error code"
    - "`src/biblio_validador/__init__.py` existe com versão `0.1.0`"
    - "`pyproject.toml` declara todas as dependências do SUMMARY.md com versões fixadas"
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 621 metadata + deps M1 + optional llm + dependency-groups dev + tool config (ruff/mypy/pytest/coverage)"
      contains: 'name = "biblio-validador"'
      contains_also:
        - 'requires-python = ">=3.13"'
        - 'validar = "biblio_validador.cli:app"'
        - 'markdown-it-py'
        - 'pylatexenc'
        - 'typer'
        - 'rich'
        - 'loguru'
        - 'mdit-py-plugins'
        - 'line-length = 80'
        - 'strict = true'
        - 'testpaths = ["tests"]'
    - path: ".python-version"
      provides: "Pin Python 3.13 para uv auto-download"
      contains: "3.13"
    - path: "src/biblio_validador/__init__.py"
      provides: "Pacote importável com __version__ = '0.1.0' (D-03)"
      contains: '__version__ = "0.1.0"'
    - path: "src/biblio_validador/cli.py"
      provides: "Typer app exposto como entry point validar (D-10/D-11)"
      contains: "@app.callback()"
      contains_also:
        - "biblioteca canônica"
        - "import typer"
    - path: "tests/__init__.py"
      provides: "Marcador de pacote pytest (D-16)"
    - path: "tests/test_cli.py"
      provides: "Smoke tests para os 4 success criteria (D-15 + Wave 0 gaps)"
      contains: "test_validar_help_exit_zero"
      contains_also:
        - "from typer.testing import CliRunner"
        - "biblioteca canônica"
    - path: "uv.lock"
      provides: "Lockfile cross-platform commitado (D-09) — reprodutibilidade dia 1"
    - path: ".gitignore"
      provides: "Caches de tooling Python ampliados sem perder seções LaTeX/IDE"
      contains: ".pytest_cache/"
      contains_also:
        - ".ruff_cache/"
        - ".mypy_cache/"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "src/biblio_validador/cli.py:app"
      via: "uv_build PEP 503 entry point"
      pattern: 'validar = "biblio_validador.cli:app"'
    - from: "src/biblio_validador/cli.py"
      to: "typer.Typer instance"
      via: "@app.callback() preserva Typer(help=...) (Pitfall 1)"
      pattern: "@app.callback\\(\\)"
    - from: "tests/test_cli.py"
      to: "src/biblio_validador/cli.py:app"
      via: "from biblio_validador.cli import app + CliRunner.invoke"
      pattern: "runner.invoke\\(app, \\[\"--help\"\\]\\)"
    - from: "pyproject.toml [tool.coverage.run]"
      to: "src/biblio_validador/ (módulo importável)"
      via: "source = ['biblio_validador'] (módulo, NÃO distribution name)"
      pattern: 'source = \["biblio_validador"\]'
---

<objective>
Phase 1 — Bootstrap: criar projeto Python instalável via `uv` com layout `src/biblio_validador/`, entry point `validar` no PATH, smoke test verde e dependências M1 fixadas no `pyproject.toml`.

Purpose: cobrir 100% de CORE-12 (REQUIREMENTS.md). Sem este bootstrap, todas as phases 2+ não têm onde plugar código (parser, dataclasses, validadores). Phase 1 é estritamente estrutural — não há lógica de negócio, apenas scaffolding correto + verificação automatizada de que o scaffolding funciona.

Output:
- `pyproject.toml` canônico com 6 deps M1 + grupo opcional `llm` + `dependency-groups.dev` + 4 tabelas `[tool.*]`
- `src/biblio_validador/__init__.py` (`__version__ = "0.1.0"` apenas)
- `src/biblio_validador/cli.py` (Typer skeleton com `@app.callback()` — Pitfall 1)
- `tests/__init__.py` (vazio) + `tests/test_cli.py` (3 smoke tests cobrindo success criteria #2, #3, #4)
- `.python-version` (`3.13`) + `.gitignore` ampliado + `uv.lock` commitado (D-09)
- `validar --help` exit 0 com string "biblioteca canônica" verificável via `pytest` in-process (CliRunner)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap/01-CONTEXT.md
@.planning/phases/01-bootstrap/01-RESEARCH.md
@.planning/phases/01-bootstrap/01-PATTERNS.md
@.planning/phases/01-bootstrap/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@.gitignore

<interfaces>
<!-- Phase 1 cria as primeiras interfaces Python do projeto. Não há código pré-existente. -->
<!-- Os contratos canônicos abaixo foram VALIDADOS EMPIRICAMENTE no sandbox /tmp/uv-bootstrap-smoke (RESEARCH.md 2026-05-04). -->

## Contrato 1: pyproject.toml (canonical, sandbox-verified)

```toml
[project]
name = "biblio-validador"
version = "0.1.0"
description = "Validador & Fixer acadêmico PT-BR — biblioteca canônica PPGD/Unifor"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "Kalyl Lamarck" },
]
dependencies = [
    "loguru>=0.7.3",
    "markdown-it-py>=4.0.0",
    "mdit-py-plugins",
    "pylatexenc>=2.10,<3.0",
    "rich>=15.0.0",
    "typer>=0.25.1",
]

[project.optional-dependencies]
llm = [
    "anthropic>=0.97.0",
    "jinja2>=3.1.6",
]

[project.scripts]
validar = "biblio_validador.cli:app"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff",
]

[tool.ruff]
line-length = 80
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
strict = true
python_version = "3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short"

[tool.coverage.run]
source = ["biblio_validador"]
branch = true
```

NOTA: `dependencies` e `dev` ficam ordenados alfabeticamente porque `uv add` re-alfabetiza (Pitfall 4). Aceitar essa ordem como canônica.

## Contrato 2: src/biblio_validador/__init__.py (D-03)

```python
"""Validador & Fixer Acadêmico — Biblioteca Canônica PPGD/Unifor."""

__version__ = "0.1.0"
```

## Contrato 3: src/biblio_validador/cli.py (D-10/D-11 + Pitfall 1)

```python
"""CLI entry point — esqueleto Phase 1.

Comandos reais (`validar`, `corrigir`, `auditar`) virão em phases futuras
(M1 piloto Phase 8 + M8 orchestrator Phase 52).
"""
import typer

app = typer.Typer(
    help="Validador acadêmico — biblioteca canônica PPGD/Unifor",
)


@app.callback()
def main() -> None:
    """Comandos virão em phases futuras (validar, corrigir, auditar)."""
    pass
```

CRÍTICO: usar `@app.callback()` — NUNCA `@app.command()` único. Pitfall 1 (severidade ALTA): `@app.command()` único colapsa Typer em "single-command mode" e o `--help` mostra docstring do comando em vez do `Typer(help=...)`, fazendo o smoke test falhar silenciosamente.

NÃO adicionar `if __name__ == "__main__": app()` no fim do módulo (Pitfall 7) — entry point já invoca `app()`; duplicar quebra os testes com `SystemExit` no import.

## Contrato 4: tests/test_cli.py (D-15 + Wave 0 gaps)

```python
"""Smoke tests Phase 1 — valida 4 success criteria de CORE-12."""
import tomllib
from pathlib import Path

from typer.testing import CliRunner

import biblio_validador
from biblio_validador.cli import app

runner = CliRunner()


def test_validar_help_exit_zero() -> None:
    """Criterion #2: `validar --help` retorna exit 0 + nome canônico."""
    result = runner.invoke(app, ["--help"])
    assert result.exit_code == 0
    assert "biblioteca canônica" in result.output


def test_version_exposed() -> None:
    """Criterion #3: `biblio_validador.__version__` == '0.1.0'."""
    assert biblio_validador.__version__ == "0.1.0"


def test_pyproject_declares_m1_deps() -> None:
    """Criterion #4: pyproject.toml declara as 6 deps M1."""
    pyproject = Path(__file__).parent.parent / "pyproject.toml"
    data = tomllib.loads(pyproject.read_text(encoding="utf-8"))
    deps = " ".join(data["project"]["dependencies"])
    for required in (
        "markdown-it-py",
        "mdit-py-plugins",
        "pylatexenc",
        "typer",
        "rich",
        "loguru",
    ):
        assert required in deps, f"M1 dep ausente: {required}"
```

## Contrato 5: tests/__init__.py (D-16)

Arquivo vazio (zero bytes). Apenas marcador de pacote.

## Contrato 6: .python-version

Arquivo com conteúdo único: `3.13` (sem newline-sensitivity).

## Contrato 7: .gitignore (append-only patch)

Inserir BLOCO entre `# Python` (linha 6) e `# LaTeX build` (linha 8) preservando todas as outras seções:

```
# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Python tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/

# Build artifacts
*.egg-info/
dist/
build/

# LaTeX build
**/build/
...
```

CRÍTICO (Pitfall 6): NÃO sobrescrever — APPEND-ONLY. Preservar verbatim seções LaTeX/Backups/OS/IDE/Tmp existentes.
</interfaces>

<sequenced_action_summary>
Execução em 3 tarefas sequenciais dentro deste único plano (Wave 1). Cada tarefa fecha um bloco da sequência canônica empírica de RESEARCH.md §"Sequência canônica" (Blocos 1-5):

- Task 1: Blocos 1+2 (uv init + 6 overrides + arquivos novos + .gitignore patch + tests scaffolding)
- Task 2: Bloco 3 (uv add x3 — core, optional llm, dev) + lockfile
- Task 3: Bloco 4 (validar 4 success criteria via uv sync + pytest + grep + manual `--help`)

Bloco 5 (commit) é responsabilidade do executor após este plano (gsd-execute-phase faz commit automático).
</sequenced_action_summary>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold projeto via `uv init` + aplicar overrides D-XX + criar arquivos novos</name>
  <files>
    pyproject.toml,
    .python-version,
    src/biblio_validador/__init__.py,
    src/biblio_validador/cli.py,
    tests/__init__.py,
    tests/test_cli.py,
    .gitignore
  </files>
  <read_first>
    - .planning/phases/01-bootstrap/01-RESEARCH.md (§"Sequência canônica" Bloco 1-2 lines 626-662; §"Pattern 1: Typer skeleton" lines 228-253; §"Pattern 4: pyproject.toml canônico" lines 294-354; Pitfalls 1, 3, 5, 6, 7)
    - .planning/phases/01-bootstrap/01-PATTERNS.md (excerpts canônicos completos para os 7 arquivos)
    - .planning/phases/01-bootstrap/01-CONTEXT.md (D-01 a D-19 — decisões locked NÃO-NEGOCIÁVEIS)
    - .planning/phases/01-bootstrap/01-VALIDATION.md ("Wave 0 Requirements" — confirma necessidade dos 3 testes)
    - /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica/.gitignore (estado atual, 42 linhas — preservar verbatim)
    - CLAUDE.md raiz biblioteca_canonica + /home/kalyllamarck/projetos/Doutorado/CLAUDE.md (regras: Python 3.13+, type hints, max 80 chars, PT-BR, uv não pip)
  </read_first>
  <behavior>
    - **Test 1 (`test_validar_help_exit_zero`):** `runner.invoke(app, ["--help"])` retorna `exit_code == 0` e `"biblioteca canônica"` está em `result.output`. Cobre CORE-12 success criterion #2 + Pitfall 1.
    - **Test 2 (`test_version_exposed`):** `biblio_validador.__version__ == "0.1.0"`. Cobre CORE-12 success criterion #3 + D-03.
    - **Test 3 (`test_pyproject_declares_m1_deps`):** as 6 strings `markdown-it-py`, `mdit-py-plugins`, `pylatexenc`, `typer`, `rich`, `loguru` aparecem em `pyproject.toml [project].dependencies`. Cobre CORE-12 success criterion #4 + D-06.
    - **Estado de erro esperado durante Task 1:** os 3 testes existem mas FALHAM até Task 2 instalar o pacote (`ModuleNotFoundError: No module named 'biblio_validador'` ou `pyproject.toml` ainda sem deps). Isso é ESPERADO — Task 2 fará o pytest verde. Task 1 só GRAVA os testes; não os roda.
  </behavior>
  <action>
    Execute em ordem (uma falha aborta a tarefa inteira).

    **Passo 1: rodar `uv init --package biblio-validador` na raiz do repo.**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    uv init --package biblio-validador
    ```

    Isso cria: `pyproject.toml` (skeleton), `src/biblio_validador/__init__.py` (com `def main(): print("Hello...")`), `.python-version` (provavelmente `3.12` ou similar), `README.md` (NÃO sobrescrever — README já existe; se `uv init` reclamar, rodar `uv init --package biblio-validador --no-readme` ou aceitar e reverter o README depois com `git checkout README.md`).

    **Passo 2: aplicar 6 overrides explícitos (Pitfall 3).** Substituir o conteúdo gerado conforme abaixo.

    **2a. Sobrescrever `.python-version` com:**
    ```
    3.13
    ```
    (sem outros conteúdos, sem newlines extras intencionais — `printf '3.13\n' > .python-version` ou Write tool)

    **2b. Sobrescrever `src/biblio_validador/__init__.py` com EXATAMENTE:**
    ```python
    """Validador & Fixer Acadêmico — Biblioteca Canônica PPGD/Unifor."""

    __version__ = "0.1.0"
    ```
    Apagar o `def main(): print("Hello...")` gerado por `uv init`. Não adicionar imports.

    **2c. Criar `src/biblio_validador/cli.py` com EXATAMENTE:**
    ```python
    """CLI entry point — esqueleto Phase 1.

    Comandos reais (`validar`, `corrigir`, `auditar`) virão em phases futuras
    (M1 piloto Phase 8 + M8 orchestrator Phase 52).
    """
    import typer

    app = typer.Typer(
        help="Validador acadêmico — biblioteca canônica PPGD/Unifor",
    )


    @app.callback()
    def main() -> None:
        """Comandos virão em phases futuras (validar, corrigir, auditar)."""
        pass
    ```
    NUNCA usar `@app.command()` (Pitfall 1, severidade ALTA — quebra smoke test silenciosamente). NÃO adicionar `if __name__ == "__main__": app()` (Pitfall 7).

    **2d. Editar `pyproject.toml`** removendo o `[project.scripts] biblio-validador = "biblio_validador:main"` auto-gerado e aplicando os seguintes ajustes manuais (deps serão adicionadas em Task 2 via `uv add`, NÃO manualmente — Pitfall 4):
    - `requires-python = ">=3.13"` (substitui `>=3.12` do default)
    - `description = "Validador & Fixer acadêmico PT-BR — biblioteca canônica PPGD/Unifor"` (substitui `"Add your description here"`)
    - Adicionar bloco `[project.scripts]`:
      ```toml
      [project.scripts]
      validar = "biblio_validador.cli:app"
      ```
      (REMOVER qualquer entrada `biblio-validador = "biblio_validador:main"` que `uv init` tenha gerado)
    - Anexar 4 tabelas `[tool.*]` ao FINAL do arquivo (não mexer no `[build-system]` auto-gerado por `uv init` — ele já está correto com `uv_build`):
      ```toml
      [tool.ruff]
      line-length = 80
      target-version = "py313"

      [tool.ruff.lint]
      select = ["E", "F", "I", "UP", "B"]

      [tool.mypy]
      strict = true
      python_version = "3.13"

      [tool.pytest.ini_options]
      testpaths = ["tests"]
      addopts = "--tb=short"

      [tool.coverage.run]
      source = ["biblio_validador"]
      branch = true
      ```
    - Manter `authors = [{ name = "Kalyl Lamarck" }]` se gerado por `uv init` (ele lê de `git config user.name`); se ausente, adicionar manualmente.
    - Manter `name = "biblio-validador"` (com hífen — distribution name PEP 503).
    - NÃO adicionar `pythonpath = ["src"]` em `[tool.pytest.ini_options]` (Pitfall 5 — desnecessário com editable install).

    **Passo 3: criar arquivos de teste.**

    **3a. Criar `tests/__init__.py` VAZIO** (zero bytes — D-16):
    ```bash
    mkdir -p tests
    : > tests/__init__.py
    ```

    **3b. Criar `tests/test_cli.py` com EXATAMENTE:**
    ```python
    """Smoke tests Phase 1 — valida 4 success criteria de CORE-12."""
    import tomllib
    from pathlib import Path

    from typer.testing import CliRunner

    import biblio_validador
    from biblio_validador.cli import app

    runner = CliRunner()


    def test_validar_help_exit_zero() -> None:
        """Criterion #2: `validar --help` retorna exit 0 + nome canônico."""
        result = runner.invoke(app, ["--help"])
        assert result.exit_code == 0
        assert "biblioteca canônica" in result.output


    def test_version_exposed() -> None:
        """Criterion #3: `biblio_validador.__version__` == '0.1.0'."""
        assert biblio_validador.__version__ == "0.1.0"


    def test_pyproject_declares_m1_deps() -> None:
        """Criterion #4: pyproject.toml declara as 6 deps M1."""
        pyproject = Path(__file__).parent.parent / "pyproject.toml"
        data = tomllib.loads(pyproject.read_text(encoding="utf-8"))
        deps = " ".join(data["project"]["dependencies"])
        for required in (
            "markdown-it-py",
            "mdit-py-plugins",
            "pylatexenc",
            "typer",
            "rich",
            "loguru",
        ):
            assert required in deps, f"M1 dep ausente: {required}"
    ```

    **Passo 4: ampliar `.gitignore` (Pitfall 6 — APPEND ONLY).**

    Localizar a linha `venv/` (linha 6 atual) e INSERIR após ela (e ANTES da linha em branco que precede `# LaTeX build`):
    ```
    
    # Python tooling caches
    .pytest_cache/
    .ruff_cache/
    .mypy_cache/
    htmlcov/
    
    # Build artifacts
    *.egg-info/
    dist/
    build/
    ```

    Preservar verbatim as seções existentes: `# Python` (linhas 1-6), `# LaTeX build` (linhas 8-23 originais — agora deslocadas), `# Backups`, `# OS`, `# IDE`, `# Tmp`.

    **Verificação visual após edição (não-bloqueante):** `git diff .gitignore` deve mostrar APENAS adições (`+` lines), zero remoções.

    **Passo 5: NÃO rodar pytest ainda.** Task 1 só grava arquivos. `pytest` só verde após Task 2 fazer `uv sync` (instala pacote em editable mode).
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 5.1 — arquivos existem
      test -f pyproject.toml || { echo FAIL: pyproject.toml; exit 1; }
      test -f .python-version || { echo FAIL: .python-version; exit 1; }
      test -f src/biblio_validador/__init__.py || { echo FAIL: __init__.py; exit 1; }
      test -f src/biblio_validador/cli.py || { echo FAIL: cli.py; exit 1; }
      test -f tests/__init__.py || { echo FAIL: tests/__init__.py; exit 1; }
      test -f tests/test_cli.py || { echo FAIL: tests/test_cli.py; exit 1; }
      test -f .gitignore || { echo FAIL: .gitignore; exit 1; }

      # 5.2 — overrides aplicados em pyproject.toml
      grep -qE "^requires-python = \">=3\.13\"" pyproject.toml || { echo FAIL: requires-python; exit 1; }
      grep -qF "validar = \"biblio_validador.cli:app\"" pyproject.toml || { echo FAIL: scripts entry; exit 1; }
      ! grep -qF "biblio-validador = " pyproject.toml || { echo FAIL: leftover default script; exit 1; }
      grep -qF "[tool.ruff]" pyproject.toml || { echo FAIL: tool.ruff; exit 1; }
      grep -qF "line-length = 80" pyproject.toml || { echo FAIL: line-length; exit 1; }
      grep -qF "[tool.mypy]" pyproject.toml || { echo FAIL: tool.mypy; exit 1; }
      grep -qF "strict = true" pyproject.toml || { echo FAIL: mypy strict; exit 1; }
      grep -qF "[tool.pytest.ini_options]" pyproject.toml || { echo FAIL: pytest config; exit 1; }
      grep -qF "testpaths = [\"tests\"]" pyproject.toml || { echo FAIL: testpaths; exit 1; }
      grep -qF "[tool.coverage.run]" pyproject.toml || { echo FAIL: coverage; exit 1; }
      grep -qF "source = [\"biblio_validador\"]" pyproject.toml || { echo FAIL: coverage source; exit 1; }
      grep -qF "[build-system]" pyproject.toml || { echo FAIL: build-system; exit 1; }
      grep -qF "uv_build" pyproject.toml || { echo FAIL: uv_build backend; exit 1; }
      ! grep -qF "pythonpath" pyproject.toml || { echo FAIL: pythonpath leaked (Pitfall 5); exit 1; }

      # 5.3 — .python-version pinado
      grep -qE "^3\.13$" .python-version || { echo FAIL: python-version not 3.13; exit 1; }

      # 5.4 — __init__.py contém só __version__
      grep -qF "__version__ = \"0.1.0\"" src/biblio_validador/__init__.py || { echo FAIL: version; exit 1; }
      ! grep -qF "def main" src/biblio_validador/__init__.py || { echo FAIL: leftover def main from uv init; exit 1; }

      # 5.5 — cli.py usa @app.callback (NUNCA @app.command em Phase 1) — Pitfall 1
      grep -qF "@app.callback()" src/biblio_validador/cli.py || { echo FAIL: callback missing; exit 1; }
      ! grep -qE "^@app\.command\(" src/biblio_validador/cli.py || { echo FAIL: @app.command leak (Pitfall 1); exit 1; }
      grep -qF "biblioteca canônica" src/biblio_validador/cli.py || { echo FAIL: PT-BR help text; exit 1; }
      ! grep -qF "if __name__ ==" src/biblio_validador/cli.py || { echo FAIL: __main__ block leaked (Pitfall 7); exit 1; }
      grep -qF "import typer" src/biblio_validador/cli.py || { echo FAIL: typer import; exit 1; }
      grep -qF "def main() -> None" src/biblio_validador/cli.py || { echo FAIL: type hint missing (D-13 strict); exit 1; }

      # 5.6 — tests/__init__.py vazio (D-16)
      test ! -s tests/__init__.py || { echo FAIL: tests/__init__.py not empty; exit 1; }

      # 5.7 — tests/test_cli.py contém os 3 testes
      grep -qF "def test_validar_help_exit_zero" tests/test_cli.py || { echo FAIL: test_validar_help; exit 1; }
      grep -qF "def test_version_exposed" tests/test_cli.py || { echo FAIL: test_version; exit 1; }
      grep -qF "def test_pyproject_declares_m1_deps" tests/test_cli.py || { echo FAIL: test_deps; exit 1; }
      grep -qF "from typer.testing import CliRunner" tests/test_cli.py || { echo FAIL: CliRunner import; exit 1; }
      grep -qF "biblioteca canônica" tests/test_cli.py || { echo FAIL: PT-BR assertion; exit 1; }
      grep -qF "import tomllib" tests/test_cli.py || { echo FAIL: tomllib import; exit 1; }

      # 5.8 — .gitignore ampliado (Pitfall 6)
      grep -qF ".pytest_cache/" .gitignore || { echo FAIL: pytest_cache; exit 1; }
      grep -qF ".ruff_cache/" .gitignore || { echo FAIL: ruff_cache; exit 1; }
      grep -qF ".mypy_cache/" .gitignore || { echo FAIL: mypy_cache; exit 1; }
      grep -qF "htmlcov/" .gitignore || { echo FAIL: htmlcov; exit 1; }
      # 5.9 — append-only: seções LaTeX preservadas
      grep -qF "# LaTeX build" .gitignore || { echo FAIL: LaTeX section lost; exit 1; }
      grep -qF "*.synctex.gz" .gitignore || { echo FAIL: LaTeX entries lost; exit 1; }
      grep -qF "# Backups" .gitignore || { echo FAIL: Backups section lost; exit 1; }
      grep -qF ".DS_Store" .gitignore || { echo FAIL: OS section lost; exit 1; }

      echo PASS: Task 1 scaffolding complete
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-1.1:** Comando `test -f pyproject.toml && test -f .python-version && test -f src/biblio_validador/__init__.py && test -f src/biblio_validador/cli.py && test -f tests/__init__.py && test -f tests/test_cli.py` retorna exit 0.
    - **AC-1.2:** `grep -F 'requires-python = ">=3.13"' pyproject.toml` retorna 1 linha (override D-05 aplicado).
    - **AC-1.3:** `grep -F 'validar = "biblio_validador.cli:app"' pyproject.toml` retorna 1 linha E `grep -F 'biblio-validador = ' pyproject.toml` retorna 0 linhas (Pitfall 3 — script default removido).
    - **AC-1.4:** `grep -c '^\[tool\.' pyproject.toml` retorna ≥ 5 (ruff, ruff.lint, mypy, pytest.ini_options, coverage.run — D-12/D-13/D-14).
    - **AC-1.5:** `grep -F 'pythonpath' pyproject.toml` retorna 0 linhas (Pitfall 5 — não introduzir config desnecessária).
    - **AC-1.6:** `cat .python-version` retorna exatamente `3.13` (com ou sem newline trailing).
    - **AC-1.7:** `grep -F '__version__ = "0.1.0"' src/biblio_validador/__init__.py` retorna 1 linha E `grep -F 'def main' src/biblio_validador/__init__.py` retorna 0 linhas (D-03 + remoção do default `uv init`).
    - **AC-1.8:** `grep -F '@app.callback()' src/biblio_validador/cli.py` retorna 1 linha E `grep -E '^@app\.command\(' src/biblio_validador/cli.py` retorna 0 linhas (Pitfall 1 — severidade ALTA).
    - **AC-1.9:** `grep -F 'if __name__ ==' src/biblio_validador/cli.py` retorna 0 linhas (Pitfall 7).
    - **AC-1.10:** `grep -F 'biblioteca canônica' src/biblio_validador/cli.py` retorna 1 linha (string que o smoke test asserta — qualquer drift quebra verificação).
    - **AC-1.11:** `wc -c tests/__init__.py` retorna 0 (arquivo vazio, D-16).
    - **AC-1.12:** `grep -c '^def test_' tests/test_cli.py` retorna 3 (3 testes — Wave 0 gaps fechados).
    - **AC-1.13:** `grep -F '.pytest_cache/' .gitignore && grep -F '.ruff_cache/' .gitignore && grep -F '.mypy_cache/' .gitignore && grep -F 'htmlcov/' .gitignore` exit 0 (Pitfall 6).
    - **AC-1.14:** `grep -F '# LaTeX build' .gitignore && grep -F '*.synctex.gz' .gitignore && grep -F '# Backups' .gitignore && grep -F '.DS_Store' .gitignore` exit 0 (preservação de seções existentes).
    - **AC-1.15:** O verify script automatizado roda e imprime `PASS: Task 1 scaffolding complete`.
  </acceptance_criteria>
  <done>
    Todos os 7 arquivos existem com conteúdo canônico verificado por grep. Os 3 testes (`test_validar_help_exit_zero`, `test_version_exposed`, `test_pyproject_declares_m1_deps`) estão escritos. `.gitignore` foi append-only (Pitfall 6). `pyproject.toml` tem 6 overrides D-XX aplicados (Pitfall 3) sem leaks de defaults `uv init`. NÃO rodar pytest ainda — Task 2 instala o pacote.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolver dependências via `uv add` (3 passes) + `uv sync` + commit lockfile</name>
  <files>
    pyproject.toml,
    uv.lock
  </files>
  <read_first>
    - .planning/phases/01-bootstrap/01-RESEARCH.md (§"Sequência canônica" Bloco 3 lines 647-651; Pitfalls 2 e 4; "Environment Availability" lines 740-756)
    - .planning/phases/01-bootstrap/01-CONTEXT.md (D-06, D-07, D-08, D-09)
    - .planning/research/STACK.md (versões fixadas das 6 deps M1)
    - pyproject.toml (criado em Task 1 — base sobre a qual `uv add` opera)
  </read_first>
  <action>
    Execute em ordem (a falha de qualquer passo aborta a tarefa).

    **Pré-requisito:** Task 1 completa com `pyproject.toml` válido sem leaks de defaults.

    **Passo 1: Pass core deps (D-06).**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    uv add 'markdown-it-py>=4.0.0' mdit-py-plugins 'pylatexenc>=2.10,<3.0' 'typer>=0.25.1' 'rich>=15.0.0' 'loguru>=0.7.3'
    ```

    Saída esperada:
    - 6 entradas em `[project.dependencies]` (alfabetizadas pelo `uv add` — Pitfall 4 cosmético, aceitar)
    - `uv.lock` criado/atualizado
    - Possível auto-download de `cpython-3.13.13-linux-x86_64-gnu` (~33.7MiB) na primeira invocação se Python 3.13 ausente (Pitfall 2 — requer internet).
    - `.venv/` criada se ausente.

    **Passo 2: Pass optional `llm` extra (D-07).**

    ```bash
    uv add --optional llm 'anthropic>=0.97.0' 'jinja2>=3.1.6'
    ```

    Saída esperada:
    - Bloco `[project.optional-dependencies] llm = [...]` adicionado/atualizado em `pyproject.toml`.
    - Estas deps NÃO são instaladas no venv default (`uv sync` sem `--extra llm` ignora). Verificável via `! test -d .venv/lib/python3.13/site-packages/anthropic`.

    **Passo 3: Pass dev deps (D-08).**

    ```bash
    uv add --dev 'pytest>=9.0.3' 'pytest-cov>=7.1.0' ruff mypy
    ```

    Saída esperada:
    - Bloco `[dependency-groups] dev = [...]` adicionado/atualizado (PEP 735, não optional-deps).
    - 4 deps dev instaladas no `.venv/` por default em `uv sync` (PEP 735 — instala default, diferente de optional-dependencies).

    **Passo 4: `uv sync` final.**

    ```bash
    uv sync
    ```

    Saída esperada:
    - Exit 0.
    - Mensagem do tipo `Resolved N packages` + `Installed N packages`.
    - `.venv/bin/validar` criado (entry point script).
    - `biblio_validador` instalado em editable mode (`.venv/lib/python3.13/site-packages/biblio_validador.pth` ou similar).

    **Passo 5: Sanity check `--help` direto (independente de pytest).**

    ```bash
    .venv/bin/validar --help
    ```

    Saída esperada: exit 0, output contém "biblioteca canônica" e/ou "Validador acadêmico" (Typer help formatado por Rich).

    Se este comando falhar com `RuntimeError: Could not get a command for this Typer instance`, REGRESSÃO de Pitfall 1 → voltar para Task 1 e verificar `@app.callback()` em `cli.py`.

    Se `validar` não existir em `.venv/bin/`, REGRESSÃO de Pitfall 3 → verificar que `[project.scripts] validar = "biblio_validador.cli:app"` está em `pyproject.toml` (e não a entrada default `biblio-validador = "biblio_validador:main"`).

    **Passo 6: NÃO rodar pytest ainda.** Pytest é Task 3 (gate de validação consolidado). Task 2 fecha em sanity check direto do entry point.

    **NÃO commitar manualmente.** O orchestrator `gsd-execute-phase` faz commit ao fim do plano.
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 6.1 — uv.lock criado
      test -f uv.lock || { echo FAIL: uv.lock missing; exit 1; }

      # 6.2 — .venv criado pelo uv sync
      test -d .venv || { echo FAIL: .venv missing; exit 1; }
      test -x .venv/bin/python3.13 || test -L .venv/bin/python3.13 || { echo FAIL: Python 3.13 not in venv; exit 1; }

      # 6.3 — entry point validar criado
      test -x .venv/bin/validar || { echo FAIL: .venv/bin/validar not executable; exit 1; }

      # 6.4 — 6 deps M1 declaradas em pyproject (Pitfall 4: alfabetizadas pelo uv add)
      python3 -c "
      import tomllib
      data = tomllib.loads(open(\"pyproject.toml\", \"rb\").read().decode())
      deps = \" \".join(data[\"project\"][\"dependencies\"])
      required = [\"markdown-it-py\", \"mdit-py-plugins\", \"pylatexenc\", \"typer\", \"rich\", \"loguru\"]
      missing = [r for r in required if r not in deps]
      if missing:
          print(f\"FAIL: M1 deps ausentes: {missing}\")
          import sys; sys.exit(1)
      print(f\"PASS: 6 deps M1 declaradas\")
      "

      # 6.5 — optional llm declarado mas NÃO instalado por default
      python3 -c "
      import tomllib
      data = tomllib.loads(open(\"pyproject.toml\", \"rb\").read().decode())
      llm = data[\"project\"].get(\"optional-dependencies\", {}).get(\"llm\", [])
      assert any(\"anthropic\" in d for d in llm), f\"FAIL: anthropic missing from llm extra\"
      assert any(\"jinja2\" in d for d in llm), f\"FAIL: jinja2 missing from llm extra\"
      print(\"PASS: optional llm declarado\")
      "
      ! test -d .venv/lib/python3.13/site-packages/anthropic || { echo FAIL: anthropic instalado por default (deveria ser opcional); exit 1; }

      # 6.6 — dev deps em [dependency-groups] (PEP 735) instaladas por default
      python3 -c "
      import tomllib
      data = tomllib.loads(open(\"pyproject.toml\", \"rb\").read().decode())
      dev = data.get(\"dependency-groups\", {}).get(\"dev\", [])
      required = [\"pytest\", \"pytest-cov\", \"ruff\", \"mypy\"]
      missing = [r for r in required if not any(r in d for d in dev)]
      if missing:
          print(f\"FAIL: dev deps ausentes: {missing}\")
          import sys; sys.exit(1)
      print(\"PASS: dev deps em PEP 735 group\")
      "
      test -d .venv/lib/python3.13/site-packages/pytest || { echo FAIL: pytest não instalado; exit 1; }
      test -d .venv/lib/python3.13/site-packages/ruff || test -x .venv/bin/ruff || { echo FAIL: ruff não instalado; exit 1; }

      # 6.7 — biblio_validador instalado em editable mode
      .venv/bin/python -c "import biblio_validador; assert biblio_validador.__version__ == \"0.1.0\", biblio_validador.__version__" || { echo FAIL: editable install ou __version__; exit 1; }

      # 6.8 — validar --help exit 0 + string canônica (Pitfall 1 sanity check)
      OUT=$(.venv/bin/validar --help 2>&1)
      EC=$?
      if [ $EC -ne 0 ]; then
          echo "FAIL: validar --help exit code: $EC"
          echo "$OUT"
          exit 1
      fi
      echo "$OUT" | grep -qF "biblioteca canônica" || { echo "FAIL: --help output missing biblioteca canônica (Pitfall 1?)"; echo "$OUT"; exit 1; }

      echo PASS: Task 2 deps + sync + entry point ok
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-2.1:** `test -f uv.lock` exit 0 (D-09 lockfile presente).
    - **AC-2.2:** `test -x .venv/bin/validar` exit 0 (entry point criado pelo `uv_build` backend após `uv sync`).
    - **AC-2.3:** `python3 -c "import tomllib; data=tomllib.loads(open('pyproject.toml','rb').read().decode()); assert all(any(r in d for d in data['project']['dependencies']) for r in ['markdown-it-py','mdit-py-plugins','pylatexenc','typer','rich','loguru'])"` exit 0 (D-06 — 6 deps M1 declaradas).
    - **AC-2.4:** `python3 -c "import tomllib; data=tomllib.loads(open('pyproject.toml','rb').read().decode()); llm=data['project']['optional-dependencies']['llm']; assert any('anthropic' in d for d in llm) and any('jinja2' in d for d in llm)"` exit 0 (D-07 — optional llm declarado).
    - **AC-2.5:** `! test -d .venv/lib/python3.13/site-packages/anthropic` (optional llm NÃO instalado em sync default — economiza ~50MB).
    - **AC-2.6:** `python3 -c "import tomllib; data=tomllib.loads(open('pyproject.toml','rb').read().decode()); dev=data['dependency-groups']['dev']; assert all(any(r in d for d in dev) for r in ['pytest','pytest-cov','ruff','mypy'])"` exit 0 (D-08 — PEP 735 group).
    - **AC-2.7:** `.venv/bin/python -c "import biblio_validador; assert biblio_validador.__version__ == '0.1.0'"` exit 0 (CORE-12 success criterion #3 + editable install funcional).
    - **AC-2.8:** `.venv/bin/validar --help` exit 0 (CORE-12 success criterion #2 — entry point ELF wrapper).
    - **AC-2.9:** `.venv/bin/validar --help 2>&1 | grep -F "biblioteca canônica"` retorna ≥ 1 linha (Pitfall 1 sanity — `@app.callback()` preservou Typer help).
    - **AC-2.10:** O verify script automatizado roda e imprime `PASS: Task 2 deps + sync + entry point ok`.
  </acceptance_criteria>
  <done>
    `uv.lock` commit-ready, `.venv/bin/validar` invocável com exit 0 + string canônica, 6 deps M1 declaradas, optional llm declarado mas não instalado, 4 dev deps instaladas via PEP 735. CORE-12 success criteria #1, #2, #3, #4 todos efetivamente cumpridos — Task 3 apenas formaliza via pytest + ruff + mypy.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validar 4 success criteria via pytest + ruff + mypy (Nyquist 2× gate)</name>
  <files>
    (sem novos arquivos — só executa ferramentas)
  </files>
  <read_first>
    - .planning/phases/01-bootstrap/01-VALIDATION.md ("Phase Requirements → Test Map" + "Nyquist 2× Coverage Justification")
    - .planning/phases/01-bootstrap/01-RESEARCH.md (§"Sequência canônica" Bloco 4 lines 651-657; Pitfall 5 "sempre via uv run, nunca pytest direto")
    - tests/test_cli.py (criado em Task 1 — base do gate)
  </read_first>
  <action>
    Execute em ordem. Qualquer falha aborta o gate de phase.

    **Passo 1: pytest verde (consolidado — 3 testes cobrem 3 dos 4 success criteria).**

    ```bash
    cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica
    uv run pytest tests/ -v
    ```

    Saída esperada:
    - Exit 0
    - 3 testes passam (`test_validar_help_exit_zero`, `test_version_exposed`, `test_pyproject_declares_m1_deps`)
    - Tempo total < 2s (smoke in-process via CliRunner — sem subprocess)

    Se `ModuleNotFoundError: No module named 'biblio_validador'` → REGRESSÃO de Task 2 (`uv sync` não instalou editable). Voltar para Task 2.

    Se `assert "biblioteca canônica" in result.output` falhar → REGRESSÃO de Pitfall 1 em `cli.py`. Voltar para Task 1.

    **CRÍTICO:** rodar via `uv run pytest` — NUNCA `pytest` direto (Pitfall 5 — pytest direto requer venv ativada manualmente; `uv run` é o contrato universal).

    **Passo 2: ruff lint + format check (D-12 — gate de estilo).**

    ```bash
    uv run ruff check .
    uv run ruff format --check .
    ```

    Saída esperada: ambos exit 0, zero warnings.

    Se `E501: line too long` em algum `.py` criado pelo plano → REGRESSÃO de constraint "max 80 chars". Localizar a linha e quebrar.

    Se `F401: imported but unused` em `cli.py` → REGRESSÃO de Pitfall 1 anti-pattern (importar lib não usada). Remover import.

    **Passo 3: mypy strict (D-13 — gate de type checking).**

    ```bash
    uv run mypy src/
    ```

    Saída esperada: exit 0, `Success: no issues found in 2 source files` (apenas `__init__.py` e `cli.py`).

    Se `error: Function is missing a return type annotation` → REGRESSÃO de constraint "type hints obrigatórios". Localizar função e adicionar `-> None` ou tipo de retorno apropriado.

    **Passo 4: success criterion #1 (`uv sync` em diretório limpo) — verificação manual.**

    `uv sync` já foi executado em Task 2 com exit 0. Re-executar agora confirma idempotência:

    ```bash
    uv sync
    ```

    Saída esperada: exit 0, mensagem "Audited N packages" (ou "Installed 0 packages" se nada mudou).

    **Passo 5: snapshot de saúde (informacional — não bloqueante).**

    ```bash
    .venv/bin/validar --help
    .venv/bin/python -c "import biblio_validador; print(biblio_validador.__version__)"
    grep -cE '^\s*"(markdown-it-py|mdit-py-plugins|pylatexenc|typer|rich|loguru)' pyproject.toml
    ```

    Os 3 comandos confirmam visualmente: (1) help text correto, (2) `0.1.0` impresso, (3) número 6 (6 deps M1 detectadas via grep estrutural).
  </action>
  <verify>
    <automated>
      bash -c '
      set -euo pipefail
      cd /home/kalyllamarck/projetos/Doutorado/biblioteca_canonica

      # 7.1 — pytest verde (gate central)
      uv run pytest tests/ -v 2>&1 | tee /tmp/pytest_out.log
      grep -qE "3 passed" /tmp/pytest_out.log || { echo FAIL: pytest 3 testes não passaram; exit 1; }

      # 7.2 — ruff check
      uv run ruff check . || { echo FAIL: ruff lint; exit 1; }
      uv run ruff format --check . || { echo FAIL: ruff format; exit 1; }

      # 7.3 — mypy strict (apenas src/, tests/ pode ser permissivo)
      uv run mypy src/ || { echo FAIL: mypy strict em src; exit 1; }

      # 7.4 — uv sync idempotente
      uv sync 2>&1 | grep -qE "(Audited|Installed|Resolved)" || { echo FAIL: uv sync output suspect; exit 1; }

      # 7.5 — snapshot dos 4 success criteria
      OUT=$(.venv/bin/validar --help 2>&1)
      [ $? -eq 0 ] || { echo FAIL: validar --help; exit 1; }
      echo "$OUT" | grep -qF "biblioteca canônica" || { echo FAIL: help missing string; exit 1; }

      VERSION=$(.venv/bin/python -c "import biblio_validador; print(biblio_validador.__version__)")
      [ "$VERSION" = "0.1.0" ] || { echo "FAIL: version=$VERSION"; exit 1; }

      DEP_COUNT=$(grep -cE "^\s*\"(markdown-it-py|mdit-py-plugins|pylatexenc|typer|rich|loguru)" pyproject.toml || true)
      [ "$DEP_COUNT" = "6" ] || { echo "FAIL: dep count=$DEP_COUNT (expected 6)"; exit 1; }

      echo PASS: Phase 1 gate completo — 4 success criteria verificados.
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - **AC-3.1:** `uv run pytest tests/ -v` exit 0 com `3 passed` no output (CORE-12 success criteria #2, #3, #4 cobertos via Nyquist 2× — 8 assertions para 4 critérios).
    - **AC-3.2:** `uv run ruff check .` exit 0 (D-12 — zero violações de E/F/I/UP/B com `line-length=80`).
    - **AC-3.3:** `uv run ruff format --check .` exit 0 (D-12 — formatação ruff aplicada).
    - **AC-3.4:** `uv run mypy src/` exit 0 com `Success: no issues found` (D-13 — strict type checking nos 2 arquivos `__init__.py` + `cli.py`).
    - **AC-3.5:** `uv sync` (segunda invocação) exit 0 com mensagem `Audited` ou `Installed 0` (CORE-12 #1 — instalação idempotente).
    - **AC-3.6:** `.venv/bin/validar --help` exit 0 com `"biblioteca canônica"` no stdout (CORE-12 #2 — entry point + Typer help via `@app.callback()`).
    - **AC-3.7:** `.venv/bin/python -c "import biblio_validador; print(biblio_validador.__version__)"` retorna exatamente `0.1.0` (CORE-12 #3).
    - **AC-3.8:** `grep -cE '^\s*"(markdown-it-py|mdit-py-plugins|pylatexenc|typer|rich|loguru)' pyproject.toml` retorna `6` (CORE-12 #4 — verificação estrutural complementar ao test pytest).
    - **AC-3.9:** O verify script roda e imprime `PASS: Phase 1 gate completo — 4 success criteria verificados.`
  </acceptance_criteria>
  <done>
    Os 4 success criteria de CORE-12 estão verificados via 3 ferramentas independentes (pytest + ruff + mypy + manual snapshot). Nyquist 2× cumprido (cada criterion tem 2+ signals: pytest assertion + sanity check ad-hoc). Phase 1 está pronta para commit pelo orchestrator gsd-execute-phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem ↔ PyPI/Astral mirror | Primeira invocação de `uv sync` baixa Python 3.13.13 (~33.7MiB) do mirror Astral + ~16 wheels do PyPI. Único cruzamento de boundary nesta phase. Nenhum input de usuário, nenhum runtime de rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `uv.lock` (supply chain) | mitigate | `uv.lock` commitado desde Phase 1 (D-09) — pin determinístico de hashes de todas as deps transitivas. `uv` valida hash de cada wheel baixada contra o lockfile antes de instalar. Versões pinadas em `pyproject.toml` (D-06): `markdown-it-py>=4.0.0`, `pylatexenc>=2.10,<3.0`, `typer>=0.25.1`, `rich>=15.0.0`, `loguru>=0.7.3`, `anthropic>=0.97.0`, `jinja2>=3.1.6`, `pytest>=9.0.3`, `pytest-cov>=7.1.0`. Re-resolve só via `uv lock --upgrade` (ação explícita, commitável). |
| T-01-02 | Information Disclosure | `pyproject.toml` `authors` field | accept | `uv init` lê `git config user.name` (público — já no histórico do repo). Email NÃO incluído (PEP 621 schema permite ausência). Sem PII adicional vazada. CONTEXT.md já listou autoria pública. |
| T-01-03 | Tampering | `src/biblio_validador/__init__.py` ou `cli.py` overwrite por `uv init` defaults | mitigate | Pitfall 3 (severidade MÉDIA — RESEARCH.md): `uv init --package` gera `__init__.py` com `def main(): print("Hello...")` e `[project.scripts] biblio-validador = "biblio_validador:main"` — ambos contradizem D-03 e D-10. Mitigação: Task 1 lista 6 overrides explícitos com verify automatizado por grep (`! grep -qF "def main" src/biblio_validador/__init__.py`, `! grep -qF "biblio-validador = " pyproject.toml`). Drift silencioso bloqueado. |
| T-01-04 | Denial of Service | Auto-download Python 3.13 offline | accept | Pitfall 2 (severidade MÉDIA): primeira `uv sync` requer internet (~33.7MiB Astral mirror). Sem fallback offline em ambiente atual (Python 3.13 não instalado). Aceito porque: (a) projeto é local single-user com dev online; (b) one-time cost — após cache `~/.local/share/uv/python/`, todas as próximas syncs são offline; (c) documentado em RESEARCH.md "Environment Availability" como pré-requisito. |
| T-01-05 | Elevation of Privilege | `[project.scripts]` callable via `app.__call__` em vez de função | accept | Pitfall 7: PEP 503 spec aceita `module:object.attr` como callable, não restringe a funções. Typer.app implementa `__call__`. SEM risco de privilege escalation — entry point roda no mesmo UID do invocador. Mitigação preventiva: Task 1 verify proíbe `if __name__ == "__main__": app()` no `cli.py` (que duplicaria `app()` em module-level e quebraria testes via `SystemExit`). |
| T-01-06 | Spoofing | `--help` text content (Pitfall 1) | mitigate | Severidade ALTA documentada em RESEARCH.md: `@app.command()` único colapsa Typer em "single-command mode" e esconde `Typer(help=...)` (mostra docstring do comando). Smoke test poderia passar com exit 0 mas string "biblioteca canônica" ausente — falsa sensação de segurança. Mitigação: (a) Task 1 verify por grep (`! grep -E '^@app\.command\(' src/biblio_validador/cli.py`); (b) test_validar_help_exit_zero asserta `"biblioteca canônica" in result.output` — 2 layers redundantes (Nyquist 2×). |
| T-01-07 | Repudiation | Reprodutibilidade de builds entre máquinas | mitigate | `uv.lock` (D-09) commitado garante hash-pinned cross-platform reproducibility. Qualquer dev/CI que clonar o repo + rodar `uv sync` obtém EXATAMENTE as mesmas versões resolvidas. Sem este lockfile, `uv sync` faria nova resolução e poderia escolher versões diferentes em PyPI updates. |

## Phase 1 Risk Posture

Phase 1 é puramente estrutural (zero código de runtime). Threat surface efetivo:
- Sem network code em runtime (apenas build-time `uv sync`)
- Sem user input parsing (Typer skeleton apenas com `@app.callback()` placeholder)
- Sem auth/auth surface (CLI subcommands viriam em Phase 8/52)
- Sem secrets handling (`ANTHROPIC_API_KEY` é Phase 42+)
- Sem persistência (greenfield filesystem, sem DB/cache)

Todos os 7 threats mapeados são LOW (mitigados ou aceitos com rationale documentado). Nenhum HIGH/MEDIUM ativo. `block_on=high` config não dispara.
</threat_model>

<verification>
## Phase-level checks (run after Task 3)

1. `uv run pytest tests/ -v` → 3 passed, 0 failed (Nyquist 2× — 8 assertions cobrindo 4 success criteria via 3 testes consolidados em `test_cli.py`).
2. `uv run ruff check . && uv run ruff format --check .` → exit 0 (D-12).
3. `uv run mypy src/` → `Success: no issues found in 2 source files` (D-13).
4. `.venv/bin/validar --help` → exit 0, output contém `"biblioteca canônica"` (Pitfall 1 sanity).
5. `.venv/bin/python -c "import biblio_validador; print(biblio_validador.__version__)"` → exatamente `0.1.0` (D-03).
6. `grep -cE '^\s*"(markdown-it-py|mdit-py-plugins|pylatexenc|typer|rich|loguru)' pyproject.toml` → exatamente `6` (D-06).
7. `git ls-files --others --exclude-standard | wc -l` → 0 (todos os artefatos esperados estão tracked, nenhum untracked do tooling vazou — Pitfall 6 mitigado).
8. `git diff --stat` → mostra apenas as adições esperadas (NEW files: pyproject.toml, .python-version, uv.lock, src/biblio_validador/{__init__,cli}.py, tests/{__init__,test_cli}.py; MODIFIED: .gitignore append-only).

## Validation Architecture compliance

- VALIDATION.md "Wave 0 Requirements" — fechados pelos 3 testes em `tests/test_cli.py`.
- VALIDATION.md "Per-Task Verification Map" — cada AC referencia o critério (CORE-12 #1/#2/#3/#4).
- VALIDATION.md "Manual-Only Verifications" — `test -x .venv/bin/validar` coberto por AC-2.2; `git ls-files | grep uv.lock` coberto pelo orchestrator no commit do plano (gsd-execute-phase comita `uv.lock` automaticamente).
</verification>

<success_criteria>
Phase 1 declarada COMPLETA quando os 4 success criteria de ROADMAP §"Phase 1: Bootstrap" estão verificados de forma independente:

1. **`uv sync` em diretório limpo instala todas as dependências sem erro** — verificado em Task 2 (passo 4) e Task 3 (passo 4 idempotente).
2. **`validar --help` retorna usage sem error code** — verificado em Task 2 (sanity check direto) + Task 3 (`test_validar_help_exit_zero` via CliRunner in-process).
3. **`src/biblio_validador/__init__.py` existe com versão `0.1.0`** — verificado em Task 1 (grep) + Task 3 (`test_version_exposed` via import).
4. **`pyproject.toml` declara todas as dependências do SUMMARY.md com versões fixadas** — verificado em Task 2 (tomllib parsing) + Task 3 (`test_pyproject_declares_m1_deps` + grep estrutural com count=6).

Cada criterion tem ≥ 2 signals independentes (Nyquist 2× cumprido).
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap/01-01-SUMMARY.md` listing:
- Files created/modified (ROUGH expected: 7 new + 1 modified = 8 total)
- Verification commands executed and outputs (snippets das saídas relevantes)
- Decisions actually applied (cross-reference D-01 a D-19)
- Pitfalls actively mitigated (referenciar Pitfalls 1, 2, 3, 4, 5, 6, 7 com mitigation aplicada)
- Carryover into Phase 2 (parser markdown — `loguru.logger.add()` config primeiro consumidor; `unicodedata.normalize` no parser; D-19)
- Next phase: Phase 2 (Parser Markdown — CORE-01 + CORE-11)
</output>
